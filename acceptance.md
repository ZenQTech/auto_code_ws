以下是根据需求文档 V2.0 及架构批判分析结果，对原有验收标准进行审查后补全的完整版本。所有补充内容均以 **【补充】** 标注，其余部分保持原样。补全主要覆盖：联合系统验收（跨项目）、附录测试环境与工具要求、部分缺失的量化指标及验证方法。

---

# 验收标准.md（补全版）

## 目录

1. [项目一：智能仓库调度系统可视化平台（前端）](#项目一智能仓库调度系统可视化平台前端)  
   1.1 [模块级验收](#11-模块级验收)  
   1.2 [集成验收](#12-集成验收)  
   1.3 [系统级验收](#13-系统级验收)  
   1.4 [安全验收](#14-安全验收)  
2. [项目二：AGV集群调度系统（机器人全栈）](#项目二agv集群调度系统机器人全栈)  
   2.1 [模块级验收](#21-模块级验收)  
   2.2 [集成验收](#22-集成验收)  
   2.3 [系统级验收](#23-系统级验收)  
   2.4 [安全验收](#24-安全验收)  
3. [联合系统验收（跨项目）](#3-联合系统验收跨项目)  
4. [附录：测试环境与工具要求](#4-附录测试环境与工具要求)

---

## 项目一：智能仓库调度系统可视化平台（前端）

### 1.1 模块级验收

#### 1.1.1 AGV 实时位置地图模块
- **数据刷新验收**：使用 WebSocket 模拟后端发送位置数据（频率≥30Hz），前端地图上 AGV 图标移动平滑无卡顿，帧率≥30fps（通过 Chrome DevTools Performance 面板测量）。**【补充】同时验证 WebSocket 接收端每秒实际接收的消息数≥30，且无丢包（通过计数器对比发送与接收数量）。**
- **状态标识验收**：AGV 图标可正确显示四种状态（运行中、待命、充电、故障），状态切换颜色/图标变化在 1 秒内生效。
- **交互操作验收**：支持地图缩放（鼠标滚轮/手势）、平移（拖拽），缩放范围 0.5x~3x 无异常，平移无边界断裂。
- **异常处理验收**：手动断开 WebSocket 连接后，3 秒内前端显示“连接断开”提示，并自动按指数退避重连（1s, 2s, 4s, 8s, 16s, 30s），重连成功后提示消失且数据恢复。**【补充】验证重连间隔序列的精确性：通过日志记录每次重连的实际间隔，允许误差±200ms。**

#### 1.1.2 任务队列监控模块
- **状态展示验收**：任务列表正确显示四种状态（待执行、执行中、已完成、已取消），每个状态计数准确。
- **筛选排序验收**：支持按任务 ID（模糊匹配）、任务类型（下拉选择）、优先级（升/降序）筛选，结果实时更新，响应时间≤200ms。
- **详情展示验收**：点击任务行弹出详情弹窗，包含任务 ID、起始点、目标点、分配 AGV、任务状态、预计完成时间、实际开始/结束时间，**【补充】字段格式正确（预计完成时间、实际开始/结束时间采用 ISO 8601 格式，未发生的事件显示为“-”）**。
- **异常通知验收**：模拟后端推送任务分配失败或执行异常事件，前端 1 秒内弹出 Toast 提示并在日志面板中记录，日志包含时间戳、错误码、详细信息。

#### 1.1.3 设备状态面板模块
- **状态展示验收**：面板显示所有已注册设备（AGV、充电桩、门禁等），每行显示设备名称、在线/离线状态（图标/颜色区分）、运行模式、电量（百分比）、负载（百分比或数值）、故障码（若无故障显示“无”）。
- **远程操作验收**：管理员角色可对 AGV 执行“暂停/恢复”操作，对充电桩执行“开启/关闭”，操作后 2 秒内设备状态面板更新，且后端确认指令已执行（通过 WebSocket 回执）。
- **权限控制验收**：操作员角色可看到但无法点击远程操作按钮（按钮置灰），观察员角色完全隐藏操作按钮。

#### 1.1.4 核心算法集成模块
- **三级执行策略展示验收**：地图上每个 AGV 任务卡片显示当前策略（快速/均衡/节能），策略切换按钮仅在管理员/操作员角色下可见，点击后后端返回确认，2 秒内 UI 更新。
- **最少负载调度展示验收**：设备面板或专用侧边栏显示每个 AGV 的负载指标（任务队列长度、CPU 使用率、内存使用率），新任务分配时高亮显示被选中的 AGV 及其负载排名（如“最少负载：AGV-003”）。

#### 1.1.5 用户认证与授权模块
- **登录认证验收**：输入有效凭据后，2 秒内收到 JWT Token，页面跳转至主界面。无效凭据（用户名/密码错误）显示明确错误提示，Token 不生成。
- **Token 携带验收**：所有 API 请求（包括 WebSocket 连接）在请求头中验证是否携带 `Authorization: Bearer <token>`，缺失或无效 Token 返回 401，前端自动跳转登录页。
- **角色权限验收**：分别以管理员、操作员、观察员身份登录，验证：
  - 管理员：可执行所有操作（远程控制、策略切换、设备管理）。
  - 操作员：可查看所有数据，可切换策略，不可远程控制设备。
  - 观察员：仅可查看数据，所有操作按钮不可用。

### 1.2 集成验收

- **WebSocket 与地图集成**：后端通过 WebSocket 连续推送 100 个 AGV 数据（位置、状态），前端地图同时渲染，帧率≥30fps，数据延迟≤100ms（从后端发送到前端渲染完成，测量方法：在后端发送时记录时间戳，前端渲染后通过 console.time 对比）。
- **任务队列与后端集成**：模拟后端任务状态变更（5 个任务同时更新），前端列表在 1 秒内刷新，筛选器仍正常工作。
- **设备操作与后端集成**：执行远程操作（暂停 AGV），后端返回操作结果，前端面板状态更新，且操作日志在日志面板中出现。
- **认证与 WebSocket 集成**：WebSocket 连接建立时携带 Token，Token 过期后自动断开连接并跳转登录页；Token 有效时连接正常，断开后自动重连并携带新 Token。

### 1.3 系统级验收

- **首屏加载性能**：在 Chrome DevTools 模拟 3G 网络（Fast 3G 预设）下，首屏完全加载时间≤2 秒（通过 Performance 面板测量，以 window.onload 结束为准）。
- **高并发渲染性能**：一次性加载 100 个 AGV（通过 WebSocket 推送）后，页面帧率≥30fps（使用 FPS 计数器或 Chrome DevTools Rendering 面板），持续 30 秒无明显下降。
- **分辨率兼容性**：在 1920×1080, 1440×900, 768×1024 三种分辨率下，页面布局无错乱、元素无重叠，滚动条正常显示。浏览器窗口缩放 75%~150% 时，自适应正常，无遮挡或溢出。

### 1.4 安全验收

- **未授权访问**：直接访问 API 接口（如 `/api/tasks`）不携带 Token，返回 401，前端页面不暴露任何数据。
- **Token 过期处理**：Token 过期（24 小时后或手动修改时间戳），前端自动弹窗提示“登录已过期”，并跳转至登录页，用户需重新登录。
- **XSS/CSRF 防护**：所有用户输入（如任务筛选输入框）经过编码，禁止执行任意脚本；API 使用 SameSite Cookie 或 Token 机制防止 CSRF。**【补充】验证方法：在输入框中提交 `<script>alert('xss')</script>`，确认页面未弹出对话框；跨域请求（如不同 origin 的 POST）应被拒绝并返回 403。**

---

## 项目二：AGV 集群调度系统（机器人全栈）

### 2.1 模块级验收

#### 2.1.1 多机协同任务调度模块
- **最少负载调度算法验收**：在 3 台 AGV 负载不同（任务队列长度分别为 1, 3, 5）时，提交新任务，算法选择负载最少的 AGV（队列长度 1 的那台），验证任务分配结果。
- **任务优先级抢占验收**：提交低优先级任务分配给 AGV-1，执行中提交高优先级任务，AGV-1 中断当前任务，执行高优先级任务，被中断任务重新排队，前端任务状态显示“暂停”。
- **拓扑排序循环依赖检测验收**：提交三个任务 A→B, B→C, C→A（循环依赖），系统应在 1 秒内返回错误提示“检测到循环依赖，任务链无法执行”，并拒绝分配。

#### 2.1.2 路径规划与导航模块
- **三级执行策略路由算法验收**：
  - 快速模式：给定起点和终点，路径长度≤最短路径的 105%。
  - 均衡模式：路径长度与能耗综合指标最优（通过测试脚本验证）。
  - 节能模式：路径能耗最低（通过预设能耗模型计算）。
  - **【补充】任务复杂度评分验证：输入包含路径长度、操作难度、时间窗口紧急性、环境拥堵度四个特征，评分结果在配置文件中定义的阈值范围内，且可通过配置文件调整。验证方法：修改配置文件中的阈值，重新提交相同任务，确认策略选择发生变化。**
- **动态避障验收**：AGV 行进中，在路径上放置一个临时障碍物（高度≥0.3m 的箱子），AGV 应能在 3 秒内规划出绕行路径并继续前进，不发生碰撞。

#### 2.1.3 避障与安全模块
- **实时避障验收**：基于 LiDAR 数据，在 0.5m 距离内检测到障碍物时，AGV 减速至停止，距离障碍物≥0.3m。
- **物理碰撞检测验收**：人为触碰 AGV 碰撞传感器，AGV 立即急停（响应时间≤50ms），并发出急停警报。
- **虚拟安全区验收**：其他物体进入 AGV 周围 0.3m 安全区时，AGV 减速或停止，离开后自动恢复。
- **速度/加速度/扭矩三层限幅验收**：在任何模式下（包括手动控制），发送超出安全红线的指令（如速度 1.2 m/s），系统拦截并告警，AGV 实际运行参数始终≤红线值。

#### 2.1.4 紧急停止模块（【补充】本模块为从架构批判分析中补充的安全要求）
- **物理急停按钮验收**：按下 AGV 本体急停按钮，AGV 在 50ms 内停止，电机断电，LED 指示灯变为红色并闪烁。
- **远程急停指令验收**：通过调度系统发送远程急停指令，所有 AGV 在 50ms 内停止并进入急停状态。
- **碰撞检测自动急停验收**：触发碰撞传感器，自动进入急停状态，与物理按钮效果相同。
- **手动复位验收**：急停后，必须通过物理按钮或调度系统手动复位才能恢复运行，复位后 AGV 可正常接收任务。

### 2.2 集成验收

- **任务调度与路径规划集成**：分配任务给 AGV 后，AGV 自动规划路径并开始导航，任务状态从“待执行”变为“执行中”，路径规划时间≤500ms（地图 1000m² 内）。
- **避障与调度集成**：AGV 避障时，调度系统能实时更新 AGV 位置和状态，任务预计完成时间自动调整（误差≤10%）。
- **急停与日志集成**：任何急停触发后，系统日志记录触发时间、触发源（物理按钮/远程指令/碰撞检测）、操作者身份（若有），日志可查询。
- **数据库与调度集成**：使用 PostgreSQL 数据库，在 10 个并发任务写入场景下，无“database is locked”错误，写入延迟≤100ms。

### 2.3 系统级验收

- **多机协同任务完成率**：使用 3 台 AGV 连续执行 1000 个随机任务（排除机械故障、人为干预），任务完成率≥99%，无死锁、无冲突。
- **避障成功率**：在正常仓库环境（含静态障碍物 5 个、动态障碍物 3 个）下，运行 100 次避障测试，成功次数≥99.9%。
- **稳定性验收**：系统连续运行 7 天×24 小时，无任何进程崩溃、无内存泄漏（内存增长≤5%）、无“database is locked”错误。**【补充】内存增长测量方法：使用 `valgrind` 或系统监控工具（如 `top` / `htop`）记录初始内存和结束内存，增长百分比 = (结束 - 初始) / 初始 × 100%。**
- **性能验收**：
  - 急停响应时间≤50ms（通过日志时间戳测量，从触发到电机断电反馈）。
  - 路径规划计算时间≤500ms（地图 1000m² 内，20 个随机任务平均）。
  - WebSocket 调度指令下发延迟≤100ms（从调度系统发送到 AGV 接收回执的时间差）。

### 2.4 安全验收

- **运动参数安全红线**：连续记录 1 小时运行数据，最大直线速度≤1.0 m/s，最大角速度≤0.5 rad/s，最大加速度≤0.5 m/s²，**【补充】最大扭矩≤安全阈值（由 AGV 固件定义，此处建议补充具体数值，如 5 N·m）**。若超出则触发告警并记录。
- **【补充】固件升级安全**：固件升级过程中，必须验证签名，防止未授权固件刷入。升级失败后自动回滚至上一版本。
- **【补充】通信加密**：调度系统与 AGV 之间的所有控制指令和状态数据必须通过 TLS 1.2+ 加密传输，验证方法：使用 Wireshark 抓包确认无明文传输。

---

## 3. 联合系统验收（跨项目）【补充】

本节验证前端可视化平台与后端 AGV 集群调度系统之间的端到端协同工作。

### 3.1 功能集成验收
- **端到端任务流程**：在调度系统创建一个搬运任务（A→B），前端可视化平台应在 2 秒内显示该任务并更新 AGV 状态，AGV 开始移动后地图位置实时更新，任务完成后前端状态变更为“已完成”。
- **远程操作同步**：前端执行“暂停 AGV”操作，调度系统应实际暂停对应 AGV，前端面板状态在 2 秒内更新为“暂停”，且调度系统日志记录操作者信息。
- **策略切换一致性**：前端切换三级执行策略，调度系统应重新规划路径并返回确认，前端在 2 秒内显示新策略，AGV 按新策略行驶。

### 3.2 性能集成验收
- **端到端延迟**：从调度系统发送任务分配指令到前端地图上 AGV 状态更新（包括位置、任务状态），延迟≤200ms（测量方法：调度系统发送时记录时间戳，前端渲染后通过日志回传对比）。
- **高并发集成**：调度系统同时下发 100 个 AGV 的实时位置更新（频率 30Hz），前端地图帧率≥30fps，且调度系统无指令积压（CPU 使用率≤70%）。

### 3.3 安全集成验收
- **跨系统认证一致性**：前端 Token 与调度系统共享验证机制，Token 过期后前端重定向至登录页，同时调度系统拒绝该 Token 的后续请求。
- **数据完整性**：前端与调度系统之间的 WebSocket 数据包需包含校验和（如 CRC32），接收端验证，发现损坏则丢弃并请求重传。

---

## 4. 附录：测试环境与工具要求【补充】

### 4.1 硬件环境
- **前端测试**：标准 PC（Intel i5 及以上，8GB RAM，SSD），显示器分辨率支持 1920×1080 / 1440×900 / 768×1024。
- **后端测试**：服务器（4 vCPU，16GB RAM，SSD），运行 Ubuntu 22.04 LTS。
- **AGV 模拟器**：使用 3 台真实 AGV 或同等性能的仿真器（如 Gazebo + ROS 2），支持 LiDAR 数据、碰撞传感器、急停按钮等。

### 4.2 软件环境
- **浏览器**：Chrome 118+（最新稳定版）、Firefox 118+、Edge 118+。
- **网络模拟**：Chrome DevTools 预设“Fast 3G”（下载 1.5 Mbps，上传 0.75 Mbps，延迟 100ms）。
- **性能工具**：Chrome DevTools Performance 面板、Lighthouse（首屏性能）、FPS 计数器（如 `stats.js`）。
- **负载测试工具**：JMeter 5.5+ 模拟并发 WebSocket 连接；Locust 模拟任务调度压力。
- **数据库**：PostgreSQL 15+，配置最大连接数 100。
- **监控工具**：`htop`、`valgrind`（内存泄漏检测）、`Wireshark`（网络抓包）。
- **自动化测试框架**：Playwright（前端 E2E 测试）、pytest（后端单元/集成测试）。

### 4.3 测试数据
- **AGV 数据**：100 台 AGV 的模拟数据，包含位置（经纬度或网格坐标）、朝向角、状态、负载指标。
- **任务数据**：1000 个随机任务，包含任务 ID、优先级、起始点、目标点、时间窗口、依赖关系（含循环依赖案例）。
- **障碍物数据**：5 个静态障碍物（位置、尺寸）、3 个动态障碍物（移动路径、速度）。
- **用户数据**：管理员、操作员、观察员各 1 个账户，密码复杂度符合要求。

### 4.4 计量与通过条件
- 所有验收项必须通过自动化测试或人工测试的明确记录。
- 性能指标需在测试环境中连续运行 3 次，取平均值，且单次结果不得低于阈值的 90%。
- 安全测试中，任何漏洞（如 XSS 执行成功）视为不通过，需修复后重新验证。

---

**补全说明**：以上内容中，所有以 **【补充】** 标注的部分均为本次审查后新增或调整的内容，其余部分保持原验收标准不变。补充内容覆盖了缺失的功能点（如紧急停止模块、联合系统验收）、量化指标（如数据刷新频率、内存增长测量）、测试环境与工具要求，并增强了可执行性。