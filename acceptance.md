以下是对验收标准的审查与补全。根据需求文档 V2.0，现有验收标准已覆盖大部分功能点，但存在以下缺失或不够完整之处：

1. **系统级验收**中 `3.2 多AGV协同` 被截断，缺少剩余验收标准；`3.3 紧急停止`、`3.4 性能验收`、`3.5 可靠性验收` 仅有标题无内容。
2. **安全验收**中 `4.1 认证与授权`、`4.2 输入验证`、`4.3 通信安全`、`4.4 紧急停止安全`、`4.5 人工干预回滚安全` 仅有标题无具体测试方法与预期结果。
3. **可维护性**相关需求（日志系统、CLI版本管理、模块化）未在验收标准中体现，建议补充。
4. 部分非功能指标（如连续运行72小时、CPU占用率、数据库重试机制）未量化在验收标准中。

下面输出补全后的验收标准，新增或修改的内容以 **【补充】** 标注。原有内容保持不变，仅补充缺失部分。

---

# 验收标准.md（补全版）

## 目录

1. [模块级验收](#1-模块级验收)  
   1.1 [前端可视化模块](#11-前端可视化模块)  
   1.2 [后端调度服务模块](#12-后端调度服务模块)  
   1.3 [仿真环境模块](#13-仿真环境模块)  
2. [集成验收](#2-集成验收)  
   2.1 [前后端集成](#21-前后端集成)  
   2.2 [后端与仿真集成](#22-后端与仿真集成)  
   2.3 [前端与仿真间接集成](#23-前端与仿真间接集成)  
3. [系统级验收](#3-系统级验收)  
   3.1 [单AGV导航](#31-单agv导航)  
   3.2 [多AGV协同](#32-多agv协同)  
   3.3 [紧急停止](#33-紧急停止)  
   3.4 [性能验收](#34-性能验收)  
   3.5 [可靠性验收](#35-可靠性验收)  
4. [安全验收](#4-安全验收)  
   4.1 [认证与授权](#41-认证与授权)  
   4.2 [输入验证](#42-输入验证)  
   4.3 [通信安全](#43-通信安全)  
   4.4 [紧急停止安全](#44-紧急停止安全)  
   4.5 [人工干预回滚安全](#45-人工干预回滚安全)  
5. 【补充】[可维护性验收](#5-可维护性验收)  

---

## 1. 模块级验收

（原有内容不变，此处省略重复，仅展示新增部分）

### 1.2 后端调度服务模块

【补充】在现有表格后增加以下条目：

| 验收标准 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 日志系统：使用ROS2日志框架，记录任务下发、状态变更、异常等关键事件，日志级别可配置 | 执行任务下发、AGV状态变更、模拟异常触发，检查日志输出 | 日志文件包含对应事件的时间戳、级别、内容，无遗漏；通过修改日志配置后，低级别日志不输出 |
| CLI工具版本管理：启动时自动校验Nav2、SLAM_Toolbox等依赖版本，与部署文档中最低版本一致 | 修改依赖版本（模拟低于最低版本），启动系统；检查日志或启动脚本输出 | 启动失败并提示版本不匹配，日志记录具体版本差异 |

### 1.3 仿真环境模块

【补充】在现有表格后增加以下条目：

| 验收标准 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 仿真时间同步：与ROS2时间同步，无漂移 | 使用 `ros2 topic echo /clock` 检查，对比仿真时间与系统时钟 | 仿真时间与ROS2时间一致，长期运行无累积偏差（偏差≤1ms/h） |

## 2. 集成验收

（原有内容不变，此处省略）

## 3. 系统级验收

### 3.1 单AGV导航

（原有内容不变）

### 3.2 多AGV协同

【补充】接续原有截断内容，补全以下条目：

| 验收标准 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 5台AGV同时运行，分配不同起点和目标点，在交叉区域无碰撞完成全部任务 | 在Gazebo中启动5台AGV，各分配不同任务，任务路径包含至少2个交叉区域 | 所有AGV在任务过程中无碰撞，最终全部到达目标点 |
| 任务完成率≥95% | 运行10次任务组，统计成功完成的任务数 | 成功完成≥95%任务（即10次中至少9.5次，可四舍五入为10次中至少10次成功即100%，但允许1次失败，故实际应≥95%） |
| 碰撞次数为0 | 记录每次运行中AGV之间的碰撞事件（通过Gazebo碰撞检测或日志） | 所有运行中碰撞次数为0 |
| 三级执行策略路由：复杂任务分解为规划、执行、校验三级子任务，由不同AGV协作完成，无死锁 | 下发一个包含复杂路径（需多段规划）的任务，检查子任务调度日志 | 子任务按顺序执行，不同AGV分别承担不同阶段，最终任务完成，无死锁或阻塞 |
| 拓扑排序循环依赖检测：拒绝产生循环依赖的任务组 | 通过API提交一组存在循环依赖的任务（如A依赖B，B依赖C，C依赖A） | 后端返回400，提示“依赖关系存在循环，无法调度” |

### 3.3 紧急停止

【补充】新增完整表格：

| 验收标准 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 全局紧急停止：按下按钮后，所有AGV在0.5秒内停止运动并切断动力 | 在仿真中使多台AGV运动，在前端点击紧急停止按钮，确认弹窗后发送指令 | 所有AGV在0.5秒内速度归零，Gazebo中AGV停止移动，日志记录时间戳偏差≤50ms |
| 紧急停止指令从后端发出到AGV确认停止的响应时间≤300ms | 测量后端发布指令时间戳与AGV节点确认停止时间戳之差 | 延迟≤300ms |
| 紧急停止后，按钮变灰不可重复点击，且前端显示确认弹窗 | 观察前端UI | 按钮变灰，弹窗显示“确认紧急停止？” |
| 紧急停止后，人工干预管理器可撤销CLI操作并回滚数据库事务 | 在紧急停止前执行一个正在进行的任务，停止后调用人工干预回滚API | 子进程终止，数据库事务回滚至停止前状态，无僵尸进程，无资源泄露 |

### 3.4 性能验收

【补充】新增表格：

| 验收标准 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 前端页面加载时间≤2秒（首次加载，含地图渲染） | 使用Chrome开发者工具，网络模拟100Mbps，清除缓存后加载页面 | 首次加载时间≤2秒 |
| REST API响应时间≤500ms（95%分位），单次请求不超过1秒 | 使用JMeter或Locust模拟10个并发用户，连续调用任务下发、状态查询等API，测量响应时间 | 95%分位响应时间≤500ms，最大响应时间≤1秒 |
| WebSocket推送延迟≤1秒（状态变更到前端显示） | 在Gazebo中移动AGV，记录后端发布位置变更时间与前端显示时间差 | 延迟≤1秒 |
| 路径规划单次计算时间≤2秒（10台AGV同时请求） | 并发10次路径规划请求，测量响应时间 | 95%分位≤2秒，最大≤3秒 |
| 数据库写入并发：5个并发写入请求平均延迟≤200ms，无超时 | 使用并发脚本同时写入5条任务记录到SQLite（WAL模式+异步队列），测量延迟 | 平均延迟≤200ms，无超时失败，写入成功率100% |

### 3.5 可靠性验收

【补充】新增表格：

| 验收标准 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 单AGV在Gazebo仿真中，从起点到终点无碰撞完成10次独立导航，路径规划成功率100% | 同3.1 | 10次均成功，无碰撞 |
| 5台AGV在复杂场景（含动态障碍物）下同时运行，任务完成率≥95%，碰撞次数0 | 同3.2 | 完成率≥95%，碰撞0 |
| 系统连续运行72小时无重启，无内存泄漏，CPU占用率≤80% | 启动所有组件，运行72小时，每小时记录内存和CPU使用情况 | 系统无自动重启，内存使用不持续增长（通过监控工具如`top`、`htop`），CPU占用率≤80% |
| 数据库写入失败自动重试3次 | 模拟数据库写入失败（如临时断开文件权限），观察日志 | 日志显示重试3次，最终成功写入（或失败后记录错误） |
| 紧急停止后，后端能够正确撤销正在执行的CLI操作，防止僵尸进程和资源泄露 | 在仿真中执行长时间路径规划任务，触发紧急停止后检查进程列表和数据库事务 | 相关子进程已终止，无残留进程，数据库事务回滚，无锁表 |

## 4. 安全验收

### 4.1 认证与授权

【补充】新增表格：

| 验收标准 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 未授权访问返回401 | 不携带Token访问任何API（除登录外） | 后端返回401 Unauthorized，前端页面重定向至登录页 |
| 越权操作返回403 | 使用操作员角色Token访问管理员专属API（如人工干预回滚） | 后端返回403 Forbidden |
| Token过期后需重新登录 | 使用过期Token访问API，或等待Token过期后操作 | 后端返回401，前端提示Token过期并跳转至登录页 |
| 登录成功后签发JWT Token，前端携带Token访问所有API | 检查登录API响应及后续请求的Authorization头 | 登录API返回Token，后续请求头包含`Authorization: Bearer <token>` |

### 4.2 输入验证

【补充】新增表格：

| 验收标准 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 后端对前端传入的所有坐标、任务参数进行合法性校验，防止注入攻击 | 发送包含恶意脚本或SQL注入字符的坐标（如`0; DROP TABLE`），或超出地图范围的坐标 | 后端返回400，提示参数无效，不执行任何操作，无数据库异常 |
| 任务参数校验：目标点坐标必须在20m×20m地图范围内 | 发送超出地图边界的坐标 | 后端返回400，提示“坐标超出地图范围” |
| 防止路径规划参数注入：路径规划请求中的特殊字符被过滤 | 在目标点描述中传入`<script>alert(1)</script>` | 后端忽略或转义，规划正常执行，不触发XSS |

### 4.3 通信安全

【补充】新增表格：

| 验收标准 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 所有通信采用HTTPS/WSS | 检查前端页面URL协议，以及WebSocket连接协议 | 生产环境使用`https://`和`wss://`，开发环境可通过配置决定 |
| 敏感信息（如Token）不记录日志 | 检查日志输出，确保不包含Token明文 | 日志中Token被脱敏或完全隐藏 |
| 前后端通信使用TLS加密 | 使用Wireshark抓包，检查数据包是否加密 | 无法直接读取明文数据 |

### 4.4 紧急停止安全

【补充】新增表格：

| 验收标准 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 紧急停止指令在0.5秒内发布至所有AGV，日志记录时间戳 | 参考3.3 | 同3.3 |
| 紧急停止后，AGV动力切断，速度指令归零 | 检查Gazebo中AGV速度话题 | `/cmd_vel` 发布速度为零，AGV停止 |
| 紧急停止按钮仅允许管理员或操作员使用 | 以观察者角色登录，检查紧急停止按钮是否可用 | 按钮不可点击或点击后提示无权限 |

### 4.5 人工干预回滚安全

【补充】新增表格：

| 验收标准 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 人工干预管理器提供API允许管理员暂停/取消/回滚任务，并记录操作日志 | 调用暂停任务API，然后取消，最后回滚，检查日志 | 任务状态正确变更，数据库回滚至前状态，操作日志记录完整（包含操作者、时间、操作类型） |
| 回滚操作后，系统状态与回滚前一致，无残留数据 | 回滚后检查数据库和AGV状态 | 状态与回滚前一致，无异常 |
| 紧急停止后，CLI子进程被终止，数据库事务回滚，防止僵尸进程和资源泄露 | 模拟一个长时间运行的CLI任务，触发紧急停止后检查进程 | 子进程被终止，无僵尸进程，数据库连接正常释放 |

## 5. 【补充】可维护性验收

| 验收标准 | 测试方法 | 预期结果 |
|---------|---------|---------|
| 代码模块化：核心算法（路径规划、负载均衡、拓扑排序）封装为独立Python包，提供单元测试 | 检查项目结构，运行单元测试 | 每个核心算法有独立模块，单元测试覆盖所有核心函数，测试通过率100% |
| 日志系统：使用ROS2日志框架，记录所有关键事件，日志级别可配置 | 见1.2后端模块 | 同1.2 |
| CLI工具版本管理：启动时自动校验依赖版本，与部署文档一致 | 见1.2后端模块 | 同1.2 |
| 部署文档明确列出所有依赖的最低版本，并提供启动时版本校验脚本 | 检查部署文档和启动脚本 | 文档中包含版本要求，启动脚本执行版本比对，不匹配时给出错误提示 |

---

**补充说明**：以上内容中，所有以 `【补充】` 标注的部分均为新增或对原有截断内容的补全。原有验收标准条目未做修改，保持了原有编号和顺序。建议将补全后的标准合并到原始文档中，形成完整的验收标准。