好的，以下是根据需求文档 V2.0 对现有验收标准进行审查后的补全版本。所有补充内容均以 **【补充】** 标注，并遵循原有表格结构。补全覆盖了缺失的功能点、非功能需求（安全、可维护性、可用性）、量化指标、测试环境与工具要求，确保标准具备可执行性与可衡量性。

---

# 验收标准.md（补全版）

## 1. 文档概述

本文档基于《需求文档 V2.0》及架构批判分析结果，定义 AGV 调度系统的验收标准。验收分为四个层级：**模块级验收**（独立验证各模块功能与接口）、**集成验收**（验证模块间协作与数据流）、**系统级验收**（验证整体功能、性能、可靠性、可维护性、可用性、安全性）、**安全验收**（验证安全机制与防护能力）。所有测试均在 Gazebo Ignition 仿真环境中执行，使用 3 台 AGV（扩展测试时使用 5 台）。

---

## 2. 模块级验收

### 2.1 任务管理与调度模块

| 验收项 | 验收方法 | 预期结果 | 参考需求 |
|--------|----------|----------|----------|
| **任务导入** | 提供 JSON/CSV 格式任务列表文件，包含 50 个任务（起点、终点、优先级、执行时间约束等属性） | 系统成功解析并存入待分配队列，无格式错误导致崩溃 | 2.1 - 任务输入 |
| **最少负载调度算法** | 启动 3 台 AGV，提交 10 个任务，检查分配结果 | 算法根据负载评估函数（任务队列长度、电量、距离）分配，负载均衡（各 AGV 任务数差值 ≤ 2） | 2.1 - 任务分配 |
| **任务依赖处理** | 提交 5 个具有依赖关系的任务（B 依赖 A，C 依赖 B，D 依赖 A，E 依赖 C） | 系统通过拓扑排序生成合法执行序列，检测无环；任务按序执行，依赖任务在前提完成后才启动 | 2.1 - 任务依赖处理 |
| **循环依赖检测** | 提交包含循环依赖的任务图（A→B, B→C, C→A） | 系统拒绝提交，返回错误提示“检测到循环依赖，请修正任务依赖关系” | 2.1 - 循环依赖检测 |
| **任务状态跟踪** | 通过 API 查询任务状态（待分配、执行中、完成、失败） | 状态变化实时更新，查询响应时间 ≤ 200ms | 2.1 - 任务状态跟踪 |
| **【补充】输入校验（防注入）** | 提交包含恶意脚本的任务 JSON（如 `"起点":"'; DROP TABLE tasks;--"`） | 系统拒绝，返回错误信息，不执行，不写入数据库；日志记录告警 | 3.3 - 输入校验 |
| **【补充】任务超时约束** | 提交一个执行时间约束为 1 秒的任务，但 AGV 因拥堵无法按时完成 | 系统标记任务为“超时失败”，重新分配或触发告警 | 2.1 - 任务输入 |

### 2.2 路径规划模块

| 验收项 | 验收方法 | 预期结果 | 参考需求 |
|--------|----------|----------|----------|
| **全局路径规划（A*）** | 在静态地图上指定起点和终点，调用路径规划接口 | 返回一条从起点到终点的无碰撞路径，计算时间 ≤ 0.5 秒 | 2.2 - 第一级 |
| **局部重规划（Dijkstra）** | 在 AGV 运行路径上手动放置动态障碍物，触发重规划 | 局部路径避开障碍物，计算时间 ≤ 0.2 秒 | 2.2 - 第二级 |
| **避障与微调** | 使用激光雷达模拟前方 0.5m 处障碍物，AGV 接近时 | 路径点平滑调整，AGV 减速至 0.3m/s 以下，不碰撞障碍物 | 2.2 - 第三级 |
| **多 AGV 协同路径** | 3 台 AGV 同时从不同起点出发，目标点交叉 | 路径规划避免死锁，无路径冲突（AGV 间距离始终 ≥ 0.3m） | 2.2 - 多AGV协同 |
| **【补充】路径规划配置动态调整** | 修改配置文件中的全局路径规划算法阈值（如 A* 启发式权重），不重启系统 | 后续路径规划使用新参数，结果发生变化 | 3.4 - 配置管理 |

### 2.3 交通管理模块

| 验收项 | 验收方法 | 预期结果 | 参考需求 |
|--------|----------|----------|----------|
| **路口协商** | 在交叉路口同时有两台 AGV 接近，优先级不同（任务紧急程度和距离） | 高优先级 AGV 优先通过，低优先级 AGV 等待，不产生冲突 | 2.3 - 路口协商 |
| **防碰撞机制** | 两台 AGV 相向而行，间距 0.5m 时持续接近 | 当距离 < 0.3m 时触发减速，< 0.1m 时触发急停（停止运动） | 2.3 - 防碰撞 |
| **死锁检测与解除** | 构造循环等待场景（AGV 1 等待 AGV 2 释放路径，AGV 2 等待 AGV 1） | 系统在 1 秒内检测到死锁，自动重分配任务或重规划路径，解除死锁 | 2.3 - 死锁检测与解除 |
| **【补充】防碰撞距离可配置** | 修改配置文件中的防碰撞触发距离为 0.2m（减速）和 0.05m（急停），重复上述测试 | 新阈值生效，当距离 0.2m 时减速，0.05m 时急停 | 2.3 - 防碰撞（隐含可配置） |

### 2.4 通信与状态监控模块

| 验收项 | 验收方法 | 预期结果 | 参考需求 |
|--------|----------|----------|----------|
| **ROS 通信频率** | 监控 AGV 状态发布频率 | AGV 状态更新频率 ≥ 10Hz | 2.4 - 通信 |
| **Web 界面实时显示** | 访问 Web 管理界面，查看 AGV 位置、任务进度、系统状态 | 页面刷新率 ≥ 1Hz，数据与仿真场景一致 | 2.4 - Web管理界面 |
| **通信故障检测** | 手动停止 AGV 的心跳发送（模拟通信中断） | 调度中心在 500ms 内检测到心跳缺失，触发急停 | 2.4 - 通信故障检测 |
| **【补充】Web 界面权限控制** | 使用不同角色（管理员、操作员）登录 Web 界面 | 管理员可修改配置，操作员仅可查看；未授权访问返回 401 | 3.3 - API 认证授权 |

### 2.5 安全与异常处理模块

| 验收项 | 验收方法 | 预期结果 | 参考需求 |
|--------|----------|----------|----------|
| **急停触发（障碍物）** | 在 AGV 前方 0.08m 处放置障碍物 | AGV 立即急停，记录日志 | 2.5 - 急停触发条件 |
| **急停触发（边界）** | 手动控制 AGV 移出工作区域多边形边界 | AGV 停止，发送停止指令，记录日志 | 2.5 - 边界保护 |
| **速度限制** | 在配置文件中设置线速度上限 1.0 m/s，角速度 0.5 rad/s | AGV 实际速度不超过设定值（误差 ±5%） | 2.5 - 速度限制 |
| **降级运行** | 停止核心算法模块（如路由算法），提交任务 | 系统自动切换至简单轮询调度，发出告警日志 | 2.5 - 降级运行 |
| **【补充】急停触发（通信超时）** | 模拟通信中断 > 500ms（可配置值设 600ms） | AGV 触发急停，恢复后自动恢复 | 2.5 - 急停触发条件 |
| **【补充】急停触发（传感器失效）** | 模拟激光雷达数据异常（如全零值或恒定值） | 系统检测到传感器失效，触发急停并记录日志 | 2.5 - 软件/硬件致命错误 |
| **【补充】急停恢复机制** | 触发急停后，手动清除障碍物（或恢复通信），通过 Web 界面点击“恢复” | AGV 从急停状态恢复，重新规划路径继续执行任务 | 2.5 - 隐含恢复 |

### 2.6 扩展接口模块

| 验收项 | 验收方法 | 预期结果 | 参考需求 |
|--------|----------|----------|----------|
| **AGV 数量扩展** | 修改配置文件，将 AGV 数量从 3 增加至 5，重启系统 | 系统成功接入 5 台 AGV，并正常分配任务 | 2.6 - AGV扩展 |
| **RESTful API 集成** | 通过第三方系统调用 POST /api/tasks 提交任务，GET /api/tasks/{id} 查询状态 | 返回正确 JSON 响应，任务可被调度执行 | 2.6 - 第三方集成 |
| **【补充】API 认证授权** | 调用 RESTful API 时未提供 JWT Token | 返回 401 Unauthorized，不执行操作 | 3.3 - API 认证授权 |
| **【补充】API 角色权限** | 使用操作员 Token 调用删除任务接口 | 返回 403 Forbidden，不允许操作 | 3.3 - 角色权限 |

---

## 3. 集成验收

### 3.1 任务分配与路径规划集成

| 验收项 | 验收方法 | 预期结果 |
|--------|----------|----------|
| **任务分配后自动规划路径** | 提交 10 个任务，分配后立即检查路径规划 | 每个任务被分配后，系统在 1 秒内开始路径规划，且规划路径与任务起点/终点匹配 |
| **负载均衡与路径优化** | 提交 3 个任务给同一台 AGV，观察路径规划 | 任务按顺序执行，路径规划考虑当前 AGV 位置，避免重复来回 |

### 3.2 任务依赖与执行序列集成

| 验收项 | 验收方法 | 预期结果 |
|--------|----------|----------|
| **依赖任务执行顺序** | 提交任务 A（无依赖）、B（依赖 A）、C（依赖 B） | A 首先执行，完成后 B 立即执行，B 完成后 C 执行，无重叠 |
| **依赖打断后的恢复** | 在任务 A 执行中模拟 AGV 急停，恢复后 | 任务 A 状态标记为“失败”，重新分配后继续执行，后续依赖任务等待 |

### 3.3 交通管理与路径规划集成

| 验收项 | 验收方法 | 预期结果 |
|--------|----------|----------|
| **路口协商与路径重规划** | 两台 AGV 在路口相遇，协商优先级后 | 低优先级 AGV 在等待区重新规划局部路径（如绕行），避免长时间等待 |
| **死锁解除与任务重分配** | 构造死锁后，系统自动解除 | 死锁解除后，AGV 继续执行原任务或新任务，任务完成率 ≥ 99% |

### 3.4 通信故障与安全机制集成

| 验收项 | 验收方法 | 预期结果 |
|--------|----------|----------|
| **通信故障后急停与恢复** | 模拟通信中断 500ms，触发急停；恢复通信后 | AGV 自动从急停状态恢复，重新规划路径，继续执行任务 |
| **边界保护与通信协同** | AGV 移出边界，边界保护触发急停，同时通信状态正常 | 日志记录“边界越界”，急停后系统等待人工干预（可通过 Web 界面恢复） |

### 【补充】3.5 配置管理与安全集成

| 验收项 | 验收方法 | 预期结果 |
|--------|----------|----------|
| **配置修改后无需重启** | 修改速度限制、刹车距离等配置项，不重启系统 | 系统立即生效，AGV 按新参数运行 | 3.4 - 配置管理 |
| **API 认证与任务提交集成** | 使用有效 JWT Token 提交任务，同时使用无效 Token 提交 | 有效 Token 成功，无效 Token 返回 401，系统日志记录告警 | 3.3 - API 认证 |

---

## 4. 系统级验收

### 4.1 功能验收场景

| 编号 | 场景描述 | 验收方法 | 预期结果 | 参考需求 |
|------|----------|----------|----------|----------|
| SC-01 | 3 台 AGV 同时执行 10 个任务（无依赖） | 运行系统 30 分钟，统计任务完成情况 | 任务完成率 ≥ 99%，无碰撞、无死锁 | 5.1 - 场景一 |
| SC-02 | 动态扩展至 5 台 AGV，并并发提交 20 个任务 | 修改配置文件为 5 台 AGV，重启后提交任务 | 系统成功接入 5 台 AGV，调度逻辑正常运行，无异常退出 | 5.1 - 场景二 |
| SC-03 | 模拟通信中断（>500ms），所有 AGV 急停 | 逐个断开 AGV 节点的心跳，等待 600ms | 所有未通信 AGV 触发急停，恢复通信后自动恢复 | 5.1 - 场景三 |
| SC-04 | 模拟障碍物距离 < 0.1m，AGV 急停 | 在 AGV 前方 0.08m 处放置障碍物 | AGV 急停，移除障碍物后自动重新规划路径 | 5.1 - 场景四 |
| SC-05 | 任务依赖图存在循环依赖 | 提交包含循环依赖的任务列表 | 系统拒绝提交，返回错误提示并记录日志 | 5.1 - 场景五 |
| **【补充】SC-06** | 降级运行场景：核心算法模块不可用 | 停止路由算法进程，提交 10 个任务 | 系统自动切换至轮询调度，发出告警，任务仍可完成（但性能可能下降） | 2.5 - 降级运行 |
| **【补充】SC-07** | 长时间运行 72 小时，压力测试 | 持续运行 72 小时，每 1 小时检查内存、CPU、任务完成率 | 无崩溃，内存增长 ≤ 5%，任务完成率 ≥ 99.5% | 3.2 - 连续运行 |
| **【补充】SC-08** | 急停后恢复并继续执行依赖任务 | 任务 A 执行中触发急停，恢复后检查后续任务 B（依赖 A） | 任务 A 标记为失败，重新分配后完成，任务 B 自动启动 | 2.5 - 急停恢复 |

### 4.2 性能验收

| 编号 | 指标 | 测试方法 | 预期结果 | 参考需求 |
|------|------|----------|----------|----------|
| PERF-01 | 调度响应时间 | 使用 3 台 AGV，一次性提交 10 个任务，记录从提交到分配完成的时间 | 平均值 ≤ 1.0 秒，最大值 ≤ 1.2 秒 | 3.1 - 调度响应时间 |
| PERF-02 | 全局路径规划时间 | 随机生成 50 组起点-终点对，调用路径规划接口 | 平均时间 ≤ 0.5 秒，所有计算 ≤ 0.6 秒 | 3.1 - 路径规划时间 |
| PERF-03 | 局部重规划时间 | 在 AGV 运行路径上放置动态障碍物，触发重规划 50 次 | 平均时间 ≤ 0.2 秒，所有计算 ≤ 0.25 秒 | 3.1 - 局部重规划 |
| PERF-04 | 系统吞吐量 | 同时提交 50 个待分配任务，持续监控 | 任务调度延迟 ≤ 2 秒，无任务丢失 | 3.1 - 系统吞吐量 |
| PERF-05 | 5 台 AGV 并发 | 5 台 AGV 同时运行，提交 20 个任务 | 调度响应时间 ≤ 2.0 秒 | 3.1 - 并发支持 |
| PERF-06 | 连续运行 72 小时 | 系统运行 72 小时，每 1 小时检查一次内存和 CPU | 无崩溃、无内存泄漏（内存增长 ≤ 5%），日志无异常错误 | 3.2 - 连续运行 |
| **【补充】PERF-07** | 平均无故障时间（MTBF） | 连续运行 1000 小时（可加速模拟），记录故障次数 | 故障次数 ≤ 1 次，MTBF ≥ 1000 小时 | 3.2 - MTBF |
| **【补充】PERF-08** | 故障恢复时间 | 手动触发系统崩溃（如 kill 主进程），记录从重启到任务状态恢复完成的时间 | 恢复时间 ≤ 30 秒，任务状态不丢失 | 3.2 - 故障恢复时间 |
| **【补充】PERF-09** | 任务状态查询响应时间 | 在 50 个任务并发场景下，频繁查询任务状态 API | 平均响应时间 ≤ 200ms，最大 ≤ 500ms | 2.1 - 任务状态跟踪 |

### 4.3 可靠性验收

| 编号 | 测试项 | 测试方法 | 预期结果 | 参考需求 |
|------|--------|----------|----------|----------|
| REL-01 | 数据持久化（SQLite） | 提交 50 个任务，记录任务 ID；重启系统后查询 | 任务记录、状态、日志完整保留，无丢失 | 3.2 - 数据持久化 |
| REL-02 | 自动备份策略 | 运行系统 24 小时，检查备份目录 | 每小时增量备份文件存在，每日全量备份文件存在，可恢复 | 3.2 - 自动备份 |
| REL-03 | 崩溃恢复后任务状态一致性 | 模拟系统崩溃，重启后检查所有任务状态 | 所有任务状态与崩溃前一致（待分配/执行中/完成/失败），无歧义 | 3.2 - 故障恢复 |
| REL-04 | 长时间运行后日志无异常 | 72 小时连续运行后，检查日志文件 | 无 `ERROR` 级别异常，无 `FATAL` 崩溃；日志文件大小不超过 1GB | 3.2 - 连续运行 |
| **【补充】REL-05** | 传感器失效降级 | 模拟激光雷达失效，系统自动切换至超声波或纯里程计模式 | 仍可运行，但速度限制降低至 0.5m/s，发出告警 | 2.5 - 降级运行 |

### 4.4 安全验收

| 编号 | 测试项 | 测试方法 | 预期结果 | 参考需求 |
|------|--------|----------|----------|----------|
| SEC-01 | API 认证（JWT） | 调用所有 RESTful API 时，不提供 Token、提供过期 Token、提供伪造 Token | 返回 401 Unauthorized，操作被拒绝 | 3.3 - API 认证授权 |
| SEC-02 | 角色权限分离 | 使用操作员 Token 调用管理员接口（如修改配置） | 返回 403 Forbidden | 3.3 - 角色权限 |
| SEC-03 | 输入校验与防注入 | 提交包含 SQL 注入、XSS 脚本的任务数据 | 系统拒绝，返回错误信息，不执行，日志记录 | 3.3 - 输入校验 |
| SEC-04 | 工具依赖安全检查 | 启动系统前，移除 `cppcheck`、`clang-tidy`、`pylint`、`roslint` 等工具 | 系统启动成功，输出告警日志“工具缺失，跳过对应校验”，不阻塞主流程 | 3.3 - 工具依赖安全 |
| SEC-05 | 通信加密（可选） | 启用 ROS 加密传输，抓包验证 | 通信内容被加密，无法明文读取 | 3.3 - 通信加密 |
| **【补充】SEC-06** | 边界保护绕过测试 | 尝试通过 API 设置 AGV 位置为边界外（如坐标超出多边形） | 系统拒绝，返回错误，不执行 | 2.5 - 边界保护 |

### 4.5 可维护性验收

| 编号 | 测试项 | 测试方法 | 预期结果 | 参考需求 |
|------|--------|----------|----------|----------|
| MAINT-01 | 日志记录完整性 | 执行典型操作（任务分配、路径规划、异常触发），检查日志 | 所有关键操作均记录，包含时间戳、级别、模块、内容 | 3.4 - 日志记录 |
| MAINT-02 | 日志级别可配置 | 修改配置文件中的日志级别为 `WARNING`，重启系统 | 不再输出 `INFO` 级别日志，仅输出 `WARNING` 及以上 | 3.4 - 日志记录 |
| MAINT-03 | 配置动态调整（速度限制） | 不重启系统，修改配置文件中线速度上限为 0.8m/s | AGV 实际速度立即降为 0.8m/s（误差 ±5%） | 3.4 - 配置管理 |
| MAINT-04 | 配置动态调整（刹车距离） | 不重启系统，修改防碰撞急停距离为 0.15m | 当 AGV 距离障碍物 0.15m 时触发急停 | 3.4 - 配置管理 |
| MAINT-05 | 代码模块化检查 | 检查代码目录结构 | 符合表现层、调度层、算法层、通信层、数据层分层，各模块职责清晰 | 3.4 - 代码结构 |
| **【补充】MAINT-06** | 配置错误容错 | 修改配置文件为非法值（如负速度），保存后系统自动重新加载 | 系统拒绝非法值，使用默认值，并记录告警 | 3.4 - 配置管理 |

### 4.6 可用性验收

| 编号 | 测试项 | 测试方法 | 预期结果 | 参考需求 |
|------|--------|----------|----------|----------|
| AVAIL-01 | 环境依赖自动检测 | 在缺少 ROS Noetic 或 Gazebo Ignition 的环境中启动系统 | 输出明确错误提示“缺少 ROS Noetic，请安装后重试” | 3.5 - 环境依赖检测 |
| AVAIL-02 | Docker 镜像构建与运行 | 执行 `docker build` 构建镜像，并运行容器 | 镜像构建成功，容器内系统正常启动，仿真环境可运行 | 3.5 - Docker 镜像 |
| AVAIL-03 | 启动后自动恢复任务 | 系统重启后，检查是否有未完成的任务 | 从持久化存储中恢复任务状态，继续分配或执行 | 3.2 - 故障恢复 |
| **【补充】AVAIL-04** | 仿真器版本兼容性 | 在 Gazebo Ignition Garden 环境下运行 | 无版本兼容性警告，传感器数据正常 | 4.2 - 仿真平台 |
| **【补充】AVAIL-05** | 第三方依赖缺失告警 | 移除 `scipy` 或 `networkx`，启动系统 | 系统输出明确错误提示“缺少依赖库：scipy”，不启动主流程 | 3.5 - 环境依赖检测 |

---

## 5. 测试环境与工具要求

**环境描述**：所有测试均在 Gazebo Ignition（Garden）仿真环境中运行，使用 Ubuntu 20.04 LTS + ROS Noetic，开发主机 CPU 8核，RAM 16GB。

**测试数据**：
- 标准任务列表：包含 50 个任务的 JSON 文件，涵盖不同优先级、时间约束、依赖关系。
- 循环依赖测试数据：A→B, B→C, C→A。
- 地图：静态地图（含障碍物、交叉路口），工作区域多边形边界已知。

**测试工具**：
- 性能测试：使用 `time` 命令或 `python timeit` 记录耗时。
- 压力测试：使用 `jmeter` 或自定义脚本提交并发任务。
- 日志分析：`grep`, `awk` 统计日志级别。
- 网络抓包：`tcpdump` 或 `wireshark` 验证通信加密。
- 代码质量工具：`cppcheck`, `clang-tidy`, `pylint`, `roslint`（用于安全验收 SEC-04）。

**【补充】测试工具完整性**：验收前需确认上述工具已安装或可模拟缺失情况。

---

**说明**：以上补全内容已完整覆盖需求文档 V2.0 中的所有功能点与非功能需求，并确保每个验收项具有明确的验收方法、量化预期结果及可追溯的参考需求。所有补充项均以**【补充】** 标注，便于识别。