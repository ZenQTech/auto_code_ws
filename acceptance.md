# 补全后的验收标准 — AGV 模拟与可视化系统 V2.0

## 1. 概述

本文档定义了 AGV 模拟与可视化系统 V2.0 的验收标准，涵盖模块级、集成级、系统级及安全级四个维度。所有验收项基于需求文档 V2.0 及批判反馈中提出的关键修复要求（全局急停机制、外部输入白名单校验、降级方案等）制定，确保系统功能的完整性、健壮性与安全性。

**【补充】测试环境要求**：  
- 操作系统：Ubuntu 20.04 LTS (64位)  
- ROS版本：Noetic (官方源安装)  
- Python版本：3.8+  
- Gazebo版本：9.0+  
- 可视化库：可采用 PyQt5 QCanvas 或 HTML5 Canvas，需支持实时刷新  
- 硬件：CPU i5及以上（推荐i7），内存≥8GB，集成或独立显卡  
- 外部依赖：需提供 mock-data-generator 或其降级方案  

---

## 2. 模块级验收

### 2.1 Mock 数据生成与注入模块

| 编号 | 验收项 | 测试条件 | 通过标准 |
|------|--------|----------|----------|
| M-01 | 数据生成频率与格式 | 运行 Mock 数据生成器，输出预定义路径、障碍物位置、速度指令、状态变更事件 | 数据输出频率 ≥10Hz；每条数据包含完整字段（路径点列表至少含坐标x,y,theta；障碍物位置含坐标x,y,半径；速度值m/s；事件类型如 charging/waiting/fault）；字段类型符合 JSON 规范 |
| M-02 | 实时注入与回放模式 | 分别启动实时注入模式和回放模式（手动控制速度 0.5x、1x、2x） | 实时注入模式下界面更新频率 ≥10Hz；回放模式下速度可精确调节，数据时序保持不变 |
| M-03 | 白名单输入校验 | 输入包含特殊字符的配置文件（如 `; rm -rf /`、`../etc/passwd`、`<script>`） | 系统拒绝加载，返回错误提示“非法字符”，不执行任何命令或系统调用；校验规则仅允许字母、数字、`_`、`-`、`.` |
| M-04 | 数据中断降级 | 强制停止 Mock 数据流，持续 5 秒 | 系统自动切换至本地预存路径数据，界面显示黄色降级标识；恢复数据流后自动切回主源，无数据跳跃 |
| **【补充】M-25** | Mock 数据参数校验（UI输入） | 通过界面输入框或命令行参数注入非法字符（如 `"`、`'`、`<`、`>`） | 系统拒绝接受，提示“参数包含非法字符”，不执行任何解析 |
| **【补充】M-26** | 降级模式下数据连续性 | 在降级模式运行 30 秒后恢复主数据源 | 降级期间 AGV 持续运行，无停顿；切换回主源后位置平滑过渡，误差≤±10cm |

### 2.2 路径规划模块（三级执行策略路由）

| 编号 | 验收项 | 测试条件 | 通过标准 |
|------|--------|----------|----------|
| M-05 | 全局拓扑路径规划（Dijkstra） | 在 100m×100m 拓扑地图中设置起点和终点，包含 5 个节点 | 算法返回最短路径，路径节点序列正确，计算耗时 ≤100ms |
| M-06 | 局部栅格路径平滑（A* + 样条插值） | 在栅格地图中给定全局路径，添加随机静态障碍物 | 生成平滑路径，相邻点间距离 ≤0.5m，无碰撞风险（障碍物距离 >0.2m） |
| M-07 | 动态避障修正（DWA） | 模拟动态障碍物以 0.3m/s 横向移动，AGV 沿原路径行驶 | AGV 在距离障碍物 1m 内开始减速（≤0.5m/s）并重新规划局部路径，避免碰撞 |
| M-08 | 规划成功率与耗时 | 连续执行 100 次随机起点终点导航任务 | 成功率 ≥95%（95 次成功），单次规划耗时 ≤200ms |
| **【补充】M-27** | 拓扑+栅格混合地图路径规划 | 地图包含拓扑节点（5个）和栅格障碍物，起点和终点分别位于拓扑和栅格区域 | 算法正确切换两级规划，最终路径无碰撞，规划耗时 ≤200ms |

### 2.3 避障与碰撞处理模块

| 编号 | 验收项 | 测试条件 | 通过标准 |
|------|--------|----------|----------|
| M-09 | 静态障碍物避让 | 在路径中放置 3 个静态障碍物（直径 0.5m） | AGV 自动减速至 ≤0.5m/s，绕行后恢复原速，无碰撞 |
| M-10 | 动态障碍物冲突消解（最少负载调度） | 两辆 AGV 相向而行，路径交叉 | 系统通过最少负载调度算法分配任务或调整优先级，AGV 无碰撞，且负载较低的 AGV 优先通过 |
| M-11 | 路径不可达告警 | 在 3 秒内无法找到可行路径（如死胡同） | 系统触发系统级告警，暂停当前任务，界面显示红色警告，日志记录“路径规划失败” |
| **【补充】M-28** | 最少负载调度算法负载计算验证 | 设置两辆 AGV，负载分别为 3 和 5（任务数），路径交叉 | 负载为 3 的 AGV 优先通过，日志记录调度决策 |

### 2.4 实时可视化模块

| 编号 | 验收项 | 测试条件 | 通过标准 |
|------|--------|----------|----------|
| M-12 | 基础绘制与刷新率 | 运行系统，观察 Canvas 帧率 | 刷新率 ≥10Hz，AGV 位置更新平滑，无闪烁 |
| M-13 | 位置显示精度 | 比较 Mock 数据中 AGV 坐标与 Canvas 绘制坐标 | 误差 ≤±10cm（模拟坐标系下） |
| M-14 | 状态颜色编码 | 手动设置 AGV 状态为正常、减速、故障/停止 | 颜色正确：绿=正常，黄=减速，红=故障/停止 |
| M-15 | 悬停详细信息 | 鼠标悬停于 AGV 图标上 | 弹出气泡显示 ID、速度、电量、当前任务名称 |
| M-16 | 路径与轨迹绘制 | 观察 AGV 移动后的历史轨迹 | 轨迹为连续线段，长度 ≥10 个点，颜色随 AGV 状态变化 |
| **【补充】M-29** | AGV 方向标识 | 观察 AGV 模型（三角形或圆形） | 模型带有方向箭头或三角形指向，与运动方向一致 |

### 2.5 状态管理与异常处理模块

| 编号 | 验收项 | 测试条件 | 通过标准 |
|------|--------|----------|----------|
| M-17 | 状态机转换 | 模拟状态跳转：空闲 → 导航 → 避障 → 暂停 → 故障 → 恢复 | 状态转换正确，界面状态指示灯同步更新，日志记录每一步 |
| M-18 | 数据流中断告警 | 中断 Mock 数据流 3 秒、5 秒、10 秒 | 3 秒无反应，5 秒触发降级，10 秒触发急停；日志记录中断时间及处理动作 |
| M-19 | 异常日志记录 | 制造规划失败、碰撞风险、数据异常事件 | 日志文件包含时间戳、事件类型（如 PLANNING_FAILURE）、摘要信息，不包含原始数据 |
| **【补充】M-30** | 状态机完整转换路径 | 测试所有可能的状态转换：空闲→导航、导航→避障、避障→导航、导航→暂停、暂停→导航、导航→故障、故障→恢复、空闲→故障（直接异常） | 所有转换均正确执行，界面和日志同步 |
| **【补充】M-31** | 界面异常提示 | 触发规划失败或数据异常事件 | 界面左上角或状态栏显示红色/黄色提示信息，持续 3 秒以上 |

### 2.6 安全机制模块

| 编号 | 验收项 | 测试条件 | 通过标准 |
|------|--------|----------|----------|
| M-20 | 全局急停快捷键 | 按 Ctrl+Space | 所有 AGV 立即停止运动，导航线程锁定，界面显示“急停已触发” |
| M-21 | 全局急停按钮 | 点击界面急停按钮 | 效果同上，按钮状态变为“已急停” |
| M-22 | 急停恢复机制 | 急停后点击“恢复”按钮 | AGV 回滚至上一个安全位置（缓存路径点，距离障碍物 ≥0.5m），然后继续导航 |
| M-23 | 速度限制 | 设置 AGV 速度指令为 1.0m/s | 系统强制限制为 0.5m/s，界面显示实际速度 |
| M-24 | 边界限制 | AGV 接近边界 100m×100m 时 | 当 AGV 距边界 ≤0.5m 时自动减速；超出边界立即触发急停，回退至最近安全点（边界内 0.2m） |
| **【补充】M-32** | 自动急停 – 连续碰撞检测失败 | 模拟连续 3 次碰撞检测失败（如障碍物无法避开） | 系统自动触发急停，界面显示“自动急停：连续碰撞失败”，日志记录 |
| **【补充】M-33** | 自动急停 – 路径规划超时 | 模拟路径规划耗时超过 5 秒（如地图无解） | 系统自动触发急停，界面显示“自动急停：规划超时”，日志记录 |
| **【补充】M-34** | 自动急停 – 数据流异常中断 | 中断 Mock 数据流超过 10 秒 | 系统自动触发急停（与 M-18 联动），界面显示“自动急停：数据中断”，日志记录 |
| **【补充】M-35** | 日志内容安全 | 检查日志文件是否包含原始 Mock 数据（如完整路径点坐标、障碍物坐标） | 日志仅包含时间戳、事件类型、摘要（如“路径规划失败，耗时 5.2s”），不包含原始数据字段 |

---

## 3. 集成验收

### 3.1 数据流集成

| 编号 | 验收项 | 测试条件 | 通过标准 |
|------|--------|----------|----------|
| I-01 | Mock 数据 → 路径规划 | 持续注入 Mock 路径点，观察路径规划模块是否实时响应 | 路径规划模块以 ≥10Hz 频率接收数据，规划结果在 200ms 内返回 |
| I-02 | 路径规划 → 避障 | 规划路径中出现障碍物，避障模块触发 | 避障模块正确接收路径规划结果，并输出修正后的局部路径 |
| I-03 | 避障 → 可视化 | 避障过程中 AGV 位置变化 | Canvas 实时更新 AGV 位置，刷新率 ≥10Hz，无可见延迟 |
| I-04 | 状态管理 → 可视化 | 状态机转换时，可视化颜色同步更新 | 颜色变化与状态变化时间差 ≤100ms |
| I-05 | 异常处理 → 日志 | 触发异常事件（如数据中断） | 日志文件在 1 秒内记录事件，文件写入无阻塞 |

### 3.2 安全机制集成

| 编号 | 验收项 | 测试条件 | 通过标准 |
|------|--------|----------|----------|
| I-06 | 急停 → 导航线程锁定 | 按急停后，观察导航线程是否停止 | 导航线程立即停止，路径规划不再输出新路径，AGV 速度归零 |
| I-07 | 恢复 → 安全位置回滚 | 急停后重新规划，检查回滚位置 | 回滚至急停前最后一个安全位置（距离障碍物 ≥0.5m），然后继续导航 |
| I-08 | 边界限制 → 急停联动 | AGV 超出边界 | 急停自动触发，日志记录“边界超限”，回退至边界内 |

### 3.3 降级模式集成

| 编号 | 验收项 | 测试条件 | 通过标准 |
|------|--------|----------|----------|
| I-09 | 主数据源中断 → 降级切换 | 停止 Mock 数据生成器 5 秒 | 系统自动切换到本地预存路径，AGV 继续运动，界面显示黄色降级标识 |
| I-10 | 降级恢复 → 主数据源 | 重新启动 Mock 数据生成器 | 系统在 2 秒内切换回主数据源，位置平滑过渡，无跳跃 |
| **【补充】I-11** | 降级模式与急停联动 | 在降级模式运行中，触发边界超限或规划超时 | 自动急停仍生效，降级模式下急停逻辑与正常模式一致 |

---

## 4. 系统级验收

### 4.1 端到端任务测试

| 编号 | 验收项 | 测试条件 | 通过标准 |
|------|--------|----------|----------|
| S-01 | 连续导航任务（10 次） | 在 Gazebo 中加载 100m×100m 地图，设置 10 个随机目标点，使用 Mock 数据驱动 AGV 依次完成 10 次导航 | 无碰撞，路径规划成功率 ≥95%（9 次成功），总耗时 ≤60 分钟 |
| S-02 | 避障与急停综合测试 | 路径中放置 3 个静态障碍物和 1 个动态障碍物，触发急停（Ctrl+Space） | 障碍物前自动减速并重新规划，急停后 AGV 立即停止，恢复后继续任务，无碰撞 |
| S-03 | 异常处理与降级 | 手动中断 Mock 数据流 5 秒，再恢复 | 系统自动切换降级模式，界面显示黄色告警，恢复后自动切回，无数据丢失或异常跳变 |
| S-04 | 24 小时稳定性测试 | 运行系统 24 小时，每 1 小时记录一次内存和 CPU 使用率，同时执行连续导航任务 | 无崩溃，无内存泄漏（内存增长 <5%），CPU 平均负载 ≤50%，始终满足刷新率 ≥10Hz |
| S-05 | 多 AGV 协同（可选） | 部署 2 辆 AGV，分别执行不同任务，路径交叉 | 系统通过最少负载调度算法分配任务，无碰撞，任务完成时间在合理范围内（< 2 倍单机时间） |

### 4.2 性能指标验证

| 编号 | 验收项 | 测试条件 | 通过标准 |
|------|--------|----------|----------|
| S-06 | 定位精度 | 在多个位置记录 Mock 数据坐标与 Canvas 显示坐标 | 误差 ≤±10cm（模拟坐标系下） |
| S-07 | 响应时间 | 从收到 Mock 数据帧到界面更新完成，使用高精度计时 | ≤500ms（95% 分位） |
| S-08 | 数据丢失率 | 连续运行 1 小时，统计 Mock 数据帧数与界面更新帧数 | 丢失率 ≤0.1%（即 1000 帧中最多丢失 1 帧） |
| S-09 | 连续运行时间 | 压力测试: 重复执行 10 次导航任务，模拟异常场景 | 系统 24 小时无崩溃，满足性能指标 |

### 4.3 用户交互功能

| 编号 | 验收项 | 测试条件 | 通过标准 |
|------|--------|----------|----------|
| S-10 | 控制按钮功能 | 点击开始/暂停/停止/急停按钮 | 功能正确，状态切换正常，日志记录操作 |
| S-11 | 速度调节滑块 | 滑动滑块改变模拟速度（0.1~0.5m/s） | AGV 速度实时变化，精度 ±0.01m/s |
| S-12 | 自定义 Mock 文件加载 | 提供正确格式的 JSON 文件 | 系统加载成功，自动校验格式，开始模拟 |
| **【补全】S-13** | 非法文件格式处理 | 提供非法 JSON 文件（如语法错误、缺少必填字段、字段类型错误） | 系统拒绝加载，弹出错误提示并说明原因（如“JSON 解析错误：缺少字段‘path_points’”），不执行任何模拟 |
| **【补充】S-14** | 键盘快捷键测试 | 依次按下 P、R、E 键 | P 键暂停模拟，R 键恢复，E 键急停，功能与对应按钮一致，日志记录操作 |
| **【补充】S-15** | 日志显示区域实时更新 | 触发异常事件（如规划失败） | 日志显示区域在 1 秒内出现新条目，滚动条自动滚动至最新 |
| **【补充】S-16** | 速度滑块边界值测试 | 滑块调至最小（0.1m/s）和最大（0.5m/s） | AGV 速度分别稳定在 0.1m/s 和 0.5m/s，误差 ±0.01m/s |

---

## 5. 安全级验收（补充）

| 编号 | 验收项 | 测试条件 | 通过标准 |
|------|--------|----------|----------|
| **【补充】SA-01** | 自动急停优先级 | 同时触发多个自动急停条件（如数据中断+边界超限） | 系统立即执行急停，仅记录一次急停事件，无冲突 |
| **【补充】SA-02** | 白名单校验全面性 | 使用包含所有允许字符（字母、数字、`_`、`-`、`.`）的合法输入，以及包含禁止字符的非法输入 | 合法输入正常加载，非法输入被拒绝，无漏报 |
| **【补充】SA-03** | 日志安全合规 | 查看日志文件内容，确认无原始 Mock 数据、无敏感信息（如路径点坐标、障碍物完整坐标） | 日志仅包含时间戳、事件类型、摘要，无原始数据字段 |
| **【补充】SA-04** | 单元测试覆盖率报告 | 获取项目单元测试覆盖率报告 | 核心算法模块（三级执行策略路由、最少负载调度）覆盖率 ≥80%，总体覆盖率 ≥70% |

---

**说明**：  
- 所有补充项以【补充】或【补全】标记，原有内容不变。  
- 测试环境要求已在概述中补充，确保验收可重复。  
- 安全级验收独立列出，以强化对安全红线的验证。  
- 所有验收项均具备可执行性（明确测试条件与通过标准）。