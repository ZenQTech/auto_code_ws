# 验收标准.md（补全后，标注新增内容）

## 1. 概述

本文档基于需求文档 V2.0 定义的系统架构、功能、非功能及安全要求，制定系统验收标准。验收分为四个层次：**模块级验收**、**集成验收**、**系统级验收**和**安全验收**。每个层次包含具体测试场景、通过条件和测量方法。所有验收测试必须在 **Gazebo Ignition Garden** 仿真环境中执行，使用 3 台 AGV（默认）及可扩展至 5 台 AGV 的配置。

**【新增】测试环境与数据要求：**
- **地图**：至少包含 1 个十字路口、2 个直角弯道、1 个窄通道（宽度 0.5m），以及随机分布的静态障碍物（直径 0.2m 圆柱体 5 个）。
- **任务数据**：提供 JSON 和 CSV 格式的测试任务列表，含 10 个基础任务和 20 个扩展任务，包含起点、终点、优先级（1-5）、执行时间约束（可选绝对时间），以及依赖关系 DAG（5 个任务有依赖）。
- **AGV 模型**：使用与需求一致的 MiR 或自研模型，运动学模型为差速驱动，传感器配置：2D 激光雷达（180° 扫描，0.5° 分辨率）、超声波（4 个）、IMU。
- **测试工具**：必须安装 `cppcheck`、`clang-tidy`、`pylint`、`roslint`（工具链安全测试时验证）。

---

## 2. 模块级验收

### 2.1 任务管理与调度模块

| 验收项 | 测试场景 | 通过条件 |
|--------|----------|----------|
| **任务导入** | 提供 JSON 和 CSV 格式的任务列表（含起点、终点、优先级、执行时间约束） | 系统正确解析，输出任务列表，无格式错误时的导入成功率 100% |
| **最少负载调度算法** | 3 台 AGV 同时接收 10 个任务，负载评估函数使用可配置权重（任务队列长度、剩余电量、距离目标点距离） | 分配后各 AGV 负载差异 ≤ 20%（以任务队列长度计），且算法执行时间 ≤ 0.5 秒 |
| **【新增】负载权重可配置** | 修改配置文件中的权重（例如将“距离”权重设为 0，其他权重为 1），重新分配相同任务 | 分配结果与默认权重下的分配结果有明显差异（如更偏好低电量 AGV），且系统无需重启即可生效 |
| **任务依赖检测** | 输入包含合法 DAG 依赖图（10 个任务）和含循环依赖的图 | 合法 DAG 生成唯一拓扑排序序列；循环依赖图拒绝提交并返回错误提示（错误码 + 循环路径） |
| **任务状态跟踪** | 执行任务过程中查询状态接口 | 状态实时更新，查询响应时间 ≤ 0.2 秒，状态机正确（待分配→执行中→完成/失败，不允许跳过状态或逆向跳转） |

### 2.2 路径规划模块

| 验收项 | 测试场景 | 通过条件 |
|--------|----------|----------|
| **全局路径规划（A*）** | 静态地图上，任意两点间生成路径 | 路径长度 ≤ 理论最优的 1.1 倍，计算时间 ≤ 0.5 秒 |
| **局部重规划（Dijkstra）** | 在路径中动态插入静态障碍物（模拟拥堵） | 在 0.2 秒内生成绕行路径，且路径平滑（相邻路径点夹角变化 ≤ 30°，无突跳）【新增】量化平滑标准 |
| **避障与微调（传感器数据）** | 激光雷达模拟前方 0.3m 处障碍物 | AGV 减速至 0.2 m/s 以下，障碍物移除后 0.5 秒内恢复原路径 |
| **【新增】三级策略切换** | 在全局路径上设置密集障碍物，导致局部重规划失败（无可行路径） | 系统自动回退至全局路径规划（重新调用 A*），并记录告警，AGV 不停止超过 2 秒 |
| **多 AGV 协同路径** | 2 台 AGV 相向而行，路径交叉 | 无死锁，路径规划避免冲突（通过路口协商） |

### 2.3 交通管理模块

| 验收项 | 测试场景 | 通过条件 |
|--------|----------|----------|
| **路口优先级协商** | 3 台 AGV 同时到达十字路口 | 根据任务紧急程度和距离决定通行顺序，无碰撞且平均等待时间 ≤ 5 秒 |
| **防碰撞机制** | AGV 间距 ≤ 0.3m 时触发减速，≤ 0.1m 时急停 | 减速触发正确，急停时 AGV 速度降为 0，且记录日志（包含时间戳、AGV ID、触发原因） |
| **死锁检测与解除** | 构造循环等待场景（4 台 AGV 互相阻塞） | 系统在 2 秒内检测到死锁，输出死锁警告日志（包含循环路径图），自动重分配任务或重规划路径，解除后继续执行 |

### 2.4 通信与状态监控模块

| 验收项 | 测试场景 | 通过条件 |
|--------|----------|----------|
| **ROS 通信频率** | AGV 与调度中心通过 Topic 发布状态 | 状态更新频率 ≥ 10 Hz，连续 10 秒内丢包率 ≤ 1%【新增】量化丢包率 |
| **【新增】ROS Service 通信** | 调度中心通过 Service 查询 AGV 详细状态（如电池电压、传感器状态） | 请求响应时间 ≤ 100ms，返回数据正确 |
| **Web 管理界面** | 实时显示 3 台 AGV 位置、任务进度、系统状态 | 页面刷新延迟 ≤ 1 秒，数据与仿真一致 |
| **通信故障检测** | 模拟 AGV 心跳中断 500ms | 调度中心在 500ms 内判定通信中断，并发送急停指令 |

### 2.5 安全与异常处理模块

| 验收项 | 测试场景 | 通过条件 |
|--------|----------|----------|
| **急停触发条件** | 分别测试障碍物 <0.1m、通信超时 >500ms、超出边界、传感器失效（模拟激光雷达数据全为 0） | 每种条件触发时 AGV 在 100ms 内停止，日志记录精确（触发源、时间、AGV ID） |
| **【新增】软件致命错误** | 模拟调度核心进程崩溃（如路径规划模块 segfault） | 系统检测到进程异常退出，触发该 AGV 急停，并记录致命错误日志，其他 AGV 不受影响 |
| **边界保护** | AGV 位置超出多边形工作区域 | 发送停止指令，AGV 停止，并记录超出边界的坐标和时间 |
| **速度限制** | 设置线速度上限 1.0 m/s，角速度 0.5 rad/s | AGV 实际速度始终 ≤ 设定值，超限时自动减速并告警 |
| **降级运行** | 强制关闭路由算法模块 | 系统自动切换至轮询调度，发出告警，任务不中断 |

### 2.6 扩展接口模块

| 验收项 | 测试场景 | 通过条件 |
|--------|----------|----------|
| **AGV 数量扩展** | 通过配置文件添加第 4、5 台 AGV，并启动仿真 | 新 AGV 成功注册，接收任务并执行，不影响原有 AGV 运行 |
| **【新增】动态增加 AGV（运行时）** | 系统运行中，修改配置文件添加第 4 台 AGV，触发重新加载（不重启系统） | 新 AGV 在 5 秒内注册成功，开始接收任务，调度逻辑自动适配，无任务中断或冲突 |
| **RESTful API 集成** | 第三方系统通过 API 提交任务、查询状态 | 接口响应时间 ≤ 0.5 秒，状态查询准确，支持批量提交（≥ 50 个任务） |

---

## 3. 集成验收

### 3.1 任务分配与路径规划协同

- **场景**：3 台 AGV，10 个任务，每个任务包含不同优先级和依赖关系。
- **通过条件**：
  - 任务分配完成后，自动触发全局路径规划，路径规划结果与任务起点/终点一致。
  - 局部重规划与避障模块在任务执行过程中无缝衔接，无路径冲突。
  - 任务依赖顺序正确（如 B 在 A 完成后开始），多 AGV 并行执行时无资源冲突。

### 3.2 多 AGV 交通管理与死锁解除

- **场景**：4 台 AGV 在密集交叉路口区域执行 20 个任务。
- **通过条件**：
  - 路口协商机制保证无碰撞，死锁检测在 1 秒内发现并解除。
  - 系统吞吐量 ≥ 50 个待分配任务时，调度延迟 ≤ 2 秒。
  - 连续运行 72 小时，无死锁累积、无内存泄漏。

### 3.3 通信故障恢复与状态持久化

- **场景**：模拟一台 AGV 通信中断 500ms，然后恢复。
- **通过条件**：
  - 中断期间该 AGV 急停，恢复后自动重新连接，任务状态从断点继续（若未超时）。
  - 系统崩溃后重启，使用 SQLite 备份恢复数据，任务状态不丢失，恢复时间 ≤ 30 秒。
- **【新增】备份恢复专项测试**：
  - 手动删除当前数据库文件，然后从最新全量备份恢复，验证恢复后任务列表、AGV 状态、日志记录与崩溃前一致。

### 3.4 安全机制集成

- **场景**：同时触发障碍物急停和通信超时，且 AGV 接近边界。
- **通过条件**：
  - 急停指令优先级最高，所有 AGV 在 100ms 内停止。
  - 日志记录完整，Web 界面显示急停原因和位置。

---

## 4. 系统级验收

### 4.1 功能验收场景

| 场景编号 | 描述 | 通过条件 |
|----------|------|----------|
| **S1** | 3 台 AGV 同时执行 10 个任务（含依赖关系），仿真环境运行 1 小时 | 任务完成率 ≥ 99%，无碰撞、无死锁、无任务丢失 |
| **S2** | 动态扩展至 5 台 AGV，并分配 20 个并发任务 | 新 AGV 正常加入，调度逻辑正常运行，任务分配无异常 |
| **S3** | 模拟单台 AGV 通信中断 >500ms | 所有 AGV 触发急停，恢复通信后自动恢复，任务状态正确 |
| **S4** | 模拟障碍物距离 <0.1m | AGV 急停，障碍物移除后 2 秒内重新规划路径并继续执行 |
| **S5** | 任务依赖图包含循环依赖 | 系统拒绝提交，返回错误提示（包含循环路径） |
| **S6** | 连续 7×24 小时运行 | 无崩溃、无内存泄漏、无死锁，满足性能指标 |
| **【新增】S7** | 同时提交 50 个待分配任务，测量调度延迟 | 所有任务在 2 秒内完成分配，调度延迟 ≤ 2 秒 |
| **【新增】S8** | 运行时修改配置文件中的速度限制（线速度 0.5 m/s 改为 0.8 m/s） | AGV 实时响应新限制，无需重启，速度超限告警阈值同步更新 |
| **【新增】S9** | 运行 1 小时后，检查日志文件 | 所有关键操作（任务分配、路径规划、异常触发）均有日志记录，日志级别可配置（如切换为 DEBUG 后输出更详细） |

### 4.2 性能验收

| 指标 | 条件 | 通过值 |
|------|------|--------|
| **调度响应时间** | 3 台 AGV，10 个任务 | ≤ 1.0 秒 |
| **全局路径规划计算时间** | 静态地图，任意两点 | ≤ 0.5 秒 |
| **局部重规划计算时间** | 动态障碍物触发 | ≤ 0.2 秒 |
| **系统吞吐量** | 待分配任务 ≥ 50 个 | 调度延迟 ≤ 2 秒 |
| **并发支持** | 5 台 AGV 同时运行 | 调度响应时间 ≤ 2.0 秒 |
| **状态更新频率** | ROS 通信 | ≥ 10 Hz |
| **Web 界面刷新延迟** | 实时数据 | ≤ 1 秒 |

### 4.3 可靠性验收

| 指标 | 通过条件 |
|------|----------|
| **连续运行时间** | 7×24 小时无故障 |
| **MTBF** | ≥ 1000 小时（基于仿真加速测试，例如以 10 倍速运行 100 小时，等效 1000 小时无故障）【新增】明确加速方法 |
| **故障恢复时间** | 系统崩溃重启 ≤ 30 秒，任务状态不丢失 |
| **数据持久化** | SQLite 自动备份：每小时增量，每日全量；备份文件可恢复，恢复后数据一致（见集成验收 3.3 补充测试） |
| **子进程清理** | 父进程退出后，所有子进程（仿真器、外部工具）在 5 秒内被终止 |

### 4.4 可用性验收

| 验收项 | 通过条件 |
|--------|----------|
| **环境依赖检查** | 启动时检测 ROS、Gazebo Ignition、Python 库等，缺失时输出明确错误提示 |
| **Docker 镜像** | 提供 Dockerfile 和启动脚本，基于 Ubuntu 20.04 + ROS Noetic + Gazebo Ignition Garden，容器内一键启动 |
| **配置管理** | 所有算法阈值（速度限制、刹车距离、超时时间、复杂度评分）可通过 YAML 配置文件调整，无需重启系统（运行时修改立即生效，已验证） |

---

## 5. 安全验收

### 5.1 API 认证与授权

| 测试项 | 测试方法 | 通过条件 |
|--------|----------|----------|
| **JWT 认证** | 调用任何 RESTful API（如 `/api/tasks`）不带 Token | 返回 401 Unauthorized |
| **角色权限** | 使用管理员和操作员两种 Token，操作员尝试执行管理操作（如删除任务） | 操作员返回 403 Forbidden，管理员可执行 |
| **Token 过期** | 使用过期 Token 调用接口 | 返回 401 并提示 Token 过期 |

### 5.2 输入校验

| 测试项 | 测试方法 | 通过条件 |
|--------|----------|----------|
| **SQL 注入** | 在任务名称、坐标等字段输入 `' OR 1=1 --` | 返回参数错误，不执行 SQL 注入，系统不崩溃 |
| **边界值** | 输入极大/极小坐标值、负优先级、超大任务数 | 系统返回友好错误提示，不崩溃，不产生异常日志 |
| **格式校验** | 提交非 JSON 格式的 body | 返回 400 Bad Request，提示正确的格式要求 |

### 5.3 通信安全

| 测试项 | 通过条件 |
|--------|----------|
| **ROS 通信加密** | 若启用加密选项，所有 Topic/Service 通信经过 TLS 加密，仿真环境中可降级但不影响功能 |
| **心跳检测** | 连续 500ms 未收到心跳，触发急停，日志记录时间戳 |

### 5.4 工具链安全【新增补全】

| 测试项 | 测试方法 | 通过条件 |
|--------|----------|----------|
| **工具可用性检查** | 系统启动时，检查 `cppcheck`、`clang-tidy`、`pylint`、`roslint` 是否安装 | 若缺失，跳过对应校验并记录告警（日志级别 WARNING），不阻塞主流程，系统正常启动 |
| **工具缺失告警** | 手动移除某个工具后启动系统 | 日志中明确记录“Tool X not found, skipping check”，告警信息可被管理员获取 |
| **工具版本兼容性** | 使用不兼容版本的工具（如 cppcheck 2.0 以上但要求 1.9） | 系统仍不阻塞，但记录告警提示版本不匹配（可选，若需求未明确版本要求则跳过） |

---

**补全说明**：以上用【新增】或【新增补全】标记的内容为根据需求文档审核后补充的验收标准，覆盖了功能点遗漏、非功能量化细化、测试环境明确、可执行性增强等方面。其余部分保持原验收标准不变。