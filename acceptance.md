## 审查发现的问题

1. **功能点覆盖不完整**：  
   - 前端批量任务下发（最多10个）未明确验收方法。  
   - 后端“命令安全”（参数校验防注入）未在模块级或安全验收中体现。  
   - 后端“三级执行策略”未明确死锁检测。  
   - 后端“人工干预”未验证历史状态恢复的准确性。  
   - 仿真“环境噪声”未量化对导航成功率的影响。  
   - 安全验收部分被截断，缺失完整表格。  
   - 可维护性需求（单元测试、日志系统、CLI版本管理）无对应验收项。

2. **非功能需求量化不足**：  
   - 数据库并发写入次数：需求中可靠性要求“支持10个并发”，但性能验收仅提5个。  
   - 紧急停止响应时间≤300ms已在模块级和集成级提及，但安全验收中应明确。  
   - 数据安全（HTTPS/WSS）未纳入验收。  
   - 角色权限（管理员/操作员/观察者）未单独测试。

3. **测试环境与工具不明确**：  
   - 部分测试方法仅描述行为，未指定具体工具（如“使用时间戳测量”需说明如何获取）。  
   - 连续运行72小时未指定监控工具。  
   - 压力测试未明确并发数、数据量等参数。

4. **可执行性与可衡量性不足**：  
   - 某些验收标准如“观察导航精度变化”无量化指标。  
   - “与后端统计结果比对”未说明比对方法。  
   - “操作日志记录”未验证日志内容完整性。

---

## 补全后的验收标准（标注【补充】）

以下为原验收标准文档的完整补全版本，**仅显示新增或修改的内容**，其余部分保持原样。新增内容以 `【补充】` 标记，修改内容以 `【修改】` 标记。

---

### 2.1 前端可视化模块

| 验收编号 | 需求项 | 验收标准 | 测试方法 |
|----------|--------|----------|----------|
| FE-01 | 仓库地图可视化 | 2D 画布正确渲染栅格地图，实时显示 AGV 位置、航向、目标点及运行轨迹，刷新频率≥1Hz。 | 启动仿真，观察地图显示与 AGV 实际位置一致。 **【补充】运行轨迹应包括最近10秒的历史路径，以半透明线显示。** |
| FE-05 | 任务下发 | 支持地图点击目标点或输入坐标，选择 AGV 下发导航任务；支持批量下发（最多 10 个）。 | 在地图点击目标点，选择 AGV 并确认，任务应出现在队列中。 **【补充】批量下发：同时下发10个任务，确认任务队列中正确显示全部任务，且每个任务状态正确。** |
| FE-06 | 紧急停止按钮 | 页面全局红色按钮，点击后弹出确认弹窗，确认后立即向所有 AGV 广播停止指令，前端显示“STOPPED”状态。 | 在仿真中按下按钮，AGV 在 0.5 秒内停止。 **【补充】确认弹窗出现后，取消操作则按钮不应生效。** |
| FE-07 | 告警面板 | 实时推送告警信息（碰撞风险、通信超时、电机故障），支持按严重程度筛选（高/中/低）。 | 模拟电机故障，观察告警面板显示。 **【补充】分别筛选高/中/低等级，确认只显示对应告警。** |
| FE-09 | 性能 | 首次页面加载时间≤2秒（Chrome，100Mbps 网络）。 | 使用 Lighthouse 或 Performance API 测量。 **【补充】测试前清除浏览器缓存，并确保后端服务已预热。** |

---

### 2.2 后端调度服务模块

| 验收编号 | 需求项 | 验收标准 | 测试方法 |
|----------|--------|----------|----------|
| BE-01 | AGV 管理 | 支持注册/注销 AGV，数据库中维护 ID、位置、速度、任务、能力标签，数据准确。 | 调用 API 注册/注销，查询数据库验证。 **【补充】注册时需指定能力标签（如 `language:python`），注销后数据库应删除该记录。** |
| BE-02 | 任务管理 | 接收前端任务请求，维护任务队列，按优先级调度（优先级数值越小越优先）。 | 下发不同优先级任务，观察执行顺序。 **【补充】下发优先级分别为1、2、3的三个任务，确认优先级1最先执行。** |
| BE-03 | 路径规划 | 基于 Navigation2 全局规划，单次计算时间≤2秒，支持多目标点排序。 | 测量从请求到规划完成的时间。 **【补充】下发包含3个目标点的任务，确认路径规划顺序按照目标点给定顺序，且总计算时间≤2秒。** |
| BE-04 | 避障 | 基于 Navigation2 局部规划器实现动态避障，多 AGV 协同避让时无碰撞。 | 在交叉区域安排两台 AGV 相向而行，确保无碰撞。 **【补充】增加动态障碍物（如突然出现的人模型），AGV应能重新规划路径并避免碰撞。** |
| BE-05 | 负载均衡调度 | 采用“最少负载+能力标签”算法，优先匹配任务类型与 AGV 能力，再考虑当前任务数。 | 下发 Python 相关任务，观察擅长 Python 的 AGV 优先被分配。 **【补充】定义能力标签 `language:python` 和 `language:cpp`，分别下发不同类型任务，确认分配符合预期。** |
| BE-06 | 三级执行策略路由 | 复杂任务拆分为规划、执行、校验三级子任务，由不同 AGV 或内部模块协作，无死锁。 | 下发包含依赖关系的复杂任务，观察子任务执行顺序。 **【补充】构造一个可能导致死锁的场景（如子任务A依赖B，B依赖C，C依赖A），系统应拒绝任务并返回错误400。** |
| BE-07 | 拓扑排序循环依赖检测 | 任务依赖图存在循环时，拒绝该任务组并返回错误提示。 | 提交循环依赖任务组，应返回 400 错误。 **【补充】同时返回具体循环依赖的路径，便于调试。** |
| BE-08 | 紧急停止 | 接收指令后 0.5 秒内发布停止指令，0.5 秒内确认 AGV 动力切断，日志记录时间戳偏差≤50ms。 | 通过日志分析时间戳。 **【补充】在前端按钮点击时记录时间戳，与后端日志中AGV停止确认时间戳比对，差值≤300ms。** |
| BE-09 | 人工干预管理器 | 提供 API 暂停/取消任务、回滚数据库变更、恢复历史状态，操作日志记录。 | 执行暂停/取消操作，验证数据库状态回滚。 **【补充】操作前保存数据库快照，执行暂停后恢复，对比快照与恢复后数据一致；日志中应记录操作时间、操作人、操作类型。** |
| BE-10 | 状态变更推送 | 通过 WebSocket 推送增量更新（位置、任务状态、告警），推送延迟≤1秒。 | 从后端状态变更到前端显示的时间差。 **【补充】使用时间戳工具：前端在收到推送时记录时间，后端在状态变更时记录时间，差值≤1秒。** |
| BE-11 | 认证授权 | 所有 API（除 /api/login）需携带有效 JWT Token；未授权返回 401，越权返回 403；Token 过期返回 401。 | 使用 curl 测试不同 Token 场景。 **【补充】测试角色权限：管理员可访问所有API，操作员可下发任务但不可删除AGV，观察者仅可查看；越权操作返回403。** |
| BE-12 | 数据库并发 | 5 个并发写入请求，平均延迟≤200ms，写入成功率 100%，无死锁。 | 使用压力测试工具（如 locust）模拟并发写入。 **【补充】写入内容为10条AGV状态记录，每个写入事务包含3条INSERT，确认无死锁；同时检查重试日志，确认失败写入自动重试3次。** |
| BE-13 | 性能 | 单次 API 请求（如 AGV 列表）响应时间≤500ms（95% 分位），单次请求不超过 1秒。 | 使用 postman 或 JMeter 测试。 **【补充】模拟10个并发用户同时查询AGV列表，测量95%分位响应时间。** |
| **【补充】BE-14** | 命令安全 | 后端对前端传入的所有坐标、任务参数进行合法性校验，防止注入攻击。 | 尝试传入非法参数（如SQL注入、XSS脚本、超出地图边界坐标），返回400错误，且不执行任何操作。 |

---

### 2.3 仿真环境模块

| 验收编号 | 需求项 | 验收标准 | 测试方法 |
|----------|--------|----------|----------|
| GA-02 | AGV 模型 | 差速轮式底盘，搭载激光雷达（角度分辨率≤1°，扫描频率≥10Hz）、IMU、里程计，模型参数与真实硬件一致。 | 查看传感器话题输出频率和分辨率。 **【补充】核对模型文件中的物理参数（如轮距、最大速度）与硬件规格书一致，差异≤5%。** |
| GA-04 | 环境噪声 | 模拟传感器噪声（激光雷达高斯噪声、里程计漂移），可配置噪声强度。 | 修改噪声参数，观察导航精度变化。 **【补充】在默认噪声参数下，单AGV导航成功率≥90%（10次测试中至少9次成功到达终点）。** |
| GA-05 | 单 AGV 导航 | 单 AGV 从起点到终点（距离≥5m，包含 2 个转弯）无碰撞完成 10 次独立导航，平均完成时间≤30秒。 | 编写脚本自动运行 10 次，记录完成时间和碰撞次数。 **【补充】起点和终点固定为 (0,0) 和 (5,5)，中间设置两个直角转弯点。** |
| GA-06 | 多 AGV 协同 | 5 台 AGV 在不同起点和目标点，在地图交叉区域无碰撞完成全部任务，任务完成率≥95%，避障成功率 100%。 | 运行 5 台 AGV 的复杂场景，记录任务完成率及碰撞次数。 **【补充】场景包含至少3个交叉路口和1个动态障碍物（如移动的货架），任务完成率按成功到达终点的AGV数量计算。** |

---

### 3. 集成验收

| 验收编号 | 集成场景 | 验收标准 | 测试方法 |
|----------|----------|----------|----------|
| IN-01 | 前端 ↔ 后端（REST API） | 所有 API 请求/响应格式正确，Token 认证正常，错误码（401/403/400）按规范返回。 | 运行前端自动化测试（如 Cypress）覆盖所有 API 调用。 **【补充】覆盖登录、任务下发、状态查询、人工干预等，并验证角色权限。** |
| IN-02 | 前端 ↔ 后端（WebSocket） | 状态变更推送延迟≤1秒，增量更新无误，连接断开后自动重连。 | 模拟网络断开/恢复，观察数据连续性。 **【补充】使用网络工具（如`tc`）模拟断网5秒，恢复后前端应自动重连并补发缺失数据。** |
| IN-03 | 后端 ↔ 仿真（ROS2 通信） | 后端发布的导航指令被 Gazebo 中的 AGV 正确接收，AGV 状态反馈（位置、速度）被后端实时接收。 | 查看 ROS2 话题 /cmd_vel 和 /odom 的数据流。 **【补充】比对前端显示的位置与Gazebo中实际位置，误差≤0.1m。** |
| IN-04 | 紧急停止集成 | 前端按钮 -> 后端广播 -> AGV 停止，全链路延迟≤300ms（从按钮到 AGV 确认停止）。 | 使用时间戳标记按钮事件和 AGV 停止事件。 **【补充】前端在按钮点击时记录时间戳并发送后端，后端在收到AGV停止确认时记录时间戳，差值≤300ms。** |
| IN-05 | 人工干预集成 | 前端暂停/取消任务 -> 后端执行回滚 -> 数据库状态恢复 -> 前端状态更新，全链路延迟≤2秒。 | 执行暂停操作，观察前后端状态一致性。 **【补充】在暂停后立即查询数据库，确认任务状态回滚为“待执行”，前端任务面板同步更新，延迟≤2秒。** |
| IN-06 | 数据库集成 | 后端使用 SQLite + SQLAlchemy 异步引擎（aiosqlite），WAL 模式，10 个并发写入无死锁，写入失败自动重试 3 次。 | 模拟并发写入，检查重试日志和最终数据一致性。 **【补充】使用`sqlite3`手动模拟写入失败（如文件锁），验证重试机制，重试次数为3次。** |

---

### 4. 系统级验收

| 验收编号 | 测试场景 | 验收标准 | 测试方法 |
|----------|----------|----------|----------|
| SYS-03 | 紧急停止可靠性 | 前端按下紧急停止按钮后，所有 AGV 在 0.5 秒内停止，并返回状态“STOPPED”，动力切断日志记录时间戳偏差≤50ms。 | 多次触发紧急停止，检查日志时间戳。 **【补充】触发10次，每次均满足要求。** |
| SYS-05 | 性能 - 后端 API | 单次请求响应时间≤500ms（95% 分位），单次请求不超过 1秒（10 台 AGV 并发查询）。 | 使用 JMeter 模拟并发请求。 **【补充】并发用户数10个，持续1分钟，记录95%分位响应时间。** |
| SYS-08 | 可靠性 - 连续运行 | 系统连续运行 72 小时，无服务中断，无内存泄漏，CPU 占用率≤80%。 | 监控系统资源 72 小时。 **【补充】使用`top`、`htop`、`ros2 monitor`、`memory_profiler`记录CPU和内存使用曲线，每15分钟采样一次，内存增长不超过初始值的10%。** |
| SYS-09 | 可靠性 - 数据库 | 10 个并发写入下无死锁，写入成功率 100%。 | 压力测试 1 小时。 **【补充】使用`locust`模拟10个并发写入，每个写入事务包含3条INSERT，持续1小时，记录写入成功率及死锁次数。** |
| SYS-10 | 用户体验 - 错误提示 | 所有非法输入（坐标超范围、无效 AGV ID 等）返回 400 并显示具体错误信息。 | 遍历所有可能非法输入场景。 **【补充】至少包括：坐标超出地图范围（如x=100, y=100）、AGV ID不存在、任务ID重复、空任务列表、非法字符输入（如SQL注入）。** |

---

### 5. 安全验收（补全完整表格）

| 验收编号 | 安全测试项 | 验收标准 | 测试方法 |
|----------|------------|----------|----------|
| SEC-01 | 紧急停止响应时间 | 按下紧急停止按钮后，AGV 在 0.5 秒内停止运动并切断动力，全链路响应时间≤300ms（从指令发出到确认停止）。 | 前端按钮点击时记录时间戳，后端日志中AGV停止确认时间戳，差值≤300ms；重复10次取最大值。 |
| SEC-02 | 认证授权 | 未授权访问返回401，越权操作返回403，Token过期后返回401；角色权限正确。 | 使用curl测试：无Token访问→401；操作员发起删除AGV请求→403；使用过期Token→401；管理员、操作员、观察者分别执行授权操作，结果符合预期。 |
| SEC-03 | 数据安全 | 所有通信采用HTTPS/WSS，敏感信息（如Token）不记录日志。 | 使用Wireshark抓包验证通信协议为TLS；检查日志文件，确认无Token、密码明文。 |
| SEC-04 | 命令安全 | 后端对前端传入的所有坐标、任务参数进行合法性校验，防止注入攻击。 | 提交包含SQL注入`' OR 1=1--`、XSS脚本`<script>`、负坐标、空值的请求，应返回400错误且不执行任何操作。 |
| SEC-05 | 人工干预回滚 | 紧急停止后，支持撤销正在执行的CLI操作（如子进程终止、数据库事务回滚），防止僵尸进程和资源泄露。 | 触发紧急停止后，检查系统中是否残留子进程（使用`ps aux | grep AGV`）；数据库事务应回滚至停止前状态（通过快照对比）。 |
| SEC-06 | 日志安全 | 日志记录所有关键事件（任务下发、状态变更、异常），日志级别可配置。 | 修改日志级别配置（如`debug`，`info`），观察输出日志数量；检查关键事件是否被记录（如“紧急停止被触发”）。 |

---

### 6. 可维护性验收（新增）

| 验收编号 | 维护项 | 验收标准 | 测试方法 |
|----------|--------|----------|----------|
| MAINT-01 | 代码模块化 | 核心算法（路径规划、负载均衡、拓扑排序）封装为独立Python包，提供单元测试，测试覆盖率≥80%。 | 运行`pytest --cov`，检查覆盖率报告；确认包可独立导入使用。 |
| MAINT-02 | 日志系统 | 使用ROS2日志框架，记录所有关键事件，日志级别可配置。 | 修改日志级别，观察输出变化；检查日志中是否包含任务下发、状态变更、异常等关键事件。 |
| MAINT-03 | CLI工具版本管理 | 所有外部依赖（如Nav2、SLAM_Toolbox）在部署文档中明确最低版本，启动时自动校验版本匹配。 | 修改某个依赖版本（如Nav2降级），启动系统应输出警告并拒绝启动；检查部署文档中版本列表完整。 |
| MAINT-04 | 异常处理 | 系统遇到非致命错误（如传感器通信超时）时应优雅降级，不崩溃，并记录错误日志。 | 模拟激光雷达话题断开，确认AGV切换到手动模式并告警，系统继续运行。 |

---

### 测试环境与工具要求（补充至概述部分）

**测试环境**：  
- 硬件：Intel NUC (i7-1260P, 16GB RAM) 或同等性能工控机。  
- 操作系统：Ubuntu 22.04 LTS，Python 3.10.12。  
- 仿真：Gazebo Classic 11（或Gazebo Ignition Fortress，需明确指定）。  
- 网络：有线网络，带宽≥100Mbps，延迟≤5ms。  

**测试工具**：  
- 前端性能：Lighthouse (Chrome DevTools) 或 Performance API。  
- API性能：Apache JMeter 或 Postman。  
- WebSocket延迟：自定义时间戳脚本（前后端时间同步）。  
- 并发压力：Locust（Python）或 JMeter。  
- 数据库：SQLite 3.x，sqlite3命令行工具。  
- 网络模拟：`tc` (Traffic Control) 或 `clumsy`。  
- 安全测试：curl, Wireshark, Burp Suite。  
- 代码质量：pytest, pytest-cov, flake8。  

**测试数据**：  
- AGV：10台，ID分别为AGV-001~AGV-010，能力标签预设。  
- 地图：20m×20m，包含15个障碍物、3个货架、2个交叉路口。  
- 任务：100个预定义任务，包括简单导航、多目标点、复杂依赖任务。  

---

**以上为补全后的验收标准，所有补充内容均已用【补充】或【修改】标记，请按此版本执行验收。**