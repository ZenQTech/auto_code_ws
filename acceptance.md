好的，作为质量保障与迭代管理智能体，我已对您提供的验收标准进行了全面审查，并基于需求文档 V2.0 和架构分析结果，从四个维度（功能覆盖、非功能量化、环境工具明确、可执行性）补全了缺失项。以下是补全后的完整验收标准，所有补充内容均以 **【补充】** 标注，修正项以 **【修正】** 标注。

---

### 验收标准.md（补全版）

#### 1. 文档概述

本文档是基于《AGV 点到点自主搬运机器人系统需求文档 V2.0》及架构分析结果制定的验收标准。它明确了系统在交付前必须满足的各项可测试、可验证的指标，是项目验收的核心依据。验收范围覆盖**模块级、集成级、系统级**及**安全**四个维度。

#### 2. 模块级验收

本阶段旨在验证系统各独立模块的功能正确性、输入输出符合性及关键性能指标。

| 模块 | 验收项 ID | 验收标准 | 测试方法 / 预期结果 | 对应需求 |
| :--- | :--- | :--- | :--- | :--- |
| **认证与授权模块** | MOD-AUTH-01 | **JWT Token 认证** | 1. 使用无效/过期Token调用API，返回 \`401 Unauthorized\`。 <br> 2. 使用有效Token调用API，返回正常响应。 | 3.2.1 |
| | MOD-AUTH-02 | **权限控制** | 1. 操作员角色无法访问管理员API（如系统配置）。 <br> 2. 管理员角色可访问所有API。 | 3.2.1 |
| **任务调度模块** | MOD-SCH-01 | **最少负载调度算法** | 1. 创建3个机器人，负载分别为0，0，1。 <br> 2. 下发新任务，系统应将其分配给负载为0的机器人（优先分配第一个）。 | 2.4.1 |
| | MOD-SCH-02 | **拓扑排序循环依赖检测** | 1. 创建一组任务A（需B完成）、B（需C完成）、C（需A完成）。 <br> 2. 系统应检测到循环依赖，并拒绝执行，返回失败原因及拓扑图。 | 2.2.3 |
| **路径规划模块** | MOD-PATH-01 | **全局路径规划** | 1. 给定起点和终点。 <br> 2. 系统输出一条无碰撞、符合仓库布局的路径。 | 2.2.1 |
| | MOD-PATH-02 | **局部路径规划与避障** | 1. 机器人沿规划路径行驶。 <br> 2. 在机器人前方0.5米处放置障碍物。 <br> 3. 机器人应在<100ms内减速并停车，直到障碍物移除。 | 2.2.2, 3.1.3 |
| **任务管理模块** | MOD-TASK-01 | **任务状态流转** | 1. 依次执行：创建任务 -> 下发 -> 暂停 -> 恢复 -> 取消。 <br> 2. 验证每个操作后，任务状态正确更新（如：待执行 -> 执行中 -> 暂停 -> 执行中 -> 取消）。 | 2.1.3 |
| | MOD-TASK-02 | **多站点任务进度上报** | 1. 执行一个多站点任务（如3个站点）。 <br> 2. 系统应持续上报站点进度（如：已完成1/3站点）。 | 2.1.4 【修正】原为“6”，现改为2.1.4 |
| | MOD-TASK-03 【补充】 | **多站点顺序执行** | 1. 创建包含A、B、C三个站点的任务，要求机器人依次访问。 <br> 2. 机器人实际路径记录显示按A→B→C顺序到达，并在每个站点执行动作（如装卸货）。 | 2.1.2 |
| **三级执行策略路由算法** | MOD-ROUTE-01 【补充】 | **策略选择逻辑** | 1. 模拟三种场景：低拥堵（直接执行）、中等拥堵（逐步执行）、高拥堵/协同任务（协同执行）。 <br> 2. 系统应依据任务复杂度、环境拥堵指数、机器人自身状态（如电量、负载）自动选择正确策略。 | 2.2.3 |
| **数据持久化模块** | MOD-DB-01 | **SQLite WAL模式** | 1. 检查数据库配置文件，确认 \`PRAGMA journal_mode=WAL\`。 | 3.3.2 |
| | MOD-DB-02 | **自动备份与恢复** | 1. 系统运行15分钟后，检查备份目录，存在备份文件。 <br> 2. 手动模拟数据库文件损坏（如删除关键数据）。 <br> 3. 系统重启后，自动执行 \`PRAGMA integrity_check\`，发现损坏，从备份恢复，系统正常运行。 | 3.3.2 |
| | MOD-DB-03 【补充】 | **健康检查（写入后）** | 1. 每次数据库写入后，后台自动执行 \`PRAGMA integrity_check\`。 <br> 2. 注入一个损坏的写入操作（如通过模拟磁盘错误），系统应捕获异常并触发恢复流程。 | 3.3.2 |
| **安全机制子系统** | MOD-SAFE-01 | **硬件急停** | 1. 机器人以1.0m/s速度行驶。 <br> 2. 按下物理急停按钮。 <br> 3. 机器人应在0.3m内完全停止。 | 2.3.1, 5.1.3 |
| | MOD-SAFE-02 | **软件急停** | 1. 从远程控制界面发送急停指令。 <br> 2. 机器人应立即停止，并被锁定，无法通过常规指令恢复。 | 2.3.1 |
| | MOD-SAFE-03 | **边界限速** | 1. 设置机器人速度指令为2.0m/s。 <br> 2. 系统底层驱动强制将速度限制在1.2m/s输出。 | 2.3.3 |
| | MOD-SAFE-04 | **区域限速** | 1. 在仓库地图中标记一个“转弯区域”。 <br> 2. 机器人进入该区域时，最大速度自动降至0.5m/s，离开后恢复。 | 2.3.3 |
| **CLI依赖管理** | MOD-CLI-01 | **依赖验证** | 1. 检查CI/CD流水线，包含自动化步骤验证 \`Hermes CLI\` 版本、安装正确性。 | 3.4.1 |
| | MOD-CLI-02 | **超时降级** | 1. 模拟外部CLI调用超时（如设置网络延迟）。 <br> 2. 系统应在30s（简单任务）或120s（复杂任务）后触发超时处理。 <br> 3. 系统应上报执行进度，并降级处理（如返回部分结果或触发人工干预）。 | 3.1.3 |
| | MOD-CLI-03 【补充】 | **容器化部署文档** | 1. 检查文档中是否包含所有依赖CLI工具的Docker镜像或容器化部署方案。 <br> 2. 若为内部工具，应提供Dockerfile或docker-compose示例。 | 3.4.1 |

#### 3. 集成验收

本阶段旨在验证关键模块间的交互逻辑、数据流正确性及协同工作的稳定性。

| 验收项 ID | 验收标准 | 测试方法 / 预期结果 | 涉及模块 |
| :--- | :--- | :--- | :--- |
| **INT-01** | **任务调度 -> 路径规划集成** | 1. 调度模块将任务分配至机器人A。 <br> 2. 机器人A的任务管理模块接收任务，并调用路径规划模块。 <br> 3. 路径规划模块成功返回从起点到终点的路径，并开始执行。 | 任务调度、任务管理、路径规划 |
| **INT-02** | **CLI依赖集成** | 1. 系统发起一个需要调用 \`Hermes CLI\` 的复杂调度计算。 <br> 2. \`Hermes CLI\` 被正确调用，并返回计算结果。 <br> 3. 系统成功解析结果并完成调度。 | 调度模块、CLI依赖封装层 |
| **INT-03** | **多机器人协同死锁预防** | 1. 3台机器人在狭窄通道两端相遇。 <br> 2. 系统调度模块使用拓扑排序检测循环依赖，避免死锁。 <br> 3. 机器人有序通过，无碰撞。 | 调度模块、路径规划、安全机制 |
| **INT-04** | **认证授权集成** | 1. 所有API接口和WebSocket连接在建立时必须进行身份验证。 <br> 2. 未授权访问被拒绝。 <br> 3. WebSocket通信中，Token过期后，连接被服务器主动断开。 | 认证授权模块、所有对外接口、WebSocket服务 |
| **INT-05** | **仿真环境集成** | 1. 从仓库CAD布局导入的Gazebo仿真环境能够正确加载。 <br> 2. 机器人在仿真环境中的定位、导航、避障行为与真实环境一致。 | 路径规划、仿真平台、机器人模型 |
| **INT-06 【补充】** | **CLI超时降级集成** | 1. 模拟复杂CLI调用超时（如网络延迟>120s）。 <br> 2. 系统在120s后触发超时处理，返回部分结果或触发人工干预，并上报进度。 <br> 3. 系统整体服务不崩溃，其他任务仍可正常执行。 | 调度模块、CLI依赖封装层、任务管理 |
| **INT-07 【补充】** | **任务状态端到端上报** | 1. 机器人执行任务过程中，实时将状态（待执行、执行中、暂停、完成、失败）上报至调度中心。 <br> 2. 调度中心数据库记录与机器人实际状态一致，延迟<1s。 | 任务管理、调度模块、数据持久化 |

#### 4. 系统级验收

本阶段验证整个系统是否满足业务目标、性能指标及可靠性要求。

| 验收项 ID | 验收标准 | 测试方法 / 预期结果 | 对应需求 |
| :--- | :--- | :--- | :--- |
| **SYS-FUNC-01** | **单机搬运成功率** | 在仿真环境中，单台机器人连续执行100次点到点搬运任务，成功率 ≥ 99%。 | 5.1.1 |
| **SYS-FUNC-02** | **多机协同无冲突** | 3台机器人在同一仿真环境中同时运行，连续执行50个任务，无碰撞、死锁或任务丢失。 | 5.1.2 |
| **SYS-PERF-01** | **平均单次任务时间** | 从任务下发到机器人完成搬运并返回待命点，平均单次任务时间 ≤ 30秒。 | 5.2.1 |
| **SYS-PERF-02** | **API端到端延迟** | API接口的P95响应时间 < 500ms（非CLI调用部分，如任务查询、状态上报）。 | 5.2.2 |
| **SYS-PERF-03** | **定位精度** | 在目标站点停靠时，定位精度需达到 ±3 cm。 | 3.1.2 |
| **SYS-PERF-04** | **避障响应时间** | 从激光雷达检测到障碍物到机器人发出停车指令的总延迟 < 100ms。 | 3.1.3 |
| **SYS-PERF-05 【补充】** | **最大速度硬限验证** | 1. 设置机器人速度指令为2.0m/s。 <br> 2. 实际测量机器人直线速度，最大不超过1.2m/s。 | 2.3.3 |
| **SYS-PERF-06 【补充】** | **区域限速系统级验证** | 1. 在仓库中定义多个限速区域（转弯、狭窄通道、人员区）。 <br> 2. 机器人经过每个区域时，实际速度自动降至对应限速值，离开后恢复。 | 2.3.3 |
| **SYS-REL-01** | **连续运行** | 系统在仿真环境中连续运行24小时，无故障。 | 5.3.1 |
| **SYS-REL-02** | **数据恢复** | 模拟数据库文件损坏，系统能自动从备份恢复并正常运行。 | 5.3.2 |
| **SYS-REL-03** | **MTBF验证** | 统计系统连续运行时间，平均无故障时间（MTBF）≥ 500小时。 | 3.3.1 |
| **SYS-REL-04 【补充】** | **CLI超时降级不影响系统稳定性** | 1. 在系统满载（多任务并发）时，触发一个CLI超时。 <br> 2. 系统正确降级，其他任务不受影响，无崩溃或数据丢失。 | 3.1.3, 3.3.1 |
| **SYS-LOAD-01** | **调度算法负载均衡** | 在任务数超过机器人数量2倍的情况下，系统能自动调度并完成所有任务。所有机器人的负载量（正在执行+排队任务数）差异不超过20%。 | 5.4.1 |
| **SYS-MAIN-01** | **依赖检查** | 1. 文档中明确列出所有依赖的CLI工具版本、安装方式、开源协议。 <br> 2. CI/CD流程中包含自动化依赖检查步骤，版本不匹配时构建失败。 | 3.4.1 |
| **SYS-MAIN-02** | **日志与监控** | 系统输出结构化日志，并能通过监控接口查看关键指标（任务完成率、机器人状态、CPU/内存使用率）。 | 3.4.2 |
| **SYS-MAIN-03 【补充】** | **容器化部署文档完整性** | 1. 文档中提供所有依赖CLI工具的Docker镜像或容器化部署方案（如Dockerfile、docker-compose）。 <br> 2. 使用文档中的方案可一键部署依赖环境并通过测试。 | 3.4.1 |

#### 5. 安全验收

本阶段专门针对系统安全进行深度验证，涵盖访问控制、数据安全、功能安全及通信安全。

| 验收项 ID | 验收标准 | 测试方法 / 预期结果 | 对应需求 |
| :--- | :--- | :--- | :--- |
| **SEC-01** | **认证与授权强度** | 1. 暴力破解测试：尝试使用常见弱密码登录，应被锁定或拒绝。 <br> 2. 权限越界测试：低权限用户尝试访问高权限API，应被拒绝。 | 3.2.1 |
| **SEC-02** | **数据加密** | 1. 数据库文件（SQLite）应使用加密存储（如SQLCipher或AES-256）。 <br> 2. 检查数据库文件，无法直接使用文本编辑器或sqlite3命令行读取明文内容。 | 3.2.2 |
| **SEC-03** | **通信安全** | 1. 所有API和WebSocket通信应使用HTTPS/WSS协议。 <br> 2. 使用中间人攻击工具抓包，应无法获取明文数据。 | 3.2.1 |
| **SEC-04** | **输入验证** | 1. 对API输入参数进行SQL注入、XSS等注入攻击测试，系统应能正确过滤并返回错误。 | 3.2.1 |
| **SEC-05** | **安全更新与补丁** | 验证系统具备或文档说明安全更新的流程，确保所有依赖库（如ROS 2、Python包）能及时更新到安全版本。 | 3.4.1 |
| **SEC-06** | **日志安全审计** | 所有安全相关事件（如登录失败、权限越界、API滥用）都应记录在日志中，并包含时间戳、用户ID、源IP。 | 3.4.2 |
| **SEC-07** | **物理安全** | 1. 硬件急停按钮功能验证（同MOD-SAFE-01）。 <br> 2. 验证机器人底盘上的急停按钮是否易于触及且标识清晰。 | 2.3.1 |
| **SEC-08 【补充】** | **软件急停远程锁定** | 1. 发送软件急停指令后，机器人立即停止并被锁定。 <br> 2. 尝试通过常规指令（如恢复、移动）恢复，应被拒绝，直到管理员解锁。 | 2.3.1 |
| **SEC-09 【补充】** | **数据库加密密钥管理** | 1. 加密密钥应存储在安全区域（如硬件安全模块或环境变量），不得硬编码在代码中。 <br> 2. 密钥泄露后，系统应支持轮换密钥，且不影响已有数据。 | 3.2.2 |
| **SEC-10 【补充】** | **冗余方案文档检查** | 1. 若部署环境允许，文档中应包含迁移至PostgreSQL的详细方案及可靠性提升说明。 <br> 2. 验证当前SQLite方案在WAL模式下的并发性能满足需求（如100次/秒写入无锁）。 | 3.3.2 |
| **SEC-11 【补充】** | **WebSocket连接鉴权细节** | 1. 建立WebSocket连接时，必须在URL参数或头部携带有效Token。 <br> 2. Token过期后，服务端主动断开连接，并记录审计日志。 | 3.2.1, INT-04 |
| **SEC-12 【补充】** | **安全日志完整性** | 1. 日志不可被普通用户删除或篡改，仅管理员可管理。 <br> 2. 日志文件大小达到阈值后自动轮转，不丢失历史记录。 | 3.4.2 |

---

**补充说明**：以上所有补充项均来源于需求文档 V2.0 中明确要求但原验收标准未覆盖或未完整描述的部分。每个补充项均具备可测试的步骤和量化指标，确保验收的可执行性。