以下是补全后的验收标准文档，所有补充内容以“【补充】”标记。文档基于原结构，补全了缺失的验收项，并明确了测试环境、工具、数据要求，确保覆盖所有功能点、非功能量化、可执行性。

---

# 验收标准.md（补全版）

## 1. 文档概述

本文档基于《多AGV集中调度系统 V2.0 需求文档》定义的系统功能、非功能需求及技术约束，制定详细的验收标准。验收分为四个层级：**模块级验收**（各组件/子系统的独立验证）、**集成验收**（模块间通信与协作验证）、**系统级验收**（端到端功能、性能、可靠性验证）和**安全验收**（安全基线与攻击防护验证）。所有验收项需在真实仓库环境（或等效模拟环境）中通过自动化测试与人工检查双重确认。

**【补充】测试环境与工具要求**  
- **硬件环境**：  
  - AGV控制器：NVIDIA Jetson Orin Nano（8GB内存）或同等性能，配备激光雷达、轮式编码器、IMU、防跌落传感器（红外）、急停按钮。  
  - 中央服务器：CPU≥8核（x86_64），内存≥32GB，SSD≥500GB，双网卡（主备网络）。  
  - 仓库地图：1000×1000栅格（可动态调整），包含货架、充电站、边界。  
- **软件环境**：  
  - 操作系统：Ubuntu 22.04 LTS，ROS2 Humble，Python 3.10。  
  - 数据库：PostgreSQL 14+ 或 SQLite 3.40+（WAL模式+单写者协程队列）。  
- **测试工具**：  
  - API测试：Postman / JMeter（并发压测）。  
  - 网络抓包：Wireshark / tcpdump。  
  - 性能监控：Prometheus + Grafana（或ELK）。  
  - 模拟器：Gazebo（用于障碍物模拟及AGV动力学仿真）或真实AGV。  
  - 自动化测试框架：ROS2 `launch_testing` + `pytest`。  
- **测试数据**：  
  - 任务数据：随机生成取货点、送货点坐标（非障碍物区域）。  
  - 障碍物数据：静态障碍物（箱子坐标）、移动障碍物（行人速度0.8m/s）。  
  - 电量数据：初始电量可配置，测试充电时设为15%。

---

## 2. 模块级验收

### 2.1 中央调度器模块

#### 2.1.1 任务管理模块
- **任务分配**：输入3个任务（优先级分别为高、中、低），空闲机器人按优先级分配，高优先级任务分配给空闲机器人（若存在），否则按负载均衡分配。  
  **【补充】负载均衡公平性**：连续提交5个相同优先级任务，有3个空闲机器人，验证每个机器人分配的任务数量差≤1。
- **优先级队列**：模拟任务等待时间超过阈值（如30秒）时，任务优先级自动提升，验证调度器将该任务排到队首。
- **任务取消/暂停/重试**：
  - 下发任务后通过API取消，AGV收到取消指令并停止任务。
  - 暂停任务后，AGV原地等待，恢复后继续执行。
  - 失败任务自动重试（最多3次），重试间隔符合指数退避（1s、2s、4s）。  
  **【补充】重试次数限制**：模拟任务连续失败4次，验证第4次不再重试，任务状态标记为`failed`。

#### 2.1.2 路径规划模块
- **A*算法正确性**：在1000×1000栅格地图上，给定起点和终点，返回路径长度≤理论最短路径长度（对比Dijkstra结果），且无穿越障碍物。
- **动态重规划触发**：AGV上报局部避障失败（阻塞≥5秒），调度器在200ms内返回新路径。
- **路径规划失败处理**：当目标点被完全封锁时，返回错误码`PATH_NOT_FOUND`，并向任务管理器发送通知，日志记录失败原因。  
  **【补充】日志记录验证**：触发路径规划失败后，检查日志文件是否包含`PATH_NOT_FOUND`及目标点坐标、时间戳。

#### 2.1.3 通信管理模块
- **心跳检测**：AGV断开连接超过10秒，调度器标记该AGV为“离线”，并触发任务重分配（若有关联任务）。
- **消息协议**：所有调度器→AGV指令为JSON格式（符合预定义schema），AGV→调度器状态上报也为JSON，验证字段完整性（位置、速度、电量、故障码）。

#### 2.1.4 状态监控与日志模块
- **日志存储**：模拟连续运行30天，日志文件大小不超过100GB，且可正常按时间、机器人ID、任务ID检索（Elasticsearch或SQL查询）。  
  **【补充】日志格式统一**：检查日志条目是否为统一JSON格式（包含时间戳、级别、模块、消息），并验证可被ELK正确解析。
- **健康检查**：`/health`端点返回HTTP 200，内容包含“status: ok”及调度器版本号。  
  **【补充】调度器自动重启**：杀死调度器主进程（`kill -9`），验证systemd或supervisor在5秒内自动重启进程，重启后`/health`恢复正常，且任务队列不丢失（通过快照恢复）。

#### 2.1.5 API网关模块
- **认证拦截**：未携带JWT Token的请求返回401，携带过期Token返回403。
- **API响应时间**：在5个并发请求下，单个API（如任务提交、状态查询）平均响应时间≤100ms，P95≤300ms。

### 2.2 AGV机器人端模块

#### 2.2.1 指令执行模块
- **路径点序列执行**：接收调度器下发路径（含5个点），AGV按顺序行驶，停止时位置误差≤0.1m，到达每个路径点的时间误差≤0.5s。
- **速度/方向限制**：指令中的速度值若超过1.5m/s，AGV自动限速；方向指令与当前航向偏差超过45°时，AGV先减速再转向。

#### 2.2.2 局部避障模块
- **DWA/TEB算法集成**：在路径上随机放置移动障碍物（速度0.5m/s），AGV在无碰撞前提下绕行，绕行后重新回到原路径，局部避障响应时间≤50ms（从传感器输入到速度指令输出）。
- **阻塞超时上报**：AGV被静态障碍物（如箱子）阻挡超过5秒，向调度器发送`AVOIDANCE_FAILURE`事件，请求全局重规划。

#### 2.2.3 状态上报模块
- **上报频率**：每秒上报一次，通过抓包验证间隔为1s±0.1s。
- **数据完整性**：上报数据包含`robot_id, timestamp, x, y, theta, speed, battery, fault_code`，字段值在合理范围内（如battery: 0-100）。

#### 2.2.4 急停与边界检测模块
- **急停按钮**：按下物理急停按钮，AGV在0.1s内停止，电机抱死，并上报`EMERGENCY_STOP`状态。复位按钮按下后，AGV保持静止，等待调度器恢复指令。  
  **【补充】软件互锁验证**：在急停状态下，通过软件发送“继续行驶”指令，AGV不应执行，直到硬件复位且调度器下发恢复指令。
- **边界检测**：模拟靠近仓库边缘（距离≤0.2m），AGV自动停车并报警（蜂鸣器+指示灯），上报`BOUNDARY_ALARM`。  
  **【补充】硬件互锁验证**：检查防跌落传感器信号是否直接连接到电机驱动器使能脚，断开传感器信号后电机立即停止（不依赖软件）。

### 2.3 数据库模块

- **写入性能**：模拟5台AGV同时每秒上报状态，数据库写入延迟≤5ms（PostgreSQL）或≤10ms（SQLite WAL+单写者队列），无锁等待或数据丢失。  
  **【补充】高并发压力**：模拟10台AGV同时每秒上报，持续1小时，无写入失败或性能恶化。
- **数据持久性**：突然断电后，最后一次提交的任务状态不丢失（通过WAL或ACID事务保证）。  
  **【补充】单写者队列验证**：检查SQLite实现中是否使用`asyncio.Queue`单写者协程，确保无并发写入冲突。

---

## 3. 集成验收

### 3.1 调度器 ↔ AGV通信

- **正常通信链路**：调度器下发任务→AGV接收并执行→状态上报→调度器更新状态，端到端延迟≤50ms（Wi-Fi 6或5G专网环境）。
- **通信中断恢复**：人工断开Wi-Fi 10秒，AGV自动停车（保护模式）；恢复后AGV重新连接，调度器在5秒内下发安全路径（如原地等待或缓慢后退），AGV继续任务。
- **消息顺序性**：模拟网络丢包（丢包率10%），调度器与AGV之间采用有序消息队列（如ROS2 QoS），确保指令不丢失、不重复。

### 3.2 调度器 ↔ 外部API

- **任务提交接口**：通过RESTful API提交任务（POST /tasks），返回任务ID；查询任务状态（GET /tasks/{id}）返回正确状态（pending/active/completed/failed）。
- **身份认证流程**：获取Token（POST /auth/login）→使用Token调用API→Token过期后刷新（POST /auth/refresh）→新Token有效。

### 3.3 调度器主备切换

- **状态同步**：主调度器每5秒向备机同步快照（包含任务队列、AGV状态、路径规划缓存）。快照同步延迟≤1秒。
- **切换时间**：杀死主调度器进程，备机在5秒内接管，并重新与所有AGV建立连接，当前正在执行的任务数据不丢失（任务进度从快照恢复）。
- **AGV无感切换**：切换期间，AGV保持当前动作（未收到新指令则继续执行最后一条路径），切换完成后调度器补发增量指令。

### 3.4 多机器人冲突解决

- **时间窗冲突检测**：两台AGV路径交叉，调度器根据优先级或任务紧急度，将较低优先级AGV的路径时间窗后移，或触发重规划。验证两条路径无时空重叠。
- **动态路径预留**：AGV行驶过程中，调度器动态更新预留路径段，另一台AGV申请路径时避开已预留段。  
  **【补充】负载均衡下的冲突处理**：在3台AGV同时执行任务时，随机插入高优先级任务，验证冲突解决算法不导致死锁或无限等待。

---

## 4. 系统级验收

### 4.1 功能验收

| 验收项 | 条件与步骤 | 接受标准 |
|--------|------------|----------|
| 多机器人连续任务 | 3台AGV，每台执行10次取货→送货任务（共30次），随机分布取货/送货点 | 任务成功率100%，无碰撞（碰撞记录为0），任务完成时间不超过预期时间（基于最短路径理论值+20%余量） |
| 局部避障功能 | 在AGV路径上放置移动障碍物（行人速度0.8m/s），重复10次 | 避让成功100%，无碰撞，避让后自动恢复路径且任务完成 |
| 急停与复位 | 按下急停按钮→记录状态→按下复位→调度器恢复任务 | 急停后AGV立即停止，复位后从断点继续（未完成的路段重新规划），任务最终完成 |
| 边界检测 | AGV靠近仓库边缘（距离0.15m） | 自动停车，报警灯亮，调度器收到`BOUNDARY_ALARM`，人工干预后重启 |
| 电量不足自动充电 | 设置AGV初始电量为15%，调度器自动触发充电任务 | AGV停止当前任务，前往充电站，充电至80%后恢复原任务（或进入待命状态），验证调度器日志记录“充电完成” |
| 路径规划失败 | 将目标点用障碍物完全包围 | 调度器返回失败码，通知人工介入，日志记录“PATH_NOT_FOUND” |
| **【补充】待命任务** | 下发“待命”任务给空闲AGV | AGV原地等待，不执行移动；当有其他任务下发时，AGV退出待命并执行新任务 |
| **【补充】任务重分配触发** | 杀死AGV端路径规划子进程，导致心跳超时 | 调度器在2秒内感知并重启该子进程，同时将当前任务重分配给其他空闲AGV（若存在），被重分配的任务在新AGV上继续执行 |

### 4.2 性能验收

| 验收项 | 测试条件 | 接受标准 |
|--------|----------|----------|
| 最大速度稳定性 | AGV在直道行驶100m，激光测速 | 速度1.5m/s ±5%（即1.425-1.575m/s） |
| 全局路径规划响应时间 | 地图1000×1000栅格，随机起点终点，并发5次请求 | 平均≤200ms，P99≤500ms |
| 局部避障响应时间 | 障碍物突然出现在AGV正前方0.5m处，测量传感器到速度指令输出时间 | ≤50ms（使用ROS2时间戳测量） |
| API并发响应 | 5个并发请求（混合任务提交和状态查询） | 平均响应时间≤100ms，95%分位≤300ms |
| 系统负载 | 10台AGV同时运行（模拟），调度器CPU使用率≤80%，内存使用率≤70% | 满足性能指标，无丢包或超时 |

### 4.3 可靠性验收

| 验收项 | 测试方法 | 接受标准 |
|--------|----------|----------|
| 调度器单点故障 | 主调度器进程被kill，等待切换 | 切换时间≤5秒，任务不丢失，AGV不失控 |
| 通信中断恢复 | 断开AGV与调度器通信10秒，恢复 | AGV停车后自动恢复，任务继续完成 |
| 数据库压力测试 | 5台AGV模拟状态上报（每秒1次），持续1小时 | 无写入失败，无数据丢失，查询响应时间正常 |
| 子进程崩溃恢复 | 杀死AGV端路径规划子进程 | 调度器在2秒内感知并重启该进程，AGV短暂停止后恢复；同时检查任务重分配是否触发（见4.1） |
| 日志存储可靠性 | 连续运行7天，日志自动轮转 | 日志文件无损坏，按条件检索正确 |
| **【补充】系统可用性** | 连续运行7×24小时，统计总停机时间（计划内维护除外） | 可用性≥99.9%（即总停机时间≤10.08分钟），通过监控系统记录 |

---

## 5. 安全验收

### 5.1 认证与授权

- **未授权访问**：对任意API端点（除`/auth/login`）发送无Token请求，返回HTTP 401。
- **Token过期处理**：使用过期Token调用API，返回HTTP 403，并提示“Token expired”。
- **权限分离**：普通用户无法调用管理接口（如`/admin/restart`），返回403。
- **Token刷新**：在Token过期前通过刷新接口获取新Token，新Token有效期为24小时（从刷新时刻起）。

### 5.2 命令注入防护

- **结构化数据验证**：向任务名称字段插入恶意字符串（如`; rm -rf /`），AGV端JSON解析后不执行任何shell命令，仅记录字符串并忽略。
- **CLI扩展防护**：若未来启用CLI，验证使用`asyncio.create_subprocess_exec`而非shell=True，且参数严格白名单过滤（如仅允许数字、字母、下划线）。当前无CLI，仅做代码审查确认。

### 5.3 物理安全

- **急停优先级**：在AGV全速行驶时按下急停按钮，无论软件指令如何，AGV立即停车（可用电机抱死时间<0.1s验证）。
- **最大速度硬件限制**：在AGV电机驱动器中设定最大速度1.5m/s，尝试通过软件指令设置2.0m/s，AGV实际速度不超过1.5m/s。
- **边界检测硬件互锁**：防跌落传感器触发时，电机驱动立即停止，不依赖软件，验证传感器信号直接连接到驱动器使能脚。

### 5.4 异常安全

- **通信中断保护**：AGV在接收不到调度器指令时，自动在10秒后停车，并进入保护模式（电机抱死，蜂鸣器响）。
- **调度器崩溃保护**：杀死主调度器进程，备机在5秒内接管，AGV保持当前动作（最后一条指令），切换完成后调度器补发增量指令，无失控现象。  
  **【补充】调度器崩溃期间AGV行为**：在主调度器宕机、备机尚未接管时，AGV应继续执行最后收到的路径点序列，若路径点执行完毕则停车等待，不产生危险动作。

---

## 6. 可维护性验收（新增）

**【补充】6.1 算法可配置**  
- 修改`config/path_planning.yaml`中A*启发函数为`euclidean`，重启调度器后，验证同一任务生成的路径与默认`manhattan`不同（长度差异合理）。  
- 修改`config/local_avoidance.yaml`中DWA最大速度从0.5m/s改为0.3m/s，重启AGV后，验证避障时最大速度不超过0.3m/s。

**【补充】6.2 模块化设计**  
- 代码审查确认中央调度器分为任务管理、路径规划、通信管理、状态监控、API网关五个独立模块，每个模块有独立接口和依赖注入。  
- 单元测试覆盖每个模块核心函数，要求代码覆盖率≥80%（通过`pytest-cov`测量）。

**【补充】6.3 日志与监控集成**  
- 部署ELK（或Prometheus+Grafana）后，验证调度器日志能实时传输到Elasticsearch，并可在Kibana中按时间、机器人ID、任务ID搜索。  
- Grafana面板显示关键指标：AGV数量、任务吞吐量、API响应时间P95、子进程状态。

---

**文档结束**