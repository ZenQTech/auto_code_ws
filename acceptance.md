以下是根据需求文档和当前验收标准，从功能覆盖、非功能量化、测试环境与工具、可执行性四个维度审查后，补全的验收标准。**新增或补全的内容以 `【补充】` 标记**，原有内容保留不变。

---

# 验收标准.md（补全版）

## 1. 文档概述

本验收标准文档基于 AGV 调度系统 V2.0 需求文档及架构分析制定，覆盖模块级、集成级、系统级和安全级四个维度的验收要求。所有测试均在 Gazebo Ignition 仿真环境中执行，验证系统功能、性能、可靠性和安全性达标。

---

## 2. 模块级验收

### 2.1 任务管理与调度模块

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 任务导入 | 支持 JSON/CSV 格式导入，正确解析任务属性（起点、终点、优先级、时间约束） | 提供 10 条不同格式的任务文件，验证解析结果与预期一致 |
| 最少负载调度算法 | 根据负载评估函数（任务队列长度、剩余电量、距离）分配任务，负载均衡度偏差 ≤ 20% | 模拟 3 台 AGV 初始状态不同，提交 10 个任务，检查各 AGV 最终任务队列长度标准差 |
| 任务依赖检测 | 有向无环图（DAG）检测：合法依赖图输出正确执行序列；循环依赖时拒绝提交并返回错误码 400 | 设计 3 个依赖场景（无环、单环、多环），验证系统行为 |
| 任务状态跟踪 | 实时更新状态（待分配、执行中、完成、失败），API 查询返回最新状态 | 连续执行任务，每 100ms 查询状态，验证状态流转正确性 |
| 任务持久化 | 任务记录存储于 SQLite，崩溃重启后状态不丢失 | 强制 kill 进程，重启后查询任务状态应与备份一致 |
| **【补充】任务依赖执行顺序** | 合法的依赖关系（如 B 依赖 A）确保 A 完成后才分配 B，拒绝执行顺序反转 | 提交 3 个任务（A→B→C），验证执行序列为 A→B→C |
| **【补充】拓扑排序性能** | 循环依赖检测时间 ≤ 200ms（100 个节点任务组） | 构造包含 100 个任务的依赖图（含环），记录检测时间 |

### 2.2 路径规划模块

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 全局路径规划（A*） | 静态地图上生成最优路径，计算时间 ≤ 0.5s，路径长度 ≤ 理论最短路径的 110% | 使用 10 组随机起点终点，测量时间与路径长度 |
| 局部重规划（Dijkstra） | 动态障碍物出现后，0.2s 内生成新路径，避开障碍物且不与其他 AGV 路径冲突 | 在已有路径上放置障碍物，记录重规划时间与轨迹 |
| 避障与微调（三级） | 传感器数据触发实时偏移，AGV 与障碍物距离始终 ≥ 0.15m（安全裕度） | 在 AGV 前方 0.2m 处放置障碍物，观察 AGV 减速/偏移行为 |
| 多 AGV 协同 | 路径规划模块主动避免死锁，无路径冲突导致锁死 | 同时运行 3 台 AGV，在交叉路口反复调度，连续 100 次无碰撞 |
| **【补充】多 AGV 协同 – 死锁避免** | 路径规划模块生成的路径段在交通管理模块协商后，无循环等待导致死锁 | 构造 3 台 AGV 互相阻塞路径，验证系统在 3s 内通过重规划解除死锁 |

### 2.3 交通管理模块

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 路口协商 | 优先级高的 AGV 优先通过，次优先级等待时间 ≤ 2s | 构造两 AGV 同时到达十字路口，高优先级任务紧急程度高，验证通行顺序 |
| 防碰撞（距离 < 0.3m 减速） | 当两 AGV 距离 ≤ 0.3m 时，两者速度均降至 0.3m/s 以下 | 让两 AGV 相向而行，记录距离与速度变化曲线 |
| 防碰撞（距离 < 0.1m 急停） | 距离 ≤ 0.1m 时，两 AGV 立即停止，且停止后距离 ≥ 0.08m | 同上，验证急停触发阈值与停止位置 |
| 死锁检测与解除 | 图论检测循环等待，自动任务重分配或重规划路径，解除时间 ≤ 3s | 构造 3 台 AGV 互相等待场景，验证系统自动恢复，并检查日志中是否记录重分配/重规划行为 |
| **【补充】死锁解除后状态** | 死锁解除后，所有 AGV 恢复正常任务执行，无任务丢失 | 同上，恢复后检查任务队列完整性 |

### 2.4 通信与状态监控模块

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 心跳频率 | AGV 状态更新频率 ≥ 10Hz，调度中心接收延迟 ≤ 100ms | 在 ROS Topic 上抓取 1000 条消息，计算平均间隔与延迟 |
| 通信故障检测 | 连续 500ms 未收到心跳，触发急停 | 手动停止 AGV 节点，观察调度中心日志与 AGV 停止指令 |
| Web 界面实时性 | 界面显示位置、状态刷新延迟 ≤ 500ms | 在界面中观察 AGV 移动，与仿真器实际位置对比，同时记录界面更新时间戳与仿真器时间戳差值 |
| **【补充】Web 界面数据准确性** | 界面显示的 AGV 位置、任务状态与后端数据一致 | 随机抽取 10 个时间点，对比 API 返回数据与界面渲染内容 |

### 2.5 安全与异常处理模块

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 急停触发（障碍物 < 0.1m） | AGV 立即停止，停止后距离 ≥ 0.08m，障碍物移除后自动恢复路径规划 | 在 AGV 前方 0.05m 处放置障碍物，观察行为 |
| 急停触发（通信超时） | 同 2.4 通信故障检测，500ms 内触发急停 | 同上 |
| 边界保护 | 工作区域多边形外，AGV 停止并记录日志，不越界 | 将 AGV 遥控至边界外 0.1m，验证停止与日志 |
| 速度限制 | 线速度 ≤ 1.0 m/s，角速度 ≤ 0.5 rad/s，可通过配置文件调整 | 强制发送高速指令，检查实际速度上限；修改配置文件中的线速度最大为 0.5 m/s，重启系统后验证速度限制立即生效 |
| 降级运行 | 核心算法模块不可用时，系统自动切换为轮询调度，并发出告警 | 手动停止路径规划算法进程，观察调度模式变化，并检查日志中告警记录 |
| **【补充】急停触发（传感器失效）** | 模拟激光雷达或超声波传感器失效，系统检测到致命错误后触发急停并记录日志 | 在仿真中关闭传感器 topic，检查 AGV 是否停止且日志记录“传感器失效” |
| **【补充】降级告警通知** | 降级切换时，系统通过 Web 界面或日志输出明确告警信息（如“核心算法不可用，降级至轮询调度”） | 同上，检查 Web 界面告警提示或日志文件内容 |

### 2.6 扩展接口模块

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| AGV 数量扩展 | 通过配置文件将 AGV 数量从 3 增至 5，新增 AGV 自动注册并参与调度 | 修改配置文件，启动系统，检查 5 台 AGV 均工作 |
| RESTful API 集成 | 第三方系统可通过 API 提交任务、查询状态，响应时间 ≤ 200ms | 使用 curl 或 Postman 测试 CRUD 操作，记录响应时间 |
| **【补充】动态增加 AGV（不重启）** | 若支持运行时动态增加 AGV，则新增 AGV 在 5s 内被系统识别并开始接收任务 | 运行时修改配置文件并触发热加载，观察新增 AGV 是否自动注册 |
| **【补充】API 输入校验** | 非法参数（如负坐标、超长字符串）返回 400 错误，不导致系统崩溃 | 提交包含负坐标、超长字符串的任务，验证响应码和错误信息 |

---

## 3. 集成验收

### 3.1 模块间接口一致性

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 任务管理 → 调度 | 分配任务后，调度模块正确更新 AGV 任务队列，无丢失或重复 | 模拟 10 个任务分配，检查 AGV 内部任务队列内容 |
| 调度 → 路径规划 | 调度模块传递任务起点终点，路径规划模块返回有效路径，通信延迟 ≤ 50ms | 监控跨模块调用时间 |
| 路径规划 → 交通管理 | 路径规划生成的路径经过交通管理模块协商，无冲突片段 | 在交叉口验证路径段独占性，通过监控两个模块的交互日志确认 |
| 通信监控 → 安全急停 | 心跳超时后，安全模块正确接收信号并触发急停 | 手动断开 AGV 心跳，验证急停指令发出 |
| 数据持久化 → 恢复 | 进程崩溃后，SQLite 数据完整，恢复后任务状态与备份一致 | 模拟崩溃，从备份恢复，对比任务列表 |
| **【补充】任务依赖 → 调度** | 任务依赖检测模块输出的合法执行序列，调度模块按顺序分配任务，确保前置任务完成后再分配后续任务 | 提交依赖任务组，监控任务分配顺序是否符合预期 |

### 3.2 外部依赖集成

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| ROS 通信 | AGV 与调度中心通过 ROS Topic/Service 通信，10Hz 数据正常 | 启动系统，检查 Topic 列表与消息频率 |
| Gazebo Ignition 仿真 | 仿真环境正确加载 AGV 模型，传感器数据（激光雷达、超声波等）可用 | 检查仿真器输出 topic |
| 工具链依赖 | 启动时检测 cppcheck、clang-tidy、pylint、roslint，缺失时告警不阻塞 | 移除其中一个工具，观察启动日志 |
| Docker 镜像 | 基于 Ubuntu 20.04 + ROS Noetic + Gazebo Ignition Garden 构建，容器内系统正常工作 | 运行 Docker 容器，执行全部功能测试 |
| **【补充】环境依赖检测** | 系统启动时自动检测 ROS 版本（Noetic）、Gazebo 版本（Garden），若不匹配则输出明确错误提示并退出 | 修改 ROS 版本至 Melodic，观察启动日志是否报错 |
| **【补充】仿真模型一致性** | AGV 运动学模型与传感器配置（激光雷达、超声波、IMU、编码器）与需求文档一致 | 检查仿真模型文件中的参数，并通过 topic 类型验证传感器数据发布 |

---

## 4. 系统级验收

### 4.1 功能场景验收

| 场景 | 验收标准 | 测试设计 |
|------|----------|----------|
| **场景一**：3 台 AGV 执行 10 个任务 | 无碰撞、无死锁，任务完成率 ≥ 99%，总耗时 ≤ 不超过 A* 估算时间的 120% | 运行 10 次，记录碰撞次数、死锁次数、完成率 |
| **场景二**：动态扩展至 5 台 AGV | 新增 AGV 后，系统继续分配任务，调度逻辑正常，无数据冲突 | 先运行 3 台，运行时动态增加 2 台，观察 30 分钟 |
| **场景三**：通信中断恢复 | 中断 > 500ms 触发急停，恢复后 10s 内自动重新规划并继续执行 | 模拟网络断开 2s，恢复后检查 AGV 状态 |
| **场景四**：障碍物急停恢复 | 障碍物 < 0.1m 急停，移除后 5s 内重新规划路径，继续执行 | 放置障碍物 3s，移除后观察 |
| **场景五**：循环依赖拒绝 | 提交含有循环依赖的任务组，系统返回错误并拒绝，不修改现有任务 | 提供错误依赖图，验证 API 返回 400 及错误信息 |
| **【补充】场景六：任务依赖正确执行** | 提交 5 个具有线性依赖关系（A→B→C→D→E）的任务，系统按顺序执行，无并发冲突 | 运行测试，验证执行顺序与任务完成时间戳 |

### 4.2 性能验收

| 指标 | 验收标准 | 测量方法 |
|------|----------|----------|
| 调度响应时间 | 从任务提交到分配完成 ≤ 1.0s（3 台 AGV，10 个任务并发） | 记录 100 次任务提交时间戳，取 95 分位值 |
| 全局路径规划时间 | ≤ 0.5s | 连续 100 次规划，取最大值 |
| 局部重规划时间 | ≤ 0.2s | 同上 |
| 系统吞吐量 | 同时处理 ≥ 50 个待分配任务，调度延迟 ≤ 2s | 批量提交 50 个任务，记录最后任务分配完成时间，测量从提交到分配的延迟 |
| 5 台 AGV 并发 | 响应时间仍 ≤ 2.0s | 5 台 AGV 同时运行，重复 10 次测试 |
| 传感器数据更新频率 | 激光雷达、超声波等 ≥ 10Hz | 抓取 topic 数据，计算平均间隔 |
| **【补充】拓扑排序检测时间** | 100 个节点依赖图检测时间 ≤ 200ms | 构造大图，记录检测耗时 |

### 4.3 可靠性验收

| 指标 | 验收标准 | 测试方法 |
|------|----------|----------|
| 连续运行时间 | 72 小时无崩溃、无内存泄漏（**【补充】最终验收需满足 7×24 小时（168 小时）连续运行，因测试周期限制，先执行 72 小时测试，若期间无故障则推断通过**） | 启动系统，每 12 小时记录内存占用，检查日志 |
| MTBF | ≥ 1000 小时（无故障间隔） | 根据 72 小时测试推断，若期间无故障则通过 |
| 故障恢复时间 | 系统崩溃后重启 ≤ 30s，任务状态不丢失 | 强制 kill 进程，计时重启并查询任务状态 |
| 数据持久化 | SQLite 备份策略：每小时增量，每日全量 | 检查备份文件生成情况，并验证从备份恢复后的数据完整性（任务列表、AGV 状态日志） |
| 通信故障监测 | 500ms 内触发急停（从心跳丢失到指令发出） | 模拟心跳丢失，记录时间差（通过日志时间戳或示波器） |
| **【补充】内存泄漏检测** | 72 小时运行时内存占用增长 ≤ 5%（排除缓存） | 每 12 小时记录内存使用量，计算增长率 |

### 4.4 安全验收

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| API 认证 | 所有 RESTful API 需 JWT Token，无 Token 或 Token 无效返回 401 | 使用 curl 发送未授权请求，检查响应码 |
| 角色权限 | 管理员与操作员权限分离，操作员不能执行管理操作（如增加 AGV） | 使用不同角色 Token 测试敏感接口 |
| 输入校验 | 特殊字符、边界值输入不导致崩溃，返回友好错误信息 | 提交包含 SQL 注入、XSS 等 payload 的任务，验证返回 400 且无系统异常 |
| 通信加密 | ROS 通信可选加密，仿真环境下可降级，但需记录降级告警 | 启用加密模式，检查通信是否正常；未启用时检查日志输出“ROS 通信未加密，降级运行”告警 |
| 安全工具链 | cppcheck、clang-tidy、pylint、roslint 缺失时，系统启动并告警，不阻塞 | 删除工具，重启系统，查看告警日志 |
| 速度限制可配置 | 修改配置文件中的速度上限，实际速度立即生效 | 修改线速度最大为 0.5 m/s，重启系统（或热加载）后，强制发送高速指令，验证实际速度 ≤ 0.5 m/s |
| **【补充】通信加密完整性** | 启用加密后，所有 ROS 话题/服务通信内容不可明文读取（可选） | 在加密模式下，尝试监听 topic 内容，应无法解析原始数据 |

---

## 5. 可维护性验收（补充）

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| **【补充】关键操作日志** | 任务分配、路径规划、异常触发（急停、降级、死锁解除）等关键事件均记录详细日志，包含时间戳、事件类型、相关参数 | 执行一次完整任务流程后，检查日志文件，确认包含上述事件 |
| **【补充】日志级别配置** | 日志级别（DEBUG/INFO/WARN/ERROR）可通过配置文件动态调整，无需重启 | 修改配置文件日志级别为 DEBUG，检查日志输出是否包含 DEBUG 信息；改回 INFO 后不再输出 DEBUG |
| **【补充】配置动态加载** | 算法阈值（速度限制、刹车距离、复杂度评分阈值）修改后，无需重启系统即可生效 | 运行时修改配置文件，观察 AGV 实际行为是否立即改变（如速度限制从 1.0 改为 0.5） |
| **【补充】模块化代码结构** | 代码按表现层、调度层、算法层、通信层、数据层分层，各层之间通过接口通信，无跨层依赖 | 通过代码审查或静态分析验证层间依赖关系符合架构设计 |

---

## 6. 可用性验收（补充）

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| **【补充】环境依赖检测** | 系统启动时自动检测 ROS 版本、Gazebo 版本、Python 版本、依赖库，不满足时输出明确错误信息并退出 | 修改 ROS 版本至不兼容版本，启动系统，检查错误提示是否明确 |
| **【补充】Docker 镜像一致性** | 基于 Docker 镜像部署的系统，功能测试、性能测试结果与原生环境一致 | 在 Docker 容器内运行全部验收测试，对比结果与原生环境差异 ≤ 5% |

---

**说明**：以上补全内容已用 `【补充】` 标记，未修改原有验收标准。所有新增验收项均依据需求文档 V2.0 中的功能、非功能需求及技术约束制定，确保覆盖全面、可执行、可衡量。