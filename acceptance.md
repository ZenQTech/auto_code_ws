以下是根据您提供的需求文档和当前验收标准，从功能覆盖、非功能量化、测试环境与工具、可执行性与可衡量性四个维度进行审查后，补全的验收标准文档。所有补充内容均以 **【补充】** 标注，并建议在项目澄清阶段最终确认。

---

# 验收标准（补全版）

## 1. 模块级验收

### 1.1 导航模块
- **定位精度**：≤0.05m（静态和动态）。  
  **【补充】** 测试方法：使用高精度定位参考系统（如Vicon或激光跟踪仪）对比，静态测试10次取平均值，动态测试沿直线路径1m/s速度下连续采样100点。
- **路径跟踪误差**：直线行驶（1.0m/s）≤0.1m；转弯（0.5m/s，90°）≤0.15m（待确认）。  
  **【补充】** 弯道测试需明确转弯半径（建议≥0.5m）和路径规划算法（如DWA、TEB）。
- **路径规划**：可生成全局路径，避开静态障碍物，绕行后恢复路径误差≤0.1m（待确认）。  
  **【补充】** 测试场景：在2m×2m区域内放置1个0.5m×0.5m的静态障碍物，AGV从起点到终点绕行后恢复原路径，误差取最大偏差。
- **软件版本**：ROS/ROS2版本及依赖库版本待确认（需明确为Ubuntu 20.04+ROS Noetic、Ubuntu 18.04+ROS Melodic 或 Ubuntu 22.04+ROS2 Humble 等）。  
  **【补充】** 建议优先选择**Ubuntu 20.04 + ROS Noetic**（与RPLIDAR A2、MPU6050驱动兼容性成熟），并列出所有依赖库及版本号（如`navigation`、`slam_gmapping`、`ros_control`等）。集成测试前需验证所有依赖无冲突。

### 1.2 避障模块
- **静态障碍物检测**：LiDAR RPLIDAR A2 有效检测距离内，障碍物识别正确率≥99%。  
  **【补充】** 测试方法：在0.2m~6m范围内随机放置10个不同形状（圆柱、矩形、人形）障碍物，重复5次，正确识别率 = 正确识别次数 / 总出现次数。
- **动态避障**：对移动障碍物（如行人）的反应时间≤0.5s（待细化）。  
  **【补充】** 定义反应时间：从障碍物进入安全距离（0.5m）到AGV开始减速或停车的时间。测试时行人以0.5m/s速度横向穿过AGV路径，记录触发时刻。
- **安全距离**：虚拟安全区域阈值0.5m，触发后减速或停止。  
  **【补充】** 明确减速策略：当距离≤0.5m时，AGV速度降至0.2m/s；当距离≤0.3m时，立即停车。

### 1.3 电机与驱动模块
- **最大线速度**：≤1.5m/s（可配置）。  
  **【补充】** 软件限速必须不可绕过（如通过Ubuntu的`systemd`服务或ROS参数服务器强制硬限制，并记录日志）。
- **最大角速度**：≤0.5rad/s（可配置）。
- **制动距离**：直线行驶1.0m/s速度下，制动距离≤0.3m；满载50kg时制动距离≤0.4m（待确认）。  
  **【补充】** 制动距离测量：从发出急停指令到AGV完全停止的位移，使用激光测距仪或编码器数据计算，重复5次取最大值。
- **电机控制**：编码器反馈精度满足定位要求，差速驱动控制误差≤0.02m。  
  **【补充】** 测试方法：AGV以0.5m/s直线行驶10m，实际位移与理论位移偏差≤0.02m。

### 1.4 传感器模块
- **LiDAR**：RPLIDAR A2 扫描频率≥10Hz，测距精度≤0.03m。  
  **【补充】** 测试工具：使用标准反射率标靶（如白色亚克力板），在1m、3m、5m处各测10次，取均方根误差。
- **IMU**：MPU6050 姿态角精度≤0.5°，数据更新率≥100Hz。  
  **【补充】** 测试方法：将IMU固定于水平平台，标定后记录10分钟静态数据，角度漂移≤0.5°；动态使用旋转台测试。
- **急停传感器**：物理急停按钮（位于车身）和/或远程无线急停信号，需确认最终方案（待定）。  
  **【补充】** 若采用远程无线，需明确无线协议（如ZigBee、LoRa、WiFi）和信号覆盖范围（建议≥50m）。响应时间≤200ms，测试时使用逻辑分析仪记录信号到电机停转的时间。

### 1.5 通信与接口模块
- **主控与上位机**：STM32F4与ROS节点通信延迟≤10ms。  
  **【补充】** 测试方法：通过`rostopic`命令行发送`/cmd_vel`并接收`/odom`，循环1000次，计算平均延迟。
- **远程急停**：无线信号响应时间≤200ms（如有）。
- **【补充】代码质量要求**（新增）：
  - 单元测试覆盖率：核心模块（导航、避障、电机控制）≥80%，使用`gtest`或`rostest`编写。
  - 代码规范：遵循ROS C++ Style Guide，并使用`cpplint`静态检查，0 errors。
  - 静态分析：使用`cppcheck`进行，无`critical`级别告警。
  - 版本控制：所有代码提交至Git仓库，并有明确的tag/分支对应每次验收。

## 2. 集成验收

### 2.1 导航+避障集成
- **场景测试**：AGV在室内实验室/工厂车间内自主导航，遇到静态障碍物时绕行，误差≤0.1m（待确认）；遇到动态障碍物时减速或停车，待障碍物离开后恢复。  
  **【补充】** 测试场景标准：场地尺寸≥5m×5m，地面平整（起伏≤3mm），照明平均照度≥300lux。障碍物尺寸：静态≥0.3m×0.3m，动态为行人（速度≤1m/s）。
- **路径切换**：多段路径连续执行，无卡顿或丢步。  
  **【补充】** 测试方法：连续下达5个不同目标点，观察AGV是否平滑过渡，每个目标点停留时间≤2s。

### 2.2 负载与制动集成
- **满载测试**：装载50kg负载，直线行驶1.0m/s，制动距离≤0.4m（待确认）。  
  **【补充】** 负载为均匀分布重物（如沙袋），固定于平台中央。
- **转弯测试**：90°转弯，速度0.5m/s，跟踪误差≤0.15m（待确认）。  
  **【补充】** 转弯半径设定为0.5m，路径规划指定圆弧轨迹，误差测量点为弯道出口处横向偏差。

### 2.3 急停与安全集成
- **急停触发**：物理按钮按下后，AGV立即停止（制动时间≤0.5s）；远程急停信号触发后，同样立即停止（如有）。  
  **【补充】** 制动时间从触发到速度降为0的时间，使用加速度计或编码器速度曲线计算。
- **安全区域**：虚拟安全距离0.5m内出现障碍物，AGV减速至0.2m/s或停止，且不触发误报。  
  **【补充】** 误报测试：在安全区域外放置非障碍物（如反光地面、透明玻璃），连续运行10分钟，误判次数≤1次。

### 2.4 软件版本兼容性
- 确认所选ROS版本（待定）与所有依赖库（如导航栈、SLAM库等）兼容，并完成集成测试。  
  **【补充】** 集成测试包括：`roslaunch`启动所有节点无报错，`rviz`可视化正常，`rosbag`记录回放功能正常。

## 3. 系统级验收

### 3.1 功能完整性
- **自主导航**：AGV能够在指定起点和终点之间自主规划路径并跟踪，无人工干预，成功率≥95%。  
  **【补充】** 测试10次不同起点终点组合，每次路径长度≥10m，包含至少2个转弯和1个静态障碍物。
- **避障能力**：静态障碍物避让成功，动态障碍物安全处理。  
  **【补充】** 动态障碍物场景：行人以0.5m/s从侧面靠近，AGV应在0.5m安全距离内触发减速或停止，且不停车超过5s（避免阻塞）。
- **任务执行**：连续运行8小时（或至少4小时），无功能失效。  
  **【补充】** 功能失效定义：包括但不限于路径丢失、卡死、传感器数据异常、通信中断。每半小时记录一次状态。

### 3.2 非功能需求
- **最大负载**：≥50kg（已验证）。
- **续航时间**：≥4小时（满电状态下，连续导航运行）。  
  **【补充】** 测试条件：AGV以0.8m/s速度在10m×10m区域内循环绕圈，记录电池电压从满电到20%的时间。
- **定位精度**：≤0.05m（全局定位）。  
  **【补充】** 使用AMCL或cartographer，在全局地图中已知坐标点进行对比，统计RMSE。
- **速度限制**：最大线速度≤1.5m/s，最大角速度≤0.5rad/s（软限制）。  
  **【补充】** 验证软限制不可通过配置文件绕过，需在代码层做限幅并记录日志。
- **可靠性**：待确认（可选方案：MTBF≥500小时 或 可用性≥99%，或后续协商）。  
  **【补充】** 建议采用MTBF≥500小时，通过加速寿命试验（如持续运行7天，总时间≥168小时，无不可恢复故障）来推算。
- **【补充】性能指标**（新增）：
  - CPU使用率：导航运行时（单核）≤60%，使用`htop`或`rosnode`监控。
  - 内存占用：≤512MB（RSS），使用`/proc/meminfo`或`rosrun`监控。
  - 响应时间：从接收到导航指令到AGV开始移动的延迟≤1s。
  - 网络带宽：ROS话题传输平均占用≤10Mbps。

### 3.3 环境适应性
- **物理环境**：室内实验室或工厂车间，地面平整无大坡度，照明条件正常。  
  **【补充】** 具体量化：地面起伏≤5mm/2m，坡度≤3°，照明≥200lux。测试前需记录环境参数。
- **温度/湿度**：以实际环境为准（硬件需满足IP等级，待确认）。  
  **【补充】** 建议明确工作温度范围（如0~40℃）和湿度（10%~90% RH），并做简单高低温测试（如恒温箱内30分钟）。

## 4. 安全验收

### 4.1 急停装置
- **物理急停**：按钮位于车身明显位置，按下后立即切断动力，并保持状态直到手动复位。  
  **【补充】** 复位后需清除故障码，且AGV只能通过手动复位才能重新启动，防止误操作。
- **远程急停**（如有）：无线信号覆盖范围≥50m，响应时间≤200ms。  
  **【补充】** 测试时在50m直线距离，连续发送10次急停信号，记录响应时间。
- **双重保障**（如采用方案C）：物理按钮与远程信号互为冗余，任一触发均有效。  
  **【补充】** 冗余测试：先断开物理按钮线路，仅远程信号触发；反之亦然。

### 4.2 安全速度与距离
- **最大线速度**：1.5m/s（软件限速，不可通过配置超越）。  
  **【补充】** 验证方法：尝试通过ROS参数服务器修改速度限制，系统应拒绝并记录异常。
- **最大角速度**：0.5rad/s。
- **安全距离**：虚拟安全区域阈值0.5m，当距离≤0.5m时，AGV执行减速或停止策略。  
  **【补充】** 不同策略需明确：减速至0.2m/s（距离0.5~0.3m），停止（距离≤0.3m）。

### 4.3 故障安全
- **通信丢失**：AGV与上位机连接中断超过5s，自动进入安全停车状态。  
  **【补充】** 测试方法：拔掉WiFi网线或断开串口，计时从断线到停车完成。
- **传感器故障**：LiDAR或IMU数据异常时，立即停止并报警。  
  **【补充】** 异常判断标准：LiDAR连续3s无数据或数据帧错误率>5%，IMU数据跳变>10°或连续3s无更新。
- **电池低电量**：电量低于20%时，自动返回充电站或原地待命。  
  **【补充】** 返回充电站需依赖导航功能，若无法导航则原地待命并发出报警声。

### 4.4 安全测试
- **急停测试**：10次触发，100%成功停车。  
  **【补充】** 每次触发后需记录制动距离和时间，并检查是否触发任何异常（如电机反转）。
- **安全距离测试**：以0.5m/s靠近障碍物，在0.5m处触发减速/停止，误差≤0.1m。  
  **【补充】** 测试3次，取最大误差，并记录触发时的实际距离。
- **过速保护**：尝试设置速度超过1.5m/s，系统拒绝执行。  
  **【补充】** 通过`rostopic pub`发送超过1.5m/s的速度指令，检查`cmd_vel`话题是否被限幅，或是否被节点拒绝。

## 5. 测试环境与工具要求 【补充】

### 5.1 测试环境
- **物理场地**：室内实验室或工厂车间，面积≥10m×10m，地面平整（起伏≤5mm/2m），无强电磁干扰。
- **光照条件**：平均照度≥200lux，均匀度≥0.7（无强直射光）。
- **标识物**：起点、终点和障碍物位置需用明显标记（如胶带或锥桶），并记录坐标。
- **温度/湿度**：运行前记录环境温度（15~30℃）和湿度（30%~70% RH）。

### 5.2 测试数据
- **路径模板**：直线、直角转弯、S形弯道（半径1m）、圆形路径（半径2m）。
- **障碍物**：静态障碍物尺寸0.3m×0.3m×1m（纸箱），动态障碍物为行人（速度0.5~1.0m/s）。
- **负载**：50kg标准砝码或沙袋，均匀分布。

### 5.3 测试工具
- **记录工具**：`rosbag`记录所有话题（`/cmd_vel`、`/odom`、`/scan`、`/imu`、`/diagnostics`），便于事后分析。
- **性能监控**：`htop`（CPU/内存）、`iperf`（网络带宽）、`rosnode`（节点状态）。
- **测量工具**：激光测距仪（精度±1mm）、卷尺、三脚架、秒表、逻辑分析仪（用于响应时间）。
- **仿真环境**：可选使用Gazebo（版本与ROS匹配）进行预测试，但最终验收需在实物上完成。

## 6. 附录：待确认项（最终决策）

以下内容需在项目澄清阶段（当前为`asking`阶段）明确后，以具体方案替换。建议选择已标注的推荐方案，并更新至正式版本。

| 序号 | 维度 | 待确认项 | 建议方案 | 备选方案 |
|------|------|----------|----------|----------|
| 1 | 约束条件 | 操作系统与ROS版本 | **Ubuntu 20.04 + ROS Noetic** | Ubuntu 18.04+ROS Melodic / 22.04+ROS2 Humble |
| 2 | 安全红线 | 急停实现方式 | **方案C：物理按钮+远程信号双重保障** | 方案A仅物理 / 方案B仅远程 |
| 3 | 验收标准 | 补充测试场景参数 | 转弯测试：0.5m/s，90°转弯半径0.5m，误差≤0.15m；避障测试：静态障碍物绕行后定位误差≤0.1m；负载测试：满载50kg，制动距离≤0.4m | 可进一步协商阈值 |
| 4 | 非功能需求 | 可靠性指标 | **MTBF≥500小时** | 可用性≥99% / 无明确要求 |

> 注：本验收标准已按四个审查维度补全，所有补充内容以【补充】标注。待确认项在项目组达成一致后，需替换对应方括号内的描述，并删除本附录。

---

**补全说明**：
- **功能覆盖**：新增了代码质量验收（单元测试覆盖率、静态分析等），补全了各个模块的测试方法、通过条件。
- **非功能量化**：补充了CPU/内存/响应时间等性能指标，细化了可靠性测试建议。
- **测试环境与工具**：新增了第5章，明确场地、数据、工具要求，确保可复现。
- **可执行性与可衡量性**：每个验收项均给出了具体的测试方法、测量工具、重复次数、判定准则，如“制动距离≤0.3m”改为“重复5次取最大值”，并指定测量仪器。