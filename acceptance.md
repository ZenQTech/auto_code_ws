好的，遵照您的要求，我对现有验收标准进行了全面审查，并基于需求文档V2.0补充了遗漏的维度与细节。以下为补全后的验收标准，所有补充内容均以 **【补充】** 标注，同时在末尾新增了“可维护性验收”与“可用性验收”两个独立章节。

---

# 验收标准.md（补全版）

## 1. 文档信息

| 项目 | 内容 |
| :--- | :--- |
| **文档名称** | 仿真环境AGV调度系统验收标准 |
| **文档版本** | V1.1（补全版） |
| **基于需求** | 需求文档 V2.0 |
| **制定日期** | 2023-XX-XX |
| **制定人** | 质量保证团队 |

## 2. 验收概述

### 2.1 验收范围
本验收标准旨在全面验证“仿真环境AGV调度系统”是否满足《需求文档 V2.0》中定义的功能、性能、安全、可靠性、可维护性及可用性要求。验收范围覆盖所有软件模块、核心算法、接口、系统级行为及非功能属性。

### 2.2 验收原则
- **全覆盖**：所有功能点、非功能指标及异常场景均需有对应的测试用例。
- **可量化**：性能及可靠性指标需有明确的测试方法和数据支撑。
- **可复现**：所有测试用例需在标准化的仿真环境（Docker镜像）中稳定复现。
- **自动化优先**：鼓励对核心功能、性能及回归测试进行自动化，以支持持续集成。

### 2.3 验收环境
- **硬件**：开发主机（CPU: 8核，RAM: 16GB，GPU: 可选）。
- **软件**：基于提供的 \`Dockerfile\` 构建的镜像，包含 Ubuntu 20.04 + ROS Noetic + Gazebo Ignition Garden。
- **仿真模型**：3台标准AGV模型（含激光雷达、IMU、编码器），工作区域为已定义的多边形地图。
- **【补充】验证传感器配置**：检查AGV模型是否包含需求规定的激光雷达（2D/3D）、超声波、IMU、编码器，且传感器数据可通过ROS Topic正常获取。

## 3. 模块级验收

### 3.1 任务管理模块
| 验收项 | 验收标准 | 测试方法 |
| :--- | :--- | :--- |
| **任务导入** | 支持从JSON/CSV格式文件导入任务列表 | 提供标准格式的JSON和CSV文件，系统能正确解析并生成任务列表。 |
| **【补充】输入校验** | 对任务输入进行格式校验和边界检查，防止注入攻击 | 提交包含特殊字符（如 \`' or 1=1--\`）、超大数值、缺少必填字段的任务文件，系统应返回明确错误（如400 Bad Request）且不崩溃。 |
| **任务分配算法** | 1. 采用最少负载调度算法。 <br>2. 负载评估函数包含任务队列长度、剩余电量、距离目标点距离，权重可通过配置文件调整。 | 1. 提交多个任务，验证系统是否根据负载均衡原则（如任务队列最短或电量最高）分配任务。 <br>2. 修改配置文件中的权重，验证分配结果随之变化。 |
| **任务依赖处理** | 1. 提交无环依赖的DAG任务，系统能生成合法拓扑排序并执行。 <br>2. 提交有环依赖的任务，系统拒绝提交并返回清晰错误提示，包含具体冲突节点。 <br>**【补充】3. 验证生成的执行序列与拓扑排序结果一致** | 1. 定义任务A（前置）->任务B（后置），提交后，验证任务B在A完成后才被分配。 <br>2. 提交任务A->B->C->A的循环依赖，验证系统拒绝并提示错误。 <br>3. 记录拓扑排序输出，与任务实际执行顺序比对，确认无偏差。 |
| **任务状态跟踪** | 任务状态流转正确：待分配 -> 执行中 -> 完成 / 失败，并提供查询接口。 | 1. 提交一个新任务，状态应为“待分配”。 <br>2. 任务分配后，状态更新为“执行中”。 <br>3. 任务执行完成后，状态更新为“完成”。 <br>4. 通过API查询任何任务的状态，返回结果正确。 |

### 3.2 路径规划模块
| 验收项 | 验收标准 | 测试方法 |
| :--- | :--- | :--- |
| **三级执行策略路由算法** | 1. **一级（全局）**：使用A*算法在静态地图上生成最优路径，计算时间≤0.5秒。 <br>**【补充】2. 验证全局路径为最优（距离最短或时间最短，根据配置）** <br>3. **二级（局部）**：当检测到动态障碍物或交通拥堵时，使用Dijkstra算法重新规划局部路径，计算时间≤0.2秒。 <br>4. **三级（避障）**：利用传感器数据实现实时避障，平滑调整路径点。 | 1. 设置静态地图，下达任务，测量路径规划时间。 <br>**【补充】2. 在无动态障碍物时，对比A*规划路径与已知最短路径长度，误差应＜5%** <br>3. 在AGV行驶路径上放置动态障碍物，观察是否触发局部重规划，并测量时间。 <br>4. 模拟传感器数据，验证AGV能在接近障碍物时平滑绕行，避免碰撞。 |
| **多AGV协同** | 多AGV在相同或交叉路径上运行时，不会发生死锁和路径冲突。 | 安排两辆AGV相向而行，验证系统能通过协商或重规划避免正面冲突和死锁。 |

### 3.3 交通管理模块
| 验收项 | 验收标准 | 测试方法 |
| :--- | :--- | :--- |
| **路口协商** | 在交叉路口，系统根据任务紧急程度和距离决定通行顺序，优先级高的AGV先行。 | 安排两辆AGV同时接近一个交叉路口，分别设置不同优先级，观察优先级高的AGV优先通过。 |
| **防碰撞机制** | 1. AGV间距离<0.3m时，触发减速。 <br>2. AGV间距离<0.1m时，触发急停。 | 1. 让两辆AGV靠近，当距离达到0.3m时，检查速度曲线是否下降。 <br>2. 继续靠近，当距离达到0.1m时，检查AGV是否立即停止。 |
| **死锁检测与解除** | 1. 系统能通过图论检测到循环等待（死锁）状态。 <br>2. 检测到死锁后，能自动重分配任务或重规划路径，解除死锁。 | 安排三辆AGV形成循环等待的路径，验证系统能检测到死锁并自动解除（例如，让其中一辆AGV让路或重新规划路径）。 |

### 3.4 通信与状态监控模块
| 验收项 | 验收标准 | 测试方法 |
| :--- | :--- | :--- |
| **通信协议与频率** | AGV与调度中心通过ROS 2 (或ROS 1 Noetic) Topic/Service通信，状态更新频率≥10Hz。 | 监听ROS Topic，验证AGV位置、状态等信息的发布频率≥10Hz。 |
| **Web管理界面** | 提供基于React + FastAPI的Web界面，实时显示AGV位置、任务进度、系统状态。 | 模拟运行，在Web界面上观察AGV位置更新（刷新频率≥1Hz）、任务进度条变化、系统状态指示（如正常、告警、故障）。 |
| **通信故障检测** | 若连续500ms（可配置）未收到AGV心跳，判定为通信中断，并触发急停。 <br>**【补充】验证超时时间可配置** | 1. 模拟中断AGV的心跳发布，验证系统在500ms后发出告警并触发急停。 <br>2. 修改配置文件中的超时时间（如改为1000ms），重复测试，验证急停触发时间随之变化。 <br>3. 恢复心跳，验证AGV能自动恢复（或通过人工确认恢复）。 |

### 3.5 安全与异常处理模块
| 验收项 | 验收标准 | 测试方法 |
| :--- | :--- | :--- |
| **急停触发** | 1. 障碍物距离<0.1m（可配置）。 <br>2. 通信超时>500ms（可配置）。 <br>3. AGV超出工作区域边界。 <br>4. 软件/硬件致命错误。 | 分别模拟以上四种场景，验证AGV均能触发急停并记录日志。 <br>**【补充】验证障碍物距离阈值可配置**：修改配置文件中的阈值（如0.2m），重复测试，验证急停触发距离变化。 |
| **边界保护** | AGV位置超出工作区域多边形边界时，发送停止指令并记录日志。 | 1. 手动控制AGV移出边界，验证AGV立即停止。 <br>2. 检查日志中是否记录了“越界”事件。 |
| **速度限制** | 线速度≤1.0 m/s，角速度≤0.5 rad/s（可配置）。 | 修改配置文件中的速度限制，验证AGV实际运行速度不超过设定值。 |
| **降级运行** | 若核心算法（如路由算法）模块不可用，系统能回退至简单轮询调度，并发出告警。 | 模拟核心算法模块崩溃，验证系统能切换到轮询模式并输出告警日志，且AGV仍能响应基本调度指令。 |

### 3.6 【补充】扩展接口模块
| 验收项 | 验收标准 | 测试方法 |
| :--- | :--- | :--- |
| **AGV数量扩展** | 支持通过配置文件动态增加AGV（上限5台），无需重启系统 | 1. 系统运行时，修改配置文件将AGV数量从3台增至5台，观察新AGV成功注册并开始接收心跳。 <br>2. 向新AGV分配任务，验证其能正常执行。 |
| **第三方RESTful API集成** | 对外提供RESTful API（提交任务、查询状态），支持外部系统集成 | 1. 使用HTTP客户端（如curl）通过POST /tasks提交任务，验证任务被正确解析并加入调度队列。 <br>2. 通过GET /tasks/{id}查询任务状态，返回结果与内部状态一致。 <br>3. 验证API接口符合RESTful规范（如使用标准HTTP方法、状态码）。 |

## 4. 集成验收

### 4.1 模块间数据流
- **任务分配到路径规划**：任务分配后，\`任务管理模块\` 传递给 \`路径规划模块\` 的起点和终点信息正确。
- **路径规划到交通管理**：\`路径规划模块\` 生成的路径被 \`交通管理模块\` 正确解析，用于路口协商和防碰撞计算。
- **状态监控与Web界面**：\`通信模块\` 接收到的AGV状态数据，能实时、正确地更新到 \`Web管理界面\`。

### 4.2 通信接口集成
- **ROS 2/1 Topic/Service**：调度中心发出的路径指令、速度指令能被AGV接收并执行。
- **RESTful API集成**：通过外部RESTful API提交任务、查询状态，能与内部 \`任务管理模块\` 正确交互。

### 4.3 核心算法集成
- **最少负载调度 + 三级路由**：验证调度算法分配的任务，其路径规划能正确使用三级策略，并在交通管理中协同工作，实现无冲突运行。
- **拓扑排序 + 任务调度**：验证依赖任务的处理顺序与拓扑排序结果一致，且调度系统能正确处理。

## 5. 系统级验收

### 5.1 功能验收场景
- **场景一（并发任务与无冲突）**：3台AGV同时执行10个任务，连续运行72小时。 **验收标准**：无碰撞、无死锁，任务完成率≥99%。
- **场景二（动态扩展）**：系统运行时，通过配置文件将AGV数量从3台扩展至5台。 **验收标准**：新AGV能成功注册、分配任务，调度逻辑正常运行，无异常中断。
- **场景三（通信故障恢复）**：模拟通信中断超过500ms。 **验收标准**：所有AGV触发急停，并在恢复通信后，系统能自动恢复（或通过人工确认后恢复）任务执行。
- **场景四（紧急避障）**：在AGV路径上放置障碍物，距离<0.1m。 **验收标准**：AGV急停，障碍物移除后，系统自动重新规划路径并继续执行任务。
- **场景五（循环依赖检测）**：提交一个包含循环依赖的任务集。 **验收标准**：系统拒绝提交，并返回明确的错误提示，指出依赖关系中的冲突节点。

### 5.2 性能验收
- **调度响应时间**：在3台AGV、10个任务并发的负载下，从任务提交到调度分配完成，平均响应时间≤1.0秒。
- **路径规划计算时间**：全局路径规划平均时间≤0.5秒，局部重规划平均时间≤0.2秒。
- **系统吞吐量**：系统能同时处理≥50个待分配任务，任务调度延迟平均值≤2秒。
- **高并发稳定性**：5台AGV同时运行时，调度响应时间平均值≤2.0秒，系统无崩溃。

### 5.3 可靠性验收
- **连续运行**：系统在仿真环境中连续运行72小时，无崩溃、无死锁、无内存泄漏（通过 \`valgrind\` 或类似工具检查，内存增长不超过5%）。
- **【补充】MTBF验证**：通过长期运行记录（如连续运行1000小时或加速老化测试）推算MTBF≥1000小时，或提供设计文档证明冗余机制满足要求。
- **故障恢复**：模拟系统崩溃，重启后，基于SQLite数据库恢复，任务状态不丢失，且能在30秒内恢复服务。
- **数据持久化**：验证SQLite数据库的自动备份策略（每小时增量，每日全量）正确执行，备份文件完整可用。

## 6. 安全验收

### 6.1 API安全
- **认证授权**：所有外部RESTful API接口需通过JWT Token验证。未通过认证的请求（无Token或Token无效）返回 **401 Unauthorized** 错误。
- **角色权限**：管理员和操作员具有不同权限。普通用户无法访问只有管理员才有的接口（如系统配置修改、用户管理）。**【补充】具体测试：使用操作员Token调用管理员接口，应返回403 Forbidden**。
- **输入校验**：所有API接口的输入参数需进行格式校验和边界检查。提交包含特殊字符（如 \`' or 1=1--\`）或超大数值的请求，系统应返回 **400 Bad Request**，且不崩溃。

### 6.2 通信安全
- **加密传输**：ROS通信采用加密传输（仿真环境可选，但需提供加密模块）。若未启用，系统启动时应有明确告警。
- **心跳安全**：验证心跳包是唯一能触发“通信故障”判断的机制，恶意伪造的其他消息不能干扰系统状态。

### 6.3 工具链安全
- **依赖检查**：系统启动时，自动检查 \`cppcheck\`、\`clang-tidy\`、\`pylint\`、\`roslint\` 等工具是否可用。若缺失，跳过对应校验并记录告警日志，不阻塞主流程。

### 6.4 异常与边界安全
- **急停验证**：验证所有四个急停条件（障碍物、通信、越界、致命错误）均能触发急停，并记录日志。
- **【补充】输入校验集成**：验证任务输入、API输入等所有外部数据源均经过格式校验和边界检查，防止注入攻击（已在3.1和6.1中覆盖）。

## 7. 【新增】可维护性验收

| 验收项 | 验收标准 | 测试方法 |
| :--- | :--- | :--- |
| **模块化设计** | 代码结构采用分层设计（表现层、调度层、算法层、通信层、数据层），各模块职责清晰，接口定义明确。 | 通过代码审查，检查模块依赖关系是否符合分层架构，无循环依赖。 |
| **日志记录** | 所有关键操作（任务分配、路径规划、异常触发、系统启动/关闭）均记录详细日志，日志级别可配置（DEBUG/INFO/WARN/ERROR）。 | 1. 执行典型操作，检查日志文件是否包含对应事件。 <br>2. 修改配置文件中的日志级别，验证日志输出粒度变化。 |
| **配置管理** | 算法阈值（速度限制、刹车距离、复杂度评分阈值等）均可通过配置文件动态调整，无需重启系统。 | 1. 修改配置文件中的速度限制（如从1.0m/s改为0.5m/s），验证AGV实时响应新限制。 <br>2. 修改其他阈值（如防碰撞距离），验证行为变化。 |

## 8. 【新增】可用性验收

| 验收项 | 验收标准 | 测试方法 |
| :--- | :--- | :--- |
| **环境依赖检测** | 系统启动时自动检测环境依赖（ROS版本、仿真器版本、第三方工具等），不满足时输出明确错误提示，并退出或跳过。 | 1. 在缺少ROS或Gazebo的环境中启动系统，验证输出清晰错误信息（如“ROS Noetic未安装”）。 <br>2. 在缺少cppcheck的环境下启动，验证跳过工具检查并记录告警，主流程继续。 |
| **Docker镜像一致性** | 提供Docker镜像（Ubuntu 20.04 + ROS Noetic + Gazebo Ignition Garden），确保环境一致性。 | 1. 使用提供的Dockerfile构建镜像，在另一台主机上运行，验证系统功能正常。 <br>2. 检查Dockerfile中所有依赖库版本与需求文档一致。 |

---

**结束**：以上为补全后的验收标准，所有补充内容均已标注。请质量保障团队据此执行验收。