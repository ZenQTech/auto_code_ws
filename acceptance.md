以下为补全后的验收标准，所有新增或修改内容均以【补充】标注。文档结构保持原有层次，并补充了缺失的测试环境、测试数据、测试工具要求，以及关键安全、性能、可扩展性验收项。

---

# 智能仓储系统验收标准.md

**文档版本:** V2.0  
**创建日期:** 2023-10-27  
**审核人:** 测试负责人/架构师  
**批准人:** 项目经理  

---

## 1. 总则

### 1.1. 验收目标
本验收标准旨在确保“智能仓储系统”满足需求文档V2.0中定义的所有功能、性能、安全、可靠性及可维护性要求。验收过程将覆盖从模块级单元测试到系统级端到端验证，并对关键安全缺陷进行专项、深入的测试。

### 1.2. 验收范围
本验收标准涵盖以下核心模块：
- 货物入库与出库管理模块
- 货架盘点与库存实时监测模块
- 机器人自主导航与取放货模块
- 可视化监控与人工操作界面模块
- 安全校验与全局急停模块
- 调度算法引擎模块
- 数据库与后端服务模块

### 1.3. 验收通过标准
所有“P0 (关键)”级别的验收项必须100%通过。所有“P1 (重要)”级别验收项通过率不低于95%。所有“P2 (次要)”级别验收项通过率不低于90%。任何未通过的验收项必须记录缺陷，并经过修复后重新验证。

### 1.4. 【补充】测试环境要求
- **硬件/操作系统**：干净安装的 Ubuntu 20.04 LTS 虚拟机（4核CPU，8GB内存，50GB磁盘）。
- **软件栈**：Docker 20.10+，Docker Compose v2，Python 3.8+，Node.js 16+（用于Web前端）。
- **仿真环境**：提供 Gazebo 或类似仿真仓库地图（含货架、障碍物、机器人模型）。
- **物理环境**（可选）：真实仓库布局、真实机器人（如TurtleBot3）、二维码/RFID读写器、重量传感器。
- **网络**：所有组件在同一局域网内，支持WebSocket通信。

### 1.5. 【补充】测试数据与工具要求
- **测试数据**：
  - 10个标准二维码（QR码，内容为JSON格式的货物信息：ID、名称、重量、类型）。
  - 10个符合ISO 15693标准的RFID标签，对应货物信息。
  - 预定义任务列表（入库/出库/盘点各30条，含正常与异常场景）。
  - 故障注入脚本（模拟传感器离线、二维码破损、网络中断等）。
- **测试工具**：
  - `curl` / `httpie`：用于API测试。
  - `JMeter` 或 `Locust`：用于并发性能测试。
  - `pytest` + `pytest-asyncio`：用于单元测试和集成测试。
  - `pylint` (v2.17.0+)、`cppcheck` (v2.9+)、`clang-tidy` (Ubuntu 20.04自带)、`roslint` (ROS Noetic)：用于代码安全扫描验证。
  - `WebSocket` 客户端（如 `wscat`）：用于心跳信号验证。
  - `docker` 和 `docker-compose`：用于部署验证。

---

## 2. 模块级验收

| 验收项 ID | 所属模块 | 验收项描述 | 验收标准 | 测试方法 | 优先级 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **M-01** | 货物入库出库 | **二维码/RFID识别** | 系统能正确识别标准二维码和符合ISO 15693标准的RFID标签，并提取货物信息。 | 使用10个不同编码的二维码/RFID标签进行扫描测试。 | P0 | 识别成功率100%，信息提取无误。 |
| **M-02** | 货物入库出库 | **异常处理-识别失败** | 当二维码破损或RFID标签损坏时，系统应弹出人工录入界面，且提供重试按钮。 | 尝试扫描一个破损的二维码和一个无效的RFID标签。 | P1 | 页面弹出“识别失败”提示，提供人工录入和重试选项。 |
| **M-03** | 货物入库出库 | **异常处理-无可用存储位** | 当所有货架已满时，系统应提示用户“无可用存储位”，并触发“货架整理”任务（如存在）。 | 在系统状态为“库存满”时，尝试执行入库操作。 | P1 | 系统提示“无可用存储位”，并自动或手动触发“货架整理”任务（若该功能已实现）。 |
| **M-04** | 货架盘点 | **传感器数据更新** | 系统能正确读取并解析来自重量传感器和RFID读取器的数据，并更新库存。 | 在货架上放置/移除物品，观察系统库存变化。 | P0 | 库存变化与物理操作一致，更新延迟 < 2秒。 |
| **M-05** | 货架盘点 | **异常处理-传感器离线** | 当传感器离线或数据异常时，系统应标记该货架为“待人工核查”，并记录错误日志。 | 模拟断开传感器连接，观察系统界面和日志。 | P1 | 界面上该货架状态变为“异常-待人工核查”，后台日志记录错误详情。 |
| **M-06** | 机器人导航 | **路径规划与避障** | 机器人能根据任务指令，在地图上规划出一条避开所有静态障碍物的路径，并在遇到动态障碍物时重新规划路径（重新规划时间 < 2秒）。 | 在仿真环境中设置静态和动态障碍物，下发取货任务。 | P0 | 机器人成功规划路径，避开障碍物，并最终到达目标点。 |
| **M-07** | 机器人导航 | **异常处理-电量不足** | 机器人电量低于阈值（如15%）时，应自动返回充电站，并暂停非紧急任务。 | 模拟机器人电量下降至阈值以下。 | P1 | 机器人停止当前非紧急任务，切换至“低电量-返回充电”状态，并导航至充电站。 |
| **M-08** | 可视化监控 | **实时数据展示** | Web界面应实时展示机器人位置、状态、货架库存、任务进度等数据，刷新频率 < 1秒。 | 在仿真环境中执行任务，观察界面数据更新。 | P0 | 界面数据与仿真环境同步，延迟 < 1秒。 |
| **M-09** | 可视化监控 | **网络断开处理** | 当WebSocket连接断开时，界面应显示“离线”状态，并提供本地缓存（如有）的最近数据。 | 手动断开网络连接，观察界面状态。 | P1 | 界面显示“离线”状态，并保留断开前的数据快照。 |
| **M-10** | 算法引擎 | **任务复杂度评分** | 算法对输入任务的复杂度评分结果应可复现，与预期结果一致。 | 编写单元测试，输入已知复杂度的任务（如依赖图深度为3，子任务数量为5），验证评分结果。 | P0 | 单元测试通过，评分结果与预期一致。 |
| **M-11【补充】** | 调度算法引擎 | **拓扑排序循环依赖检测** | 当任务依赖图中存在循环依赖时，系统应检测并返回错误，阻止任务调度，并在日志中记录循环路径。 | 构造一个包含循环依赖的任务图（如 A→B→C→A），提交调度请求。 | P1 | 系统返回错误码400，提示“检测到循环依赖”，日志记录循环路径。 |
| **M-12【补充】** | 调度算法引擎 | **最少负载调度算法正确性** | 调度系统应优先将新任务分配给当前负载最轻的机器人（负载定义为未完成任务数+正在执行任务数）。 | 设置3个机器人，负载分别为2、3、1，下发新任务；检查分配结果。 | P0 | 新任务分配给负载为1的机器人。 |
| **M-13【补充】** | 安全校验模块 | **工具版本与安装源验证** | 安全校验模块使用的 `pylint`、`cppcheck`、`clang-tidy`、`roslint` 版本必须与 `requirements.txt` 和 `Dockerfile` 中指定的版本完全一致，安装源（apt/pip/二进制）也应一致。 | 运行 `pylint --version` 等命令，与 `requirements.txt` 记录对比；检查 `Dockerfile` 中安装命令。 | P0 | 版本号完全匹配，安装源一致。 |

---

## 3. 集成验收

| 验收项 ID | 集成场景 | 验收项描述 | 验收标准 | 测试方法 | 优先级 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **I-01** | 调度模块与机器人 | **任务分发与执行** | 调度系统通过WebSocket向机器人下发任务指令，机器人能正确接收并开始执行。 | 通过Web界面下发一个“取货”任务，观察机器人是否接收到指令并开始动作。 | P0 | 机器人在 1秒内 接收到指令并开始执行。 |
| **I-02** | 安全校验模块与代码执行 | **代码安全校验流程** | 用户上传的代码必须经过安全校验模块（pylint, cppcheck等）检查，未通过检查的代码无法执行。 | 上传一个包含已知安全漏洞的Python脚本（如`os.system()`），并尝试执行。 | P0 | 系统返回“安全校验失败”，并详细列出违规项，阻止代码执行。 |
| **I-03** | 全局急停模块与系统 | **急停触发与恢复** | 通过API端点、GUI按钮或自动熔断模式触发急停后，系统应停止所有子进程、清空任务队列，之后可通过管理员操作恢复。 | 1. 触发API急停；2. 触发GUI按钮急停；3. 连续多次提交失败代码（如5次），触发自动熔断。 | P0 | 所有场景下，系统进入“急停”状态，所有机器人停止，任务队列清空。恢复后，系统能正常接收新任务。 |
| **I-04** | 数据库与后端服务 | **高并发写入（PostgreSQL）** | 在50个并发任务同时更新状态时，使用PostgreSQL（通过asyncpg驱动）的数据库写入操作平均延迟 < 500ms，且无数据丢失或冲突。 | 使用Locust模拟50个并发任务，同时向数据库写入状态更新数据。 | P0 | 平均写入延迟 < 500ms，数据完整性检查通过，无死锁或超时异常。 |
| **I-05** | 状态更新模块 | **长时间任务状态同步** | 对于执行时间超过30秒的CLI子进程，系统每5秒通过WebSocket向客户端发送“执行中”心跳信号。 | 模拟一个执行时间为5分钟的CLI子进程，观察客户端是否每5秒收到一次状态更新。 | P0 | 客户端接收到的心跳间隔 < 5秒，无“卡死”状态。 |
| **I-06** | 系统与部署环境 | **Docker环境一致性** | 使用 `docker-compose up` 命令在干净的Ubuntu 20.04虚拟机中部署后，系统能正常运行，所有功能与开发环境一致。 | 在干净的Ubuntu 20.04虚拟机中，执行 `docker-compose up` 并运行所有功能测试用例。 | P0 | 部署成功，所有功能测试用例通过。 |
| **I-07【补充】** | 安全校验模块与代码执行 | **工具版本强制检查** | 如果安全校验工具版本与 `requirements.txt` 不符，系统应拒绝执行代码扫描，并输出警告。 | 修改 `Dockerfile` 安装一个不同版本的 `pylint`，然后尝试上传代码。 | P1 | 系统返回错误，提示“工具版本不匹配，请检查环境”。 |
| **I-08【补充】** | 自动熔断模块 | **熔断参数可配置** | 自动熔断的触发条件（连续失败次数N、CPU/内存使用率阈值）应可通过配置文件或环境变量动态调整，无需修改代码。 | 修改配置文件中的 `max_failures=3` 和 `cpu_threshold=90%`，然后验证熔断行为。 | P1 | 新配置生效，3次失败后触发急停；CPU超过90%时也触发急停。 |

---

## 4. 系统级验收

| 验收项 ID | 验收场景 | 验收项描述 | 验收标准 | 测试方法 | 优先级 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **S-01** | 端到端业务流 | **完整入库-存储-出库流程** | 执行一个完整的业务流：货物入库（识别、分配货架、机器人搬运）-> 存储 -> 盘点 -> 出库（订单触发、机器人取货、送至出库口）。 | 在仿真环境中，按流程操作，观察系统是否自动完成所有步骤。 | P0 | 流程100%自动化完成，系统状态与物理世界一致，无人工干预。 |
| **S-02** | 性能基准 | **调度系统响应时间** | 从任务下发到机器人开始执行，平均延迟 < 1秒。 | 使用JMeter模拟10个并发任务，测量从API调用到机器人开始执行的时间差。 | P0 | 平均延迟 < 1秒，P99延迟 < 2秒。 |
| **S-03** | 可靠性 | **任务执行成功率** | 在连续执行100次任务（包括入库、出库、盘点）后，机器人执行任务成功率 > 99.5%。 | 随机生成100次任务，并记录成功/失败次数。 | P0 | 成功次数 >= 99次。 |
| **S-04** | 耐用性 | **长时间运行稳定性** | 系统在连续运行48小时后，无内存泄漏、关键进程崩溃或性能退化。 | 运行系统48小时，监控CPU、内存、数据库连接数等指标，并执行日常任务。 | P1 | 各项指标稳定，无异常增长或崩溃。 |
| **S-05** | 一键部署 | **脚本与Docker部署** | 提供的 `setup.sh` 脚本或 `docker-compose.yml` 文件能一键构建和启动系统，无需手动配置环境。 | 在干净的Ubuntu 20.04虚拟机中，执行 `git clone` 后运行 `setup.sh` 或 `docker-compose up`。 | P0 | 系统自动安装所有依赖，启动成功，可使用。 |
| **S-06** | 算法验收 | **调度算法决策记录** | 系统日志中应记录每个任务的调度决策，包括使用的算法（如“最少负载调度”）、分配的目标机器人、规划路径等。 | 执行一个任务，检查系统日志。 | P1 | 日志中包含任务ID、算法名称、分配机器人、路径关键点等字段。 |
| **S-07【补充】** | 可扩展性 | **算法可插拔** | 系统应支持通过配置文件或环境变量切换核心调度算法（如从“最少负载调度”切换为“轮询调度”），切换后调度行为相应改变。 | 修改配置文件 `algorithm=round_robin`，重启服务，然后执行任务分配。 | P1 | 新算法生效，任务按轮询方式分配给机器人。 |
| **S-08【补充】** | 可维护性 | **日志格式统一** | 所有日志条目必须包含时间戳（ISO 8601格式）、模块名、日志级别（DEBUG/INFO/WARNING/ERROR/CRITICAL）、详细描述，且日志文件可滚动。 | 检查各模块日志输出，使用 `grep` 验证格式。 | P1 | 所有日志符合格式要求，无缺失字段。 |
| **S-09【补充】** | 异常分级 | **致命异常自动急停** | 当系统检测到致命异常（如内存溢出、核心进程崩溃）时，应自动触发全局急停，并记录日志。 | 模拟内存泄漏触发致命异常，观察系统行为。 | P0 | 系统进入急停状态，日志记录“FATAL: 内存溢出，触发全局急停”。 |

---

## 5. 安全验收 (专项)

| 验收项 ID | 安全维度 | 验收项描述 | 验收标准 | 测试方法 | 优先级 | 预期结果 |
| :--- | :--- | :--- | :--- | :--- | :--- | :--- |
| **A-01** | 认证与授权 | **API访问控制** | 所有API请求（除 `/auth/login` 外）必须通过JWT Token验证。无Token或Token无效的请求应被拒绝，并返回401状态码。 | 使用 `curl` 发送一个无Token的GET请求到 `/api/tasks`，以及一个带过期Token的请求。 | P0 | 两次请求均返回HTTP 401。 |
| **A-02** | 认证与授权 | **角色权限控制** | 管理员、操作员、访客角色应有明确的权限边界。例如，访客无法执行“紧急停止”操作。 | 1. 使用管理员Token访问 `/api/system/emergency_stop`，应成功。<br>2. 使用访客Token访问同一端点，应被拒绝。 | P0 | 管理员成功，访客返回403。 |
| **A-03【补全】** | 代码安全 | **静态安全扫描** | 集成 `pylint==2.17.0`、`cppcheck==2.9`、`clang-tidy`（Ubuntu 20.04自带版本）、`roslint`（ROS Noetic 版本），并确保它们安装自 `requirements.txt` 和 `Dockerfile` 指定的源（apt/pip/二进制）。 | 运行安全扫描功能，验证工具调用成功，并输出结果。 | P0 | 扫描正常执行，无错误。 |
| **A-04【补充】** | 代码安全 | **安全扫描结果反馈** | 安全扫描失败时，系统应返回详细的违规项列表（包括行号、错误类型、严重级别），并阻止代码执行。 | 上传包含 `eval()` 的Python脚本，检查返回信息。 | P0 | 返回JSON包含 `errors` 列表，每项有 `line`, `severity`, `message`。 |
| **A-05【补充】** | 自动熔断 | **连续失败次数熔断** | 当连续N次安全校验失败（N可配置，默认5次），系统应自动进入全局急停状态。 | 连续提交5个包含安全漏洞的代码，观察系统状态。 | P0 | 第5次提交后，系统进入急停，并记录“自动熔断触发”。 |
| **A-06【补充】** | 自动熔断 | **资源阈值熔断** | 当CPU或内存使用率超过阈值（可配置，默认85%）时，系统应自动触发急停。 | 使用压力工具（如 `stress`）使CPU使用率超过85%，观察系统状态。 | P0 | 系统进入急停状态，日志记录“CPU负载过高，触发自动熔断”。 |
| **A-07【补充】** | 环境隔离 | **Docker容器隔离** | 运行在Docker容器中的服务无法访问宿主机上的敏感文件（如 `/etc/shadow`），且容器之间的网络隔离符合配置。 | 在容器内尝试 `cat /etc/shadow`，并尝试访问其他容器的未暴露端口。 | P0 | 访问被拒绝，返回“Permission denied”或连接超时。 |
| **A-08【补充】** | 异常分级 | **严重异常通知管理员** | 当系统发生严重异常（如传感器离线、机器人通信超时）时，系统应记录详细日志并通过Web界面或邮件通知管理员，提供人工干预入口（如确认/取消任务）。 | 模拟传感器离线，检查系统日志和界面通知。 | P1 | 日志记录“ERROR: 传感器离线”，界面显示“异常通知”，并显示“确认”按钮。 |

---

**文档结束**