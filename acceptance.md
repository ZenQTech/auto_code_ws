# 验收标准.md（补全版）

## 1. 概述

本文档基于《多AGV协同调度与路径规划系统 需求文档 V2.0》及架构批判分析结果，制定系统验收标准。验收范围覆盖模块级、集成级、系统级及安全维度，确保系统满足功能、性能、可靠性及安全要求。所有测试需在典型仓库环境（含交叉口、窄通道、L形弯道）中，使用10台差速轮式AGV（ROS1 Melodic/Noetic）进行，并部署Web管理界面。

**【补充】测试环境基本参数：**  
- 仓库地图尺寸：40m×30m，含3个交叉口、2个窄通道（宽度0.8m）、4个L形弯道，静态障碍物覆盖率≤15%。  
- AGV模型：差速轮式，激光雷达（2D，角度270°，最大探测距离10m），IMU，Wi-Fi模块（2.4GHz/5GHz）。  
- 调度服务器：Ubuntu 20.04，Python 3.8，FastAPI，SQLite（WAL模式），systemd管理。  
- 测试工具：Locust（API压测）、wscat（WebSocket延迟测试）、高速相机（1000fps）、Wireshark（网络抓包）、sqlmap（SQL注入扫描）、自研自动化测试脚本（Python）。

---

## 2. 模块级验收

### 2.1 基础调度与导航模块

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 全局路径规划（A*） | 1000次任务下发，95%分位路径规划响应时间≤1秒 | 批量调用REST API，记录响应时间 |
| 局部轨迹规划（DWA） | 动态障碍物（行人模型）前，AGV在0.3秒内生成避让轨迹 | 在AGV前方1m随机放置障碍物，观察轨迹生成时间 |
| 最少负载调度算法 | 算法正确计算综合评分（任务数、电量、距离），分配最优AGV | 构造不同负载场景，验证分配结果与预期评分一致 |
| 三级执行策略路由 | 任务复杂度评分后，自动选择直接执行/局部重规划/全局重规划+Agent Team | 测试三类典型任务（简单直道、中等弯道多障碍、复杂死锁） |
| 拓扑排序循环依赖检测 | 人为构造循环等待场景，系统在1秒内检测并调整任务顺序，避免死锁 | 创建3台AGV互相等待的路径，检测死锁解除时间 |

**【补充】最少负载调度算法详细验证：**  
- 验收标准：算法按权重（任务数占40%、电量占30%、距离占30%）计算综合评分，分配评分最高的AGV。构造3个场景：  
  1. AGV1任务数0、电量80%、距离10m；AGV2任务数1、电量90%、距离15m → 预期AGV1被选中。  
  2. 所有AGV任务数相同，电量不同 → 预期电量最高的AGV被选中。  
  3. 任务数、电量相同，距离不同 → 预期距离最近的AGV被选中。  
- 测试方法：通过API发送任务，记录调度器返回的AGV ID，与预期对比，100次测试准确率100%。

**【补充】三级执行策略路由通过条件：**  
- 验收标准：三类任务在8小时连续运行中，均按策略正确执行，无死锁、无任务失败。  
  - 简单任务（直线距离≤10m，无动态障碍）→ 直接执行，路径规划时间≤0.5s。  
  - 中等任务（含2个弯道、1个动态障碍）→ 局部重规划，轨迹更新频率≥5Hz。  
  - 复杂任务（含3台AGV同时申请同一交叉口，存在潜在死锁）→ 全局重规划+Agent Team协作，在3秒内完成重规划，任务完成率100%。  
- 测试方法：分别构造10个简单、10个中等、10个复杂任务，记录执行模式日志和任务结果。

**【补充】拓扑排序循环依赖检测后续行为验证：**  
- 验收标准：检测到死锁后，系统在1秒内输出调整后的任务顺序，且调整后任务队列在执行时无死锁。  
- 测试方法：构造3台AGV的循环等待任务（如A等待B释放资源，B等待C，C等待A），触发调度，记录检测时间、调整后的任务顺序，并执行该顺序验证无死锁。

### 2.2 交通管制与防撞模块

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 区域互斥（交叉口/窄通道） | 同一时间仅允许一台AGV进入互斥区域，无冲突 | 两台AGV同时申请同一交叉口，监控互斥锁状态 |
| 动态避障（其他AGV） | 两AGV对向行驶时，最小距离≥0.3m，无碰撞 | 记录AGV间距离，连续运行8小时 |
| 电子围栏 | AGV超出边界后0.2秒内停止，并上报事件 | 在地图边界外设置虚拟障碍，测量停止时间 |

**【补充】电子围栏测试细化：**  
- 测试方法：利用测试脚本发送命令，使AGV朝边界外直线运动，同时用高速相机记录从AGV进入违禁区到完全停止的时间。共测试10次，平均≤0.2秒，最大≤0.25秒。  
- 上报事件验证：停止后检查Web界面状态日志和/health接口，确认事件记录包含“电子围栏触发”和AGV ID。

### 2.3 任务管理模块

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 任务创建/取消 | 通过Web界面和API创建、取消任务，状态实时更新 | 执行100次创建/取消操作，验证状态一致性 |
| WebSocket状态推送 | 任务状态变更（已分配/执行中/完成/失败/取消）延迟≤1秒（99%分位） | 1000次推送测试，记录延迟 |
| 异常任务重分配 | AGV故障后，其未完成任务在5秒内自动重新分配给其他AGV | 模拟单台AGV断网，测量任务重新分配时间 |

**【补充】WebSocket状态推送测试工具：**  
- 测试方法：使用wscat连接WebSocket端点，通过API触发任务状态变更，记录客户端收到消息的时间戳与服务器发送时间戳的差值。共1000次，记录99%分位值≤1秒，最大延迟≤2秒。

**【补充】异常任务重分配测试细化：**  
- 测试方法：创建5个任务分配给一台AGV，然后手动断开该AGV的Wi-Fi连接（或关闭ROS节点），记录从断网到任务状态变为“已重新分配”的时间（查看日志时间戳）。共测试10次，平均≤5秒，最大≤6秒。同时验证任务数据完整（无丢失）。

### 2.4 安全与紧急停止模块

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 物理急停按钮 | 按下后AGV在0.1秒内切断动力并刹车 | 使用高速相机记录急停时间（10次测试） |
| 软件紧急停止（Web界面） | 点击“紧急停止”后，所有AGV在0.5秒内收到指令并停止 | 广播指令，测量最后一台AGV响应时间 |
| 紧急停止后任务状态 | 所有当前任务终止，AGV进入待命状态 | 检查任务日志和AGV状态 |
| 认证授权 | 未登录用户访问Web界面重定向至登录页，未授权API调用返回401 | 使用curl测试无Token请求，使用不同角色验证权限 |

**【补充】软件紧急停止广播延迟测试细化：**  
- 测试方法：通过Wireshark抓包，记录Web界面点击“紧急停止”时间戳（前端记录）与所有AGV最后一条停止指令确认报文的时间戳，取最大值。共测试10次，平均≤0.5秒，最大≤0.6秒。  
- 安全冗余验证：同时模拟Wi-Fi故障，确认独立硬线（GPIO）仍能触发停止（可使用测试脚本模拟GPIO信号）。

### 2.5 系统监控与数据持久化模块

| 验收项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 实时状态监控 | Web界面显示所有AGV位置、速度、电量、任务状态、网络连接状态，刷新延迟≤1秒 | 观察页面数据与AGV实际数据的一致性 |
| 数据备份与恢复 | 调度数据（任务记录、轨迹、日志）实时写入本地数据库，备份文件可恢复，恢复时间≤2分钟 | 模拟数据库损坏，使用备份文件恢复 |
| 数据库高可用（WAL模式+重试） | 5个并发任务持续10分钟，写入失败率≤0.1%，写入失败自动重试最多3次，仍失败则切换备用数据库并告警 | 压力测试，查看日志中重试和切换记录 |
| 健康检查接口 | 提供`/health`端点，返回系统各组件状态（数据库、调度进程、WebSocket） | 调用接口，验证响应内容 |

**【补充】数据库高可用测试细化：**  
- 测试方法：  
  1. 使用5个并发线程同时向数据库写入任务记录（模拟真实场景），持续10分钟。  
  2. 在测试过程中，临时禁止主数据库文件写入（如`chmod 000`），观察系统是否自动重试（最多3次）并切换到备用数据库文件。  
  3. 恢复主数据库后，系统应自动切换回主库。  
  4. 验收标准：总写入失败率≤0.1%（即失败次数/总写入次数），日志中记录重试和切换事件，且告警信息通过WebSocket或API推送至前端。

**【补充】健康检查接口响应格式：**  
- 验收标准：`GET /health`返回JSON对象，包含`status`（整体“ok”或“degraded”）、`database`（“connected”/“error”）、`scheduler`（“running”/“stopped”）、`websocket`（“active”/“inactive”）、`last_backup_time`（最近备份时间戳）。  
- 测试方法：调用接口，验证响应字段符合预期，并模拟组件故障（如停止数据库），检查状态变化。

---

## 3. 集成验收

### 3.1 调度服务器与AGV通信集成

- **通信延迟**：Wi-Fi环境下，AGV与调度服务器之间的命令/状态消息往返延迟≤50ms（99%分位）。  
  **【补充】测试方法：** 使用Wireshark抓包，统计1000次命令-响应时间差，计算99%分位值。  
- **断线重连**：AGV断网后，恢复连接后5秒内自动重新注册并接收未完成的任务（若任务未超时）。  
  **【补充】测试方法：** 断开AGV Wi-Fi 10秒，然后恢复，记录从恢复连接至AGV重新出现在Web界面并收到任务的时间，共测试10次，平均≤5秒。

### 3.2 Web前端与后端API集成

- **API响应时间**：所有REST API（任务创建、查询、状态）99%分位响应时间≤500ms。  
  **【补充】测试工具：** 使用Locust进行100并发用户持续5分钟压测，记录响应时间分布。  
- **WebSocket稳定性**：连续运行8小时，WebSocket连接无意外断开，状态推送无中断。  
  **【补充】测试方法：** 使用自动化脚本连接WebSocket并持续监听，记录断开次数和重连行为。

### 3.3 算法模块集成（热插拔）

- **配置文件切换**：修改配置文件中的算法模块路径（如`a_star`替换为`jps_plus`），重启调度服务后，路径规划算法自动切换，无需修改代码。  
  **【补充】验收标准：** 切换后执行相同的10个任务，路径规划结果（路径长度、节点数）与预期一致（JPS+应比A*更少节点）。  
- **模块兼容性**：各算法模块输入输出接口符合预定义协议，集成后系统功能正常。  
  **【补充】测试方法：** 分别切换路径规划、任务分配、死锁检测三个模块为备用实现（如A*↔JPS+、最少负载↔轮询、拓扑排序↔银行家算法），运行8小时连续测试，确保无接口错误。

---

## 4. 系统级验收

### 4.1 功能完整性验收

- **连续运行测试**：10台AGV同时运行，执行随机任务（含交叉口、窄通道、L形弯道），连续8小时无碰撞、无死锁、无任务丢失，任务完成率≥99%。  
  **【补充】统计方法：** 记录总任务数（≥1000个）、完成数、失败数（包括超时、碰撞、未达目标），计算完成率；碰撞检测通过AGV间距离传感器日志和激光雷达数据判断最小距离是否≥0.3m。  
- **强制停止测试**：在运行过程中按下物理急停按钮（5次），每次AGV在0.1秒内停止；通过Web界面点击紧急停止（5次），所有AGV在0.5秒内停止。

### 4.2 性能验收

- **路径规划响应时间**：1000次任务下发，95%分位≤1秒，99%分位≤2秒。  
  **【补充】测试工具：** 使用Locust或自研脚本并发下发任务，记录响应时间。  
- **WebSocket推送延迟**：1000次推送，99%分位≤1秒，最大延迟≤2秒。  
  **【补充】测试工具：** wscat+时间戳比对。  
- **数据库并发写入**：5个并发任务持续10分钟，无写入失败，日志中无重试记录。  
  **【补充】测试方法：** 同时模拟5个任务创建、状态更新、日志写入，检查数据库无报错，且日志中无“retry”关键词。

### 4.3 可靠性验收

- **AGV故障接管**：模拟单台AGV断网/故障，其未完成任务在5秒内被其他AGV接管，期间无任务丢失。  
  **【补充】测试方法：** 创建10个任务分配给一台AGV，然后强制关闭该AGV的ROS节点，记录任务重新分配时间，并验证所有任务最终完成。  
- **调度服务器崩溃恢复**：模拟FastAPI进程崩溃，systemd/supervisor在10秒内自动重启进程，重启后任务数据不丢失，AGV重新连接后继续执行未完成的任务。  
  **【补充】测试方法：** 使用`kill -9`杀死调度进程，记录从崩溃到systemd重启成功（通过`/health`返回正常）的时间，共测试5次，平均≤10秒。  
- **数据库文件损坏恢复**：手动删除数据库文件，使用备份恢复，恢复时间≤2分钟，恢复后数据完整。  
  **【补充】测试方法：** 记录从执行恢复命令到数据库可正常查询的时间，并对比恢复前后任务记录数量一致性。

### 4.4 可维护性验收

- **日志保留**：系统日志（关键事件、异常、急停）保留≥30天，日志文件按日滚动，可通过`/logs`接口导出。  
  **【补充】测试方法：** 检查日志目录，确认日志文件命名规则（如`scheduler_YYYY-MM-DD.log`），修改系统时间模拟30日后，验证旧日志未被删除或可配置保留策略。  
- **健康检查**：`/health`接口返回正常，监控系统可定期调用确认系统连通性。  
  **【补充】测试方法：** 编写监控脚本每分钟调用一次，持续8小时，记录失败次数（应≤1次）。

---

## 5. 安全验收

### 5.1 紧急停止机制（关键安全缺陷修复）

- **物理急停**：每台AGV的物理急停按钮按下后，0.1秒内切断动力，AGV电机刹车，直至人工复位。  
- **软件紧急停止**：Web界面“紧急停止”按钮发送广播指令，所有AGV在0.5秒内停止运动并终止当前任务。调度服务器收到指令后，立即终止所有子进程（CLI/ROS节点），确保无残留进程。  
- **安全冗余**：紧急停止指令同时通过Wi-Fi和独立硬线（如GPIO）发送，防止单点故障。  
  **【补充】测试方法：** 在Wi-Fi断开的情况下，通过GPIO模拟硬线信号，确认AGV仍能停止。

### 5.2 认证与授权

- **JWT认证**：所有API请求（除`/login`）必须携带有效JWT Token，过期或无效Token返回401。  
- **角色权限**：管理员可执行所有操作（任务创建/取消、紧急停止、系统配置）；操作员仅可查看状态和创建任务，无法执行紧急停止和系统配置。  
- **会话安全**：JWT Token有效期≤24小时，支持刷新Token；密码使用bcrypt加密存储。  

**【补充】角色权限测试方法：**  
- 使用管理员Token调用紧急停止API → 成功（返回200）。  
- 使用操作员Token调用紧急停止API → 返回403。  
- 使用操作员Token调用系统配置API → 返回403。  
- 检查数据库密码字段，确认是bcrypt哈希（如`$2b$...`）。

### 5.3 数据安全与SQL注入防护

- **SQL注入防护**：所有数据库操作使用参数化查询（如`aiosqlite`的`?`占位符）或ORM，禁止拼接SQL字符串。验收测试使用SQL注入工具（如`sqlmap`）进行扫描，无漏洞。  
- **数据隔离**：不同会话（任务组）的数据通过逻辑隔离（会话ID）和物理隔离（若使用SQLite，每个会话独立数据库文件）相结合，防止一个会话的数据被另一个会话访问。  
- **备份加密**：数据库备份文件在传输和存储时进行AES-256加密，密钥由环境变量管理。  

**【补充】数据隔离测试方法：**  
- 同时登录两个不同会话（如两个管理员账号），分别创建任务，然后尝试通过API查询另一个会话的任务列表 → 应返回空或仅包含本会话的数据。  
- 若使用多数据库文件，检查文件权限，确保一个会话无法读取其他会话的数据库文件。

### 5.4 通信安全

- **WebSocket连接**：必须使用`wss://`（TLS加密），防止中间人攻击。  
- **API接口**：生产环境强制使用HTTPS，证书由受信任CA签发。  
- **AGV通信**：ROS Master节点与AGV之间的Topic通信使用TLS加密（ROS1 noetic支持），或通过VPN隧道。  

**【补充】测试方法：**  
- 使用wscat连接`ws://`（非加密）应被拒绝。  
- 使用curl访问`http://`应重定向到`https://`。  
- 检查ROS节点配置，确认TLS证书路径。

### 5.5 安全测试专项

| 测试项 | 验收标准 | 测试方法 |
|--------|----------|----------|
| 物理急停响应时间 | 10次测试，平均≤0.1秒，最大≤0.12秒 | 高速相机记录 |
| 软件紧急停止广播延迟 | 10次测试，平均≤0.5秒，最大≤0.6秒 | 网络抓包测量 |
| 未授权API访问 | 返回401，且无任何数据泄露 | 使用curl访问无Token端点 |
| SQL注入 | 无注入成功，系统日志记录所有可疑SQL尝试 | 使用自动化SQL注入工具扫描所有API |
| 会话数据隔离 | 一个会话无法读取另一个会话的数据 | 同时登录两个不同会话，尝试交叉访问API |
| 密码强度 | 密码最小长度8位，包含大小写字母、数字、特殊字符 | 检查密码策略；尝试注册弱密码应被拒绝 |

**【补充】密码强度测试细化：**  
- 测试方法：通过API注册用户，尝试以下密码：`abc123`（长度不足）、`12345678`（无字母）、`abcdefgh`（无数字和特殊字符）、`Abcd1234`（无特殊字符）。预期均返回400错误，提示密码不符合要求。  
- 检查数据库密码字段，确认bcrypt哈希长度≥60字符。

---

## 6. 验收结论

所有模块级、集成级、系统级及安全验收项均需达到100%通过率方可视为系统验收合格。对于关键安全缺陷（紧急停止、SQL注入防护、数据隔离），如未通过，则整体验收不通过，需整改后重新测试。验收过程需记录详细测试日志，归档至项目文档。

**【补充】验收报告要求：**  
- 每个验收项需记录测试时间、测试人员、测试结果、失败原因及整改记录。  
- 提供测试脚本、抓包文件、日志截图等证据。  
- 验收完成后，生成《系统验收报告》，由质量保障负责人签字存档。