好的，作为专业的质量保障与迭代管理智能体，我将严格遵循5阶段流转规则，从功能覆盖、非功能量化、环境工具、可执行性四个维度，对您提供的验收标准进行审查并补全。

---

### 审查结论

**总体评价：** 现有验收标准结构清晰，覆盖了大部分核心功能点，量化指标明确，具备良好的可执行性。但存在以下关键缺失：

1.  **功能点覆盖不足：**
    *   **安全红线验证缺失：** 需求文档3.3节明确提及的“AGV最大速度≤1.5m/s”、“急停触发条件：激光雷达碰撞检测（距离<0.5m）”、“电子围栏限制”等安全红线，在验收标准中无对应测试项。
    *   **子进程安全规范验证不完整：** 现有标准S-10仅检查“无子进程”，但未验证核心调度逻辑是否确实通过ROS 2节点实现，也未检查是否存在其他潜在的shell命令注入风险（如通过API调用系统命令）。
    *   **日志审计验证缺失：** 需求文档3.4节要求“所有关键操作记录审计日志，保留至少90天”，现有标准未覆盖。
    *   **通信加密验证缺失：** 需求文档3.3节要求WebSocket及HTTP API使用TLS 1.2+，现有标准未覆盖。
    *   **兼容性测试缺失：** 需求文档未明确，但作为Web应用，需要在主流浏览器上验证，现有标准未覆盖。

2.  **非功能需求量化不充分：**
    *   **“可用性”测试场景不明确：** S-06“系统可用性”的测试方法“通过监控工具统计”过于笼统。需要明确具体的故障注入场景（如数据库宕机、网络中断、后端服务崩溃）来验证HA切换和恢复时间。
    *   **“可扩展性”验证缺失：** 需求文档3.4节要求“后端服务可部署多实例，通过负载均衡分发WebSocket连接”，现有标准无对应测试项。

3.  **环境与工具要求不明确：**
    *   **测试环境配置缺失：** 未定义测试所需的硬件、软件、网络环境的具体配置，影响测试结果的可复现性。
    *   **测试工具未指定：** 测试方法中提及“使用Chrome Performance面板”、“使用压力测试工具”，但未明确具体工具名称及版本，影响测试执行的一致性。

4.  **可执行性细节不足：**
    *   **部分通过标准模糊：** 如M-03“所有元素可见，操作流畅”中的“流畅”缺乏量化标准（如帧率、操作响应延迟）。
    *   **测试数据准备缺失：** 未说明如何准备测试所需的AGV数据、地图数据、任务数据等。

### 补全后的验收标准

以下是补全后的验收标准，**新增内容以 `[补充]` 标注，修改内容以 `[修改]` 标注**。

---

# 验收标准.md（补全版）

## 1. 文档概述

本文档基于《需求文档 V2.0》定义的功能、非功能、安全及技术约束，制定完整的验收标准。验收范围覆盖模块级、集成级、系统级及安全级，确保系统满足多AGV协作调度与实时可视化平台的全部技术要求。

---

## 2. 测试环境与工具 [补充]

### 2.1 硬件环境 [补充]

| 组件 | 测试环境配置 | 说明 |
|------|--------------|------|
| 服务器 | 8核CPU / 16GB内存 / 500GB SSD | 用于部署后端服务、数据库、ROS 2桥接模块 |
| 客户端 | 4核CPU / 8GB内存 / 256GB SSD，显示器分辨率1920x1080 | 用于运行前端Web应用 |
| 网络 | 局域网千兆以太网，模拟100ms延迟、1%丢包率 | 用于测试网络异常场景 |
| AGV模拟器 | 部署于独立服务器，8核CPU / 16GB内存 | 用于模拟100台AGV的并发行为 |

### 2.2 软件环境 [补充]

| 类别 | 名称/版本 | 说明 |
|------|-----------|------|
| 操作系统 | Ubuntu 22.04 LTS (服务器), Windows 11 / macOS 14 (客户端) | 后端与前端测试环境 |
| 浏览器 | Chrome 最新版、Firefox 最新版、Edge 最新版 | 用于兼容性测试 |
| 后端框架 | ROS 2 Humble | 核心调度逻辑 |
| 数据库 | PostgreSQL 15 | 存储KPI数据、AGV配置、地图数据 |
| 桥接中间件 | rosbridge_suite (WebSocket) | 前后端通信 |
| 负载均衡器 | Nginx 1.24 | 用于测试水平扩展 |

### 2.3 测试工具 [补充]

| 工具名称 | 版本 | 用途 |
|----------|------|------|
| JMeter | 5.6.3 | 进行API压力测试、并发测试 |
| Lighthouse | 最新版 (Chrome内置) | 测量页面加载性能 |
| Wireshark | 4.0+ | 抓包分析WebSocket通信延迟、TLS握手 |
| Chrome Performance / DevTools | 最新版 | 前端性能分析（帧率、内存） |
| Postman | 最新版 | API接口测试与JWT验证 |
| 自定义AGV模拟器 | 内部开发 | 模拟AGV行为、里程计、故障注入 |

---

## 3. 模块级验收标准

### 3.1 核心调度与可视化模块

| 编号 | 测试项 | 验收条件 | 测试方法 | 通过标准 |
|------|--------|----------|----------|----------|
| M-01 | 多AGV实时可视化 | 实时展示所有AGV位置、状态(空闲/忙碌/故障/充电)、当前任务进度、运行轨迹 | 启动10台AGV模拟器，观察Web前端显示更新频率 ≥10Hz，状态变化在1秒内反映 | 所有AGV状态正确，轨迹无断裂，刷新率达标 |
| M-02 | 大规模AGV显示优化 | 当AGV数量≥100台时，自动切换为按区域分层或聚合显示，界面无卡顿 | 模拟100台AGV，检查前端帧率≥30fps，使用Chrome Performance面板 | 聚合显示无卡顿，点击可展开查看详情 |
| M-03 | 仓库地图展示 | 支持2D/2.5D地图，含电子围栏、货架、充电站、障碍物、路径节点；支持缩放、平移、点击查看详情 | 手动操作地图，检查所有静态元素渲染正确；缩放/平移平滑 | 所有元素可见，操作流畅，详情弹窗正常。**修改：定义“流畅”为：缩放/平移操作响应延迟≤100ms，帧率≥30fps** |
| M-04 | 地图数据更新平滑过渡 | 地图数据更新时无闪烁，过渡动画时间≤500ms | 动态更新地图数据（如新增货架），观察前端过渡效果 | 无明显闪烁或跳变 |
| M-05 | 地图加载失败处理 | 地图加载失败时显示默认占位图，并提示“地图加载失败，请重试” | 断开后端地图服务，观察前端显示 | 占位图出现，提示信息正确，重试按钮有效 |
| M-06 | AGV位置追踪精度 | 位置误差≤0.1m，朝向指示正确 | 在指定坐标点（如(2.5, 3.0)）放置AGV，对比前端显示坐标 | 偏差≤0.1m，朝向箭头方向正确 |
| M-07 | AGV离线检测 | 当AGV信号丢失＞3秒，将其标记为“离线”状态，并触发告警 | 停止AGV通信，观察前端状态变化及告警面板 | 3秒后状态变为离线，告警出现 |
| M-08 | 任务下发与分配 | 支持手动创建、批量导入、定时任务；任务自动分配给最合适的AGV | 创建10个任务，检查分配结果，确认同一AGV不会同时收到多个任务 | 任务分配正确，同一AGV队列有序，所有AGV忙碌时任务进入等待队列 |
| M-09 | 紧急停止响应 | 一键停止所有AGV或指定AGV，响应时间≤200ms | 使用高精度计时器（毫秒级）测量从点击按钮到AGV实际停止的时间 | 全部AGV在200ms内急停，急停后状态变为待机，需人工确认恢复 |
| M-10 | 告警面板功能 | 实时展示各类告警（碰撞、超速、电机过载、低电量、通信异常、任务超时），支持按级别过滤（信息/警告/严重）和确认关闭 | 人工触发各类故障，检查告警出现时间、过滤效果、确认关闭操作 | 告警延迟≤1秒，过滤正确，确认后消失 |
| M-11 | 告警合并规则 | 同一告警源重复告警在5分钟内合并为一条，避免刷屏 | 连续触发同一AGV低电量告警3次，检查告警列表 | 仅显示一条告警，显示重复次数 |

### 3.2 核心算法模块

| 编号 | 测试项 | 验收条件 | 测试方法 | 通过标准 |
|------|--------|----------|----------|----------|
| M-12 | 三级执行策略路由算法 | 任务调度时根据优先级、AGV负载、路径拥堵选择最优路径（全局规划→局部避障→速度自适应） | 构造多AGV拥堵场景，检查算法输出路径是否避开拥堵区，并自适应速度 | 算法输出符合三级策略，路径规划时间≤1秒（100节点地图） |
| M-13 | 最少负载调度算法 | 任务分配时优先选择负载最轻的AGV（综合评估剩余电量、任务队列长度、离起点距离） | 设置3台AGV负载不同，下发10个任务，检查分配结果统计 | 负载最轻的AGV获得任务概率最高，整体负载均衡度（标准差）≤20% |
| M-14 | 拓扑排序循环依赖检测 | 定期检测路径拓扑图中是否存在循环依赖（如AGV互锁），并自动触发解死锁策略 | 故意制造两个AGV对面互锁场景，观察检测时间及解死锁动作 | 检测周期≤1秒，解死锁后AGV正常通行，无死锁持续时间＞5秒 |
| M-15 | 算法单元测试覆盖 | 三个核心算法均有单元测试，输入输出验证 | 查看单元测试报告，覆盖所有分支 | 分支覆盖率≥90%，所有测试通过 |

### 3.3 用户认证与权限模块

| 编号 | 测试项 | 验收条件 | 测试方法 | 通过标准 |
|------|--------|----------|----------|----------|
| M-16 | 用户登录认证 | 支持用户名/密码登录，返回JWT Token | 使用正确/错误凭证登录API | 正确凭证返回200及Token，错误凭证返回401 |
| M-17 | 权限分级控制 | 管理员：可配置系统参数、下发紧急停止；操作员：可查看、下发任务；只读用户：仅查看监控面板 | 分别以三种角色登录，执行不同操作 | 权限拦截正确，越权操作返回403 |
| M-18 | 未登录访问拦截 | 未登录用户无法访问任何页面，跳转至登录页 | 浏览器直接访问受保护URL | 自动跳转登录页，API返回401 |
| M-19 | Token过期处理 | Token过期后自动跳转登录页，并提示“登录已过期” | 使用过期Token访问API | 前端捕获401并跳转，用户需重新登录 |
| M-20 | 敏感操作二次确认 | 修改电子围栏、删除AGV配置等操作需弹出确认对话框 | 操作敏感功能，检查是否出现二次确认 | 必须确认后才执行，取消则不执行 |

### 3.4 用户体验优化模块

| 编号 | 测试项 | 验收条件 | 测试方法 | 通过标准 |
|------|--------|----------|----------|----------|
| M-21 | 操作反馈及时性 | 所有用户操作（任务下发、急停、确认告警）在1秒内给出成功/失败提示 | 模拟操作，观察前端反馈时长 | 提示出现时间≤1秒，失败时展示具体原因 |
| M-22 | 网络断开重连 | 网络断开时显示“连接已断开，正在重连...”，自动重连上限5次，超过后提示手动刷新 | 断开WebSocket连接，观察前端行为 | 重连提示出现，5次后停止，显示手动刷新按钮 |
| M-23 | 暗黑/明亮主题切换 | 支持一键切换主题，所有页面元素适配 | 切换主题，检查各组件配色 | 颜色正确，无未适配元素 |
| M-24 | KPI看板自定义仪表盘 | 支持拖拽组件自定义仪表盘布局 | 拖拽图表组件，修改布局后，检查前端是否通过API保存用户配置；刷新页面后，检查配置是否从后端恢复 | 布局可保存，刷新后恢复 |
| M-25 | 历史数据查询 | KPI历史数据查询支持按时间范围、AGV ID、任务类型筛选，显示结果正确 | 构造历史数据，执行组合查询 | 查询结果准确，分页正常，时间范围有效 |

### 3.5 安全红线验证 [补充]

| 编号 | 测试项 | 验收条件 | 测试方法 | 通过标准 |
|------|--------|----------|----------|----------|
| M-26 | AGV最大速度限制 | AGV在任何情况下，运行速度不得超过1.5m/s | 通过模拟器下发指令，令AGV以高于1.5m/s的速度行驶，检查系统是否强制限速并触发告警 | AGV速度被限制在1.5m/s以内，系统产生“超速”告警 |
| M-27 | 急停触发条件-激光雷达 | 当AGV与障碍物距离<0.5m时，自动触发急停 | 在AGV前方0.4m处放置障碍物，检查AGV是否自动急停 | 触发急停，且急停响应时间≤200ms |
| M-28 | 电子围栏功能 | AGV行驶轨迹不可超出电子围栏边界，超出时触发告警或急停 | 为AGV规划一条超出电子围栏的路径，或手动控制AGV靠近边界 | AGV在边界处停止并触发“边界越界”告警，不会驶出围栏 |

---

## 4. 集成验收标准

| 编号 | 测试项 | 验收条件 | 测试方法 | 通过标准 |
|------|--------|----------|----------|----------|
| I-01 | WebSocket通信稳定性 | 前后端通过WebSocket+ROS 2桥接实现低延迟通信，状态推送延迟≤1秒（端到端） | 记录AGV状态变化时间与前端收到时间差，连续测试30分钟 | 最大延迟≤1秒，平均延迟≤500ms，无断开重连 |
| I-02 | 任务调度与算法集成 | 任务下发后，调度模块正确调用三级执行策略路由和最少负载调度算法，分配结果在2秒内返回 | 下发10个任务，检查调度日志和前端显示 | 调度结果与算法预期一致，算法执行时间≤1秒 |
| I-03 | 地图管理与拓扑排序集成 | 地图路径拓扑更新后，拓扑排序检测模块自动检测循环依赖，触发解死锁 | 动态修改地图路径（增加循环），观察检测结果 | 检测到循环依赖后自动触发解死锁策略，AGV恢复正常 |
| I-04 | 前端与后端数据一致性 | 前端显示的地图、AGV状态、任务列表与后端数据库一致 | 比较前后端数据，随机抽查10个点 | 数据完全一致，无脏数据 |
| I-05 | 身份认证与API集成 | 所有API必须携带有效JWT Token，否则返回401 | 使用curl分别测试无Token、过期Token、有效Token访问API | 符合预期，无绕过漏洞 |
| I-06 | 告警推送与前端联动 | 后端告警事件通过WebSocket推送，前端1秒内显示，并支持确认关闭 | 注入告警事件，检查前端响应 | 告警延迟≤1秒，确认后后端日志记录 |

---

## 5. 系统级验收标准

### 5.1 性能验收

| 编号 | 测试项 | 验收条件 | 测试方法 | 通过标准 |
|------|--------|----------|----------|----------|
| S-01 | 页面加载时间（首屏） | 首屏加载时间≤2秒（含地图数据） | 使用Lighthouse测试5次取平均值，网络模拟4G | 平均加载时间≤2秒 |
| S-02 | 状态推送延迟 | 端到端延迟≤1秒，AGV数据更新频率≥10Hz，前端渲染帧率≥30fps | 使用WebSocket消息监控工具，记录30秒内消息间隔 | 消息间隔≤100ms，帧率≥30fps |
| S-03 | KPI看板刷新频率 | 数据刷新频率≥10Hz | 监测KPI折线图数据更新间隔 | 更新间隔≤100ms，CPU占用≤50% |
| S-04 | 数据库并发写入 | 100个并发写入任务，无“database is locked”错误 | 使用JMeter模拟100个并发写入KPI数据 | 全部成功，无死锁或超时错误 |
| S-05 | 大规模AGV场景性能 | 100台AGV同时运行，前端帧率≥30fps，WebSocket连接数≥200 | 使用模拟器模拟100台AGV，持续运行10分钟 | 前端流畅，无卡顿，CPU占用≤70% |

### 5.2 可用性与可靠性验收

| 编号 | 测试项 | 验收条件 | 测试方法 | 通过标准 |
|------|--------|----------|----------|----------|
| S-06 | 系统可用性 | 系统可用性≥99.9%（全年非计划停机≤8.76小时） | **修改：** 通过故障注入测试，模拟以下场景并记录恢复时间：1) 主数据库宕机，触发主从切换；2) 后端服务进程崩溃，触发自动重启；3) 网络中断后恢复。每种场景测试3次，取平均值。 | 所有场景下，服务恢复时间≤5分钟，且恢复后数据一致。基于此推算，30天内非计划停机时间≤720分钟。 |
| S-07 | 历史KPI数据保留 | 30天数据自动清理，过期数据归档至冷存储 | 检查数据库表中数据时间戳，观察清理脚本执行 | 数据保留30天，过期后自动移除 |
| S-08 | AGV轨迹日志保留 | 保留7天，支持按AGV ID导出 | 生成7天前的轨迹数据，检查是否保留；执行导出操作 | 7天内数据可导出，7天前数据已清理 |
| S-09 | 故障恢复时间 | 服务崩溃后自动重启，数据库主从切换后自动恢复服务，整体恢复时间≤5分钟 | 模拟服务崩溃，记录恢复时间 | 恢复时间≤5分钟 |
| S-10 | 子进程安全规范 | 系统不启动任何子进程，所有调度逻辑通过ROS 2节点实现 | **修改：** 1. 扫描系统进程列表，确认无Python `subprocess`、`os.system`等调用。2. 审查所有API接口代码，确保无通过`exec()`、`eval()`等函数执行用户输入的情况。3. 验证所有外部命令调用（如有）均通过白名单和参数化方式实现。 | 无子进程，无`subprocess`/`shell`命令执行风险，无代码注入漏洞。 |

### 5.3 安全验收 [补充]

| 编号 | 测试项 | 验收条件 | 测试方法 | 通过标准 |
|------|--------|----------|----------|----------|
| S-11 | 通信加密 | 所有WebSocket和HTTP API必须使用TLS 1.2+加密 | 使用Wireshark抓包，检查HTTP/WebSocket流量是否被加密 | 无明文HTTP/WebSocket流量，TLS版本为1.2或更高 |
| S-12 | 审计日志 | 所有关键操作（任务下发、急停、告警确认、用户登录、参数修改）均记录审计日志，包含操作人、时间、操作内容、结果，且保留至少90天 | 1. 执行一系列关键操作。<br>2. 检查审计日志数据库/文件，确认记录完整。<br>3. 生成90天前的日志，确认已被归档或清理（根据设计）。 | 审计日志记录完整，可追溯，保留期限达标。 |

### 5.4 可扩展性验收 [补充]

| 编号 | 测试项 | 验收条件 | 测试方法 | 通过标准 |
|------|--------|----------|----------|----------|
| S-13 | 后端服务水平扩展 | 后端服务可部署多实例，通过负载均衡分发WebSocket连接 | 1. 部署2个后端服务实例，配置Nginx负载均衡。<br>2. 启动100个模拟WebSocket客户端连接。<br>3. 检查Nginx是否将连接均匀分发到两个实例。 | 连接成功建立，连接数在实例间均匀分布（偏差≤10%），所有功能正常。 |

### 5.5 兼容性验收 [补充]

| 编号 | 测试项 | 验收条件 | 测试方法 | 通过标准 |
|------|--------|----------|----------|----------|
| S-14 | 浏览器兼容性 | 在主流浏览器上功能正常，布局无错乱 | 在Chrome、Firefox、Edge最新版上，执行所有核心功能测试用例（M-01至M-25）。 | 所有功能正常，样式一致，无JS报错。 |

---