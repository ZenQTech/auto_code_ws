# 架构设计文档 (spec.md)

## 1. 模块视图
系统划分为三个核心模块，职责清晰，无循环依赖：

- **UI视图层 (View Layer)**: 负责HTML结构渲染与CSS样式展示。包含主显示区、结果区、按钮网格、历史记录面板（可选）。不包含任何业务逻辑，仅通过事件通知控制器。
- **控制器层 (Controller Layer)**: 负责事件监听（按钮点击、键盘输入）、表达式字符串管理、状态管理（正常、错误、等待）、调用计算引擎、更新视图。是UI层与计算引擎层之间的唯一桥梁。
- **计算引擎层 (Engine Layer)**: 独立的、无状态的表达式解析与计算模块。负责词法分析、语法分析（基于运算符优先级与括号）、数值计算、错误检测（除零、表达式无效、溢出）。不依赖任何UI或DOM元素。

## 2. 接口契约
模块间通过定义清晰的接口进行通信，所有数据为字符串或数字，无复杂对象。

- **UI层 -> 控制器层**:
    - 事件: `input-event` (携带字符串值，如 '1', '+', '=', 'C', 'Backspace' 等)
    - 触发方式: 按钮监听 `click` 事件，键盘监听 `keydown`/`keyup` 事件。
- **控制器层 -> UI层**:
    - 方法: `updateDisplay(expression: string, result: string | null, error: string | null)`
    - 方法: `highlightButton(key: string)`
    - 方法: `showError(errorMessage: string)`
- **控制器层 -> 计算引擎层**:
    - 方法: `calculate(expression: string): CalculationResult`
    - `CalculationResult` 接口定义:
    ```typescript
    interface CalculationResult {
        success: boolean;
        value: number | null;
        error: string | null; // 如 "除数不能为0", "表达式无效", "结果超出范围"
    }
    ```
- **通信协议**: 同步调用。控制器调用计算引擎的 `calculate` 方法，并立即获得结果。`updateDisplay` 方法由控制器同步调用，确保UI更新与计算原子性。

## 3. 安全与性能基线
- **安全基线**:
    - **输入过滤**: 所有输入（按钮、键盘）在控制器层进行过滤。仅允许字符集: `[0-9+\-*/().\s]`。所有其他字符被静默忽略或阻止。
    - **DOM操作安全**: 更新显示区时，使用 `textContent` 而非 `innerHTML`，防止HTML/脚本注入。
    - **无外部依赖**: 无网络请求，无第三方库，无本地存储（除非用户明确启用历史记录）。
- **性能基线**:
    - **计算响应时间**: 从点击“=”到dom更新完成，< 100ms。
    - **内存占用**: 运行时内存峰值 < 10MB (远低于50MB要求)。
    - **零卡顿**: 所有操作在主线程同步完成，无setTimeout或异步操作。
- **可靠性基线**:
    - **计算准确率**: 核心运算（+、-、*、/）需通过所有测试用例，包括 `0.1 + 0.2` 的精确处理。
    - **24小时稳定性**: 单页应用无状态泄漏，无内存泄漏。

## 4. 技术选型
- **技术栈**: 原生HTML5 + CSS3 + JavaScript (ES6+)
- **框架**: 无
- **库**: 无
- **解析方案**: 手写递归下降解析器 或 调度场算法 (Shunting-yard algorithm) 实现。推荐使用调度场算法，因其更易于扩展新运算符。
- **数字精度处理**: 使用 `toFixed()` 或 `Number.EPSILON` 进行浮点数舍入，或定制高精度小数运算（基于字符串处理，但V2.0要求简单，可先使用原生浮点数+舍入）。

## 5. 部署架构
- **部署形态**: 单HTML文件。所有CSS内联至 `<style>` 标签，所有JavaScript内联至 `<script>` 标签。
- **资源需求**: 几乎为零。无需服务器，无需网络，无需数据库。
- **访问方式**: 用户可直接通过浏览器打开本地HTML文件，或通过任意静态服务器（如Nginx、Apache、Python http.server）托管。
- **兼容性**: 基于ES6+语法，通过Babel转译至ES5（可选，但需求明确禁止构建工具，因此直接使用现代语法，依靠浏览器兼容性）。确保在所有现代浏览器上运行。