# 架构合规检查清单 (checklist.md)

- [x] **模块职责单一，无循环依赖**: UI、控制器、引擎三层职责清晰，控制器依赖引擎和UI，但引擎和UI互不依赖。
- [x] **接口契约完整，含错误处理**: `CalculationResult` 接口明确包含 `success`、`value`、`error` 字段，覆盖正常和异常场景。
- [x] **安全红线已覆盖所有模块**: 输入过滤在控制器层统一执行；DOM更新使用 `textContent`；无网络请求；无第三方库植入。
- [x] **性能指标可量化、可测量**: 响应时间 < 100ms，内存 < 10MB，可在浏览器DevTools中直接测量。
- [x] **技术选型版本明确、兼容性已验证**: 指定原生HTML5/CSS3/ES6+，无版本依赖问题。兼容性由浏览器本身保证。
- [x] **部署方案可执行**: 单文件部署，可本地打开或通过静态服务器托管，方案简单可靠。