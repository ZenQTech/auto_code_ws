# Checklist: Loop Engineering 完整工作流

> **基于 Spec**: [spec.md](file:///home/qizheng/auto_code_ws/.trae/specs/loop-engineering-full-workflow/spec.md)

---

## Hermes 调度增强

- [x] Hermes 在用户输入开发需求时自动创建 ChiefArchitect 和 CriticalReviewer 智能体实例
- [x] 智能体实例注册到 AgentManager 并持久化到数据库
- [x] `dispatch_by_stage()` 根据工作流阶段正确激活对应智能体
- [x] 评判师复用 CriticalReviewer 的 5 维度评审能力 + 增强全链路能力

## 总架构师交互引导

- [x] 总架构师分析用户需求后生成 6 维度引导问题
- [x] 引导问题通过 SSE 逐轮推送到前端
- [x] 信息充分性判定正确（6 维度中至少 4 个有明确答案）
- [x] 信息不足时继续追问，最多 5 轮
- [x] 信息收集完成后生成结构化任务框架（项目概述、模块划分、依赖关系、技术选型、风险识别）

## 协作验收标准制定

- [x] 总架构师生成初步验收标准
- [x] 评判师从功能覆盖率、可执行性、可衡量性、边界覆盖 4 维度审查
- [x] 协作讨论循环正确执行（起草 → 审查 → 修订 → 再审查）
- [x] 验收标准包含 8 个章节（模块级/集成/系统级/代码质量/性能/安全/兼容性/环境要求）
- [x] 100% 可验证性校验通过：每条验收项有明确验证方法和量化通过条件
- [x] `acceptance_verified` 标记正确设置为 True

## 模块任务分发

- [x] 总架构师正确解析 task.md 提取所有模块任务
- [x] 模块按依赖关系排序（依赖优先执行）
- [x] 每个模块创建独立的 PromptEngineer 实例
- [x] PromptEngineer 上下文感知优化：包含模块依赖、全局接口规范、架构约束
- [x] 提示词质量闭环校验：歧义评分 < 0.3，约束覆盖率 > 0.8
- [x] 校验不通过自动重优化（最多 3 轮）
- [x] Claude Code CLI 实例成功创建并注入优化后的提示词
- [x] 独立 Git worktree 隔离工作空间正确创建
- [x] 无依赖模块并行执行，有依赖模块串行执行
- [x] 最大并行实例数由配置控制

## 全链路智能评审

- [x] 单模块评审：语义正确性、边界条件覆盖、安全漏洞扫描、规范合规性、验收标准匹配
- [x] 跨模块集成评审：接口一致性、数据类型一致、调用时序、循环依赖检测、全局状态一致性
- [x] 需求-代码可追溯性验证：需求覆盖率矩阵正确生成
- [x] 评审报告结构化输出：综合评分、各维度评分、缺陷清单、通过/不通过判定
- [x] 评审报告通过 SSE 推送到前端

## 智能迭代闭环

- [x] 评审不通过时传递精确缺陷定位和修复建议（非简单告知失败）
- [x] Claude Code CLI 实例能根据缺陷清单修复代码
- [x] 迭代跟踪：记录每次迭代的缺陷修复情况，确保无回归
- [x] 迭代 2 次仍不通过自动升级为人工审核
- [x] 迭代终止条件正确：全部通过 / 达到 3 轮上限 / 人工强制终止

## 全链路自动化测试流水线

- [x] 流水线 6 步骤按顺序执行：提示词注入 → 需求细化 → 代码生成 → 任务评判 → Git 提交 → 集成测试
- [x] 每步骤状态通过 SSE 实时推送（running/completed/failed）
- [x] 全链路成功判定：所有步骤 completed + 所有模块评审通过 + Git 提交成功 + 集成测试通过
- [x] 任一条件不满足正确判定为任务未完成
- [x] `POST /api/workflow/{id}/pipeline-test` 端点正常工作

## Goal 导向任务循环

- [x] Goal 创建后自动分解子目标
- [x] 子目标有明确验收标准和依赖关系
- [x] 子目标按依赖拓扑顺序执行
- [x] Goal 循环执行：选择子目标 → 分配 CLI → 生成代码 → 评审 → 标记完成
- [x] 所有子目标完成后触发全链路验证
- [x] 全链路验证通过后 Goal 标记为 completed
- [x] 子目标无法完成时 Goal 标记为 blocked
- [x] Workflow 模型新增 `goal_id` 字段

## 前端适配

- [x] ReviewReport 组件正确展示评审报告（评分、缺陷清单、判定）
- [x] PipelineProgress 组件正确展示流水线步骤进度
- [x] GoalProgress 组件正确展示子目标执行进度
- [x] 所有新组件正确监听对应 SSE 事件

## 全链路集成测试

- [x] 完整工作流端到端测试通过（用户输入 → 引导交互 → 架构设计 → 验收标准 → 任务分发 → 代码生成 → 评审 → 迭代）
- [x] 全链路自动化测试流水线 6 步骤正常执行
- [x] 智能迭代闭环正常触发和终止
- [x] Goal 导向循环正常创建和完成
- [x] 任务未完成判定标准正确触发

---

## 验证结果摘要

| 验证维度 | 状态 |
|----------|------|
| Python 全链路导入（13 项） | 全部通过 |
| 新增 dataclass 类型（10 个） | 全部可用 |
| 新增方法签名（23 个） | 全部存在 |
| API 新端点（2 个） | 正确注册 |
| Workflow 新增字段（2 个） | goal_id + goals |
| 前端 TypeScript 编译 | 零错误 |
| 前端新组件（3 个） | ReviewReport + PipelineProgress + GoalProgress |
| 已有功能兼容性 | 完全向后兼容 |