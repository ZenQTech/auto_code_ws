# 智能仓储系统架构方案

> **版本:** v1.0 | **日期:** 2026-07-24 | **架构风格:** 微服务 + 事件驱动 + 边缘计算

---

## 一、系统概述

智能仓储系统（Intelligent Warehouse Management System, iWMS）是一套面向现代化仓储物流场景的数字化管理平台，覆盖 **入库、出库、盘点、移库、质检、追溯** 等全业务流程，通过 **IoT 设备 + 自动化设备 + 业务中台** 三层联动，实现仓储运营的智能化、可视化和高效化。

### 系统目标

| 维度 | 目标 |
|------|------|
| 运营效率 | 出库时效 ≤ 30 分钟，库存准确率 ≥ 99.9% |
| 自动化率 | AGV/AMR 覆盖率 ≥ 80%，自动分拣 ≥ 90% |
| 实时性 | 库存变动 < 1 秒同步，设备状态 < 500ms |
| 可扩展性 | 支持 100+ 仓库节点，万级设备并发接入 |
| 可靠性 | 系统可用性 ≥ 99.99%，数据零丢失 |

---

## 二、六层架构设计

### 2.1 用户界面层（Presentation Layer）

| 终端 | 技术选型 | 说明 |
|------|----------|------|
| Web 管理面板 | React + TypeScript + Ant Design | 运营管理后台，支持 2D/3D 仓库可视化 |
| 移动端 App | Flutter | 仓管员手持 PDA 功能，扫码、盘点、上架 |
| PDA 终端 | Android 原生 | 工业级扫码终端，支持离线模式 |
| 大屏看板 | DataV / ECharts | 实时 KPI 大屏，仓储热力图、设备状态 |
| 外部系统对接 | REST API / EDI / SFTP | ERP、MES、TMS、WCS 系统集成 |

### 2.2 API 网关层（API Gateway Layer）

- **网关组件:** Nginx + Kong 或 Spring Cloud Gateway
- **核心功能:**
  - 统一入口：路由转发、协议转换
  - 认证鉴权：JWT + OAuth2 + RBAC
  - 流量治理：限流（Rate Limiting）、熔断（Circuit Breaker）、负载均衡
  - 安全防护：SQL 注入/XSS 防护、IP 黑白名单
  - 可观测：请求追踪（Sleuth/Zipkin）、日志聚合
  - API 版本管理：URL 版本 / Header 版本

### 2.3 核心业务服务层（Core Business Services）

采用 **Spring Cloud** 或 **Go Micro** 微服务框架，服务间通过 gRPC 或 REST 通信，通过 Nacos 注册发现。

#### 核心微服务清单

| 服务名称 | 职责 | 关键数据 |
|----------|------|----------|
| 库存管理服务 | 库存台账、库存锁定、库存预警 | 库存表、库存流水 |
| 订单管理服务 | 订单接收、波次管理、出库调度 | 订单表、出库单 |
| 库位管理服务 | 库位分配、库位优化、库位锁定 | 库位表、货架表 |
| 任务调度服务 | 自动化任务分派、优先级调度 | 任务表、调度日志 |
| 出入库管理 | 预约、收货、质检、上架、拣货、复核 | 入库单、出库单 |
| 质检追溯服务 | 质检规则、批次追踪、溯源链 | 质检记录、批次表 |
| 用户权限服务 | 用户管理、角色、权限、菜单 | 用户表、角色表 |
| 通知服务 | 短信、邮件、站内信、App Push | 通知模板、发送记录 |
| 报表分析服务 | 数据统计、BI 报表、趋势分析 | 报表数据、KPI 指标 |
| 日志审计服务 | 操作日志、变更追踪、合规审计 | 审计日志表 |
| 配置管理中心 | 动态配置、环境管理、灰度发布 | Nacos / Apollo |

### 2.4 数据与消息层（Data & Message Layer）

| 组件 | 技术选型 | 用途 |
|------|----------|------|
| 消息队列 | Kafka / RabbitMQ | 事件驱动、异步解耦、削峰填谷 |
| 关系型数据库 | PostgreSQL (主从架构) | 业务数据持久化（库存、订单、用户） |
| 缓存层 | Redis Cluster | 热点数据缓存、分布式锁、会话管理 |
| 时序数据库 | InfluxDB / TDengine | IoT 设备数据、传感器时序数据 |
| 搜索引擎 | Elasticsearch | 库存全文检索、日志分析、BI 查询 |
| 数据仓库 | ClickHouse | 离线分析、大屏报表、OLAP 查询 |

#### 数据流设计

```
IoT 设备 → MQTT → Edge Gateway → Kafka → 业务服务 → PostgreSQL
                                          ↓
                                     时序数据库 → 实时看板
                                          ↓
                                     ClickHouse → BI 报表
```

### 2.5 边缘计算与自动化层（Edge & Automation Layer）

| 组件 | 功能 | 通信协议 |
|------|------|----------|
| 边缘计算网关 | 协议转换、数据过滤、本地缓存 | MQTT + OPC-UA |
| AGV/AMR 调度系统 | 路径规划、交通管制、任务调度 | WebSocket + HTTP |
| 机械臂控制 | 拆垛、码垛、分拣控制 | Modbus TCP / EtherCAT |
| 输送线控制 | 分拣、合流、转向控制 | PLC (西门子/三菱) |
| 提升机/穿梭车 | 跨层运输、密集存储 | 私有协议 |

**边缘计算策略:**
- 本地处理率 ≥ 80%：设备数据在边缘端完成清洗、聚合、报警
- 断网容错：边缘节点本地缓存 72 小时数据，网络恢复后自动同步
- 低延迟控制：AGV 调度指令延迟 < 50ms

### 2.6 IoT 设备层（IoT Device Layer）

| 设备类型 | 采集数据 | 通信方式 |
|----------|----------|----------|
| RFID 读写器 | 托盘标签、货物标签 | RS485 / WiFi |
| 条码扫描器 | 一维码、二维码 | USB / Bluetooth |
| 智能货架 | 货位占用、重量传感 | ZigBee / LoRa |
| 温湿度传感器 | 环境温湿度 | ZigBee / 4G |
| 称重地磅 | 货物重量 | RS232 / Ethernet |
| AGV 小车 | 位置、电量、状态 | WiFi 5G / 私有协议 |
| 工业相机 | OCR 识别、尺寸测量 | GigE Vision |
| PLC 控制器 | 输送线、提升机控制 | Profinet / EtherCAT |

---

## 三、关键业务流程

### 3.1 入库流程

```
WMS 预约 → 到货登记 → 质检验收 → 系统分配库位 → 
                                           ↓
                                    AGV 搬运上架 → 确认锁定
```

### 3.2 出库流程

```
OMS 订单下发 → WMS 波次计算 → 库存分配 → 生成拣货任务 →
                                                  ↓
                      AGV 搬运货架到拣货站 → 人工/机械臂拣货
                                                  ↓
                              复核打包 → 装车发货 → 库存扣减
```

### 3.3 盘点流程

```
制定盘点计划 → 下发盘点任务 → PDA 扫码 / RFID 自动盘点 →
                                                       ↓
                                     差异处理 → 调整库存 → 盘点报告
```

---

## 四、非功能性设计

### 4.1 高可用架构

| 维度 | 策略 |
|------|------|
| 服务高可用 | 每个微服务 ≥ 2 副本，K8s 自动扩缩容 |
| 数据库高可用 | PostgreSQL 主从 + Patroni 自动切换 |
| 缓存高可用 | Redis Cluster 三主三从 |
| 消息高可用 | Kafka 分区副本 + ISR 机制 |
| 网关高可用 | Nginx 双活 + Keepalived |
| 跨机房容灾 | 两地三中心设计 |

### 4.2 安全设计

- **传输安全:** 全链路 HTTPS，IoT 设备 MQTTS
- **认证授权:** OAuth2 + JWT + RBAC 细粒度权限
- **数据安全:** 敏感数据 AES-256 加密存储，脱敏展示
- **操作审计:** 所有操作记录可追溯，不可篡改
- **网络安全:** VPC 隔离 + 安全组 + WAF

### 4.3 可观测性

```
指标监控: Prometheus + Grafana  (CPU/内存/QPS/延迟)
日志收集: Filebeat + ELK       (集中日志查询)
链路追踪: Jaeger / Zipkin      (全链路调用拓扑)
告警通知: AlertManager + 钉钉/企微 (分级告警)
```

---

## 五、部署架构

```
┌─────────────────────────────────────────────────────┐
│                  用户端 (CDN)                         │
│    Web 面板 · 移动端 · PDA · 大屏 · 外部系统          │
└────────────────────┬────────────────────────────────┘
                     │ HTTPS
┌────────────────────▼────────────────────────────────┐
│              API Gateway 层 (Kong/Nginx)              │
│          认证鉴权 · 限流 · 路由 · 日志                 │
└────────────────────┬────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────┐
│              Kubernetes 集群 (K8s)                    │
│  ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐ ┌──────┐      │
│  │库存  │ │订单  │ │调度  │ │权限  │ │报表  │ ...    │
│  │服务  │ │服务  │ │服务  │ │服务  │ │服务  │        │
│  └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘ └──┬───┘      │
│     │        │        │        │        │           │
│  ┌──▼────────▼────────▼────────▼────────▼───┐       │
│  │        消息队列 Kafka / RabbitMQ          │       │
│  └────────────────┬─────────────────────────┘       │
│  ┌────────────────▼─────────────────────────┐       │
│  │    PostgreSQL · Redis · InfluxDB · ES    │       │
│  └────────────────┬─────────────────────────┘       │
└────────────────────┬────────────────────────────────┘
                     │
┌────────────────────▼────────────────────────────────┐
│           边缘计算 / 自动化控制层                      │
│  Edge Gateway · AGV Scheduler · Robot Controller     │
└────────────────────┬────────────────────────────────┘
                     │ MQTT / Modbus / OPC-UA
┌────────────────────▼────────────────────────────────┐
│                 IoT 设备层                            │
│  RFID · 扫码 · 传感器 · AGV · PLC · 工业相机           │
└─────────────────────────────────────────────────────┘
```

---

## 六、技术选型总结

| 层级 | 推荐技术 | 备选方案 |
|------|----------|----------|
| 前端 | React + TypeScript | Vue 3 + Element Plus |
| 移动端 | Flutter | React Native |
| 后端框架 | Spring Cloud / Go Micro | Dubbo / Kitex |
| API 网关 | Kong / Nginx | APISIX / Spring Cloud Gateway |
| 消息队列 | Kafka | RocketMQ / Pulsar |
| 关系数据库 | PostgreSQL | MySQL 8.0 |
| 缓存 | Redis Cluster | Memcached |
| 时序数据库 | InfluxDB | TDengine / TimescaleDB |
| 搜索引擎 | Elasticsearch | OpenSearch |
| 容器编排 | Kubernetes | Docker Swarm |
| 监控 | Prometheus + Grafana | Zabbix + Nightingale |
| 日志 | ELK Stack | Loki + Grafana |
| CI/CD | GitLab CI / Jenkins | ArgoCD / GitHub Actions |

---

## 七、灰度与演进路线

### Phase 1 — 基础搭建 (1-2 月)
- 核心业务服务（库存管理、出入库管理、订单管理）
- 基础数据模型和 API 设计
- 前后端基础框架搭建
- 支持手动/ PDA 操作

### Phase 2 — 自动化集成 (3-4 月)
- AGV/AMR 调度系统对接
- 消息队列 + 事件驱动架构
- 边缘计算网关部署
- IoT 设备接入（RFID、传感器、扫码器）

### Phase 3 — 智能化升级 (5-6 月)
- AI 预测：库存需求预测、智能补货
- 路径优化：AGV 动态路径规划
- 视觉识别：OCR 识别、尺寸测量、缺陷检测
- 数字孪生：3D 仓库可视化

### Phase 4 — 生态扩展 (7-8 月)
- 多仓库统一管理
- ERP/MES/TMS 深度集成
- 数据中台 + BI 分析
- 开放平台 API 生态

---

> **文档说明:** 本架构方案为智能仓储系统的顶层设计，各模块的详细设计（接口定义、数据模型、部署配置）将在后续阶段逐步完善。