**机器人全栈自动化编程：多智能体协同 V4.1 版**

**一****、架构总览**

**2.1 核心设计逻辑**

本架构采用「智能体调度平台 + Claude Code Agent Teams 协同执行」的三层核心设计，以节省API请求次数为第一优先级，同时保障开发质量、安全合规与落地性：

• **智能体调度平台****（全局唯一管控入口，模块化解耦设计）**：注意智能体调度平台必须打开流式思考，思考过程可折叠但是必须要能够显示

采用「主进程+独立子模块」的解耦架构，避免单点故障风险，各子模块异常隔离，核心职责如下：

a. 唯一全局调度入口：负责本地拦截调度、顶层任务调度、心跳进度跟踪、状态维护反馈、异常兜底、本地校验执行、数据持久化、Git版本管理；

c. 配额全流程管控：实时监控全量+单模型API调用次数与Token消耗，触发三级告警与熔断机制，执行配额预留与鉴权规则；

d. 上下文生命周期管理：管控所有Agent的对话上下文，区分全局约束与对话信息，在上下文阈值打到70%的时候触发上下文压缩功能以保证模型的输出质量；

• **全流程刚性校验机制**：每个环节流转前必须完成格式+内容双重校验，核心环节必须完成合规性评审，从流程上杜绝错误内容向下游流转，避免返工导致的重复API调用。

**2.2 全流程架构拓扑图**

[流程图核心链路：用户需求 → 需求类型与安全风险前置分流 → 智能体调度平台调度中心 → 需求澄清→架构设计→批判迭代→人工架构确认→任务拆解与风险标记→按照模块生成claude code cli实例→按照模块分发任务→协同开发→本地前置校验→单模块安全校验→高风险模块人工审核→测试验证→集成校验→跨模块安全测试→系统评测→人工仿真验证→交付归档→智能体调度平台推送git]

**三、核心角色与协同体系**

**3.1 顶层调度层：****智能体调度平台**

| 角色/模块                   | 核心定位                                                    | 核心职责（全量管控能力升级）                                 |
| --------------------------- | ----------------------------------------------------------- | ------------------------------------------------------------ |
| 智能体调度平台 顶层调度中心 | 全局唯一调度中枢、唯一API调用入口、全流程状态与成本管控核心 | 1. 理解人类模糊开发构想，多轮引导补充开发关键细节（约束条件、性能指标、仿真环境要求、安全红线要求、ROS/依赖版本信息等）；2. 用户确认信息后执行开发，对多轮用户提示词进行提示词优化，生成无歧义、可直接执行的标准化提示词；消除语义歧义，固化核心约束，明确下游执行要求；保障架构信息在传递过程中无失真、无遗漏。输出标准化、无歧义的结构化需求文档3. 按照任务模块生成多个claude code cli实例注入提示词，要求claude code cli进行任务规划；阅读所有claude code cli生成的plan文档、check list文档、task文档，将所有文档整合为一个大的原子任务清单。明确任务执行时序与并行规则；【高风险模块刚性标记】严格按照高安全风险模块三级界定标准，在任务清单中精准标记风险等级，宁严勿漏，禁止漏标、错标；输出全局接口定义清单、自定义消息/服务规范、依赖版本统一规范，作为全流程刚性约束；4. 实时处理需求变更，同步更新需求文档，明确变更影响范围并更新相应claude code cli提示词用于处理需求变更。5. 本地优先拦截全流程管控：需求类型与风险等级前置分流、代码安全校验、用户反馈收集、生成skill入库全流程执行；6. 全局配置中心管理：维护全流程可配置参数，支持用户自定义调整，确保全文档规则执行一致性；7. 接收用户任务与确认后的架构/任务清单，全包给 Claude Code Agent Teams，同步全局约束与规范；8. 双重心跳检测：实时检测 Claude Code 运行状态、单任务执行状态、Hook打点有效性，识别超时、阻塞、停滞异常；9. 接收 Claude Code Hook 通知，同步任务进度、完成状态、告警信息给用户，触发人工节点提醒；10. API请求次数全流程刚性管控：实时监控全量+单模型 API 调用次数与Token消耗，触发三级告警与熔断机制，执行配额预留与鉴权规则；11. 上下文生命周期管理：管控所有Agent的对话上下文，在上下文阈值打到70%的时候触发上下文压缩功能以保证模型的输出质量。针对调用失败与重试添加相应措施；12. 在任务完成后阅读原子任务清单以及所有claude code cli生成的plan文档、check list文档、task文档，逐一确认所有任务是否完成；13. 全流程校验执行：claude code代码编写完成后自动执行静态检查、安全编码校验、预编译校验、测试脚本执行后提交任务完成标志，调度平台接收后阅读所有claude code计划确认所有任务完成，如果有未完成的任务则返回给指定claude code；如果确认所有任务完成后则在任务调度平台自动对集成后的全量代码做全局深度评测，覆盖架构合理性、代码质量、工程规范符合性；完成机器人系统全链路实时性评测，评估端到端控制延迟、时序稳定性、调度合理性；完成核心算法专项评测，评估算法选型合理性、性能指标、场景适配性、鲁棒性、安全冗余度；完成安全专项评测，对全系统的安全架构、急停逻辑、故障兜底能力、边界条件处理做全面评审；挖掘全局逻辑漏洞、性能瓶颈、架构缺陷，输出结构化评测报告与优先级优化建议；给出明确的终审是否通过结论，如果没有通过则根据模块给claude code cli注入系统优化提示词进行系统优化，如果通过则跟踪全流程需求变更、架构迭代、代码修改、测试问题的闭环情况，同步更新全链路状态；归档系统评测报告、测试报告，输出标准化CHANGELOG，严格遵循语义化版本（Semantic Versioning）标准；整理最终交付物，严格按照标准化目录结构归档，确保交付物规范完整；进行日志记录、产物完整性校验，提交Git；输出最终交付版本的Tag与说明文档。14. 断点续跑管理：任务中断后，完成全局变更校验、依赖链一致性校验、产物与状态一致性校验、人工修改识别，从最近的完成节点重启任务；15. Git版本管理全量执行：仓库初始化、代码自动提交、分支管理、版本标记、冲突检测与告警，严格遵循用户配置的分支策略与提交规则；16. 记忆库：由智能体调度平台调用hermes智能体针对任务完成所有流程进行skill编写并入库（claude code+hermes）；17. 全局变更全闭环管控：接口/架构/需求变更后，影响范围评估、下游任务暂停与更新、让所有正在进行编程的claude code cli更新任务计划文档，更新check list确认任务进度，任务调度平台阅读所有任务计划文档确认整体进度并存档，确保100%闭环，避免遗漏导致的返工；18. 循环依赖与死锁实时检测、人工干预指令执行、全流程异常兜底处理。 |

**3.3 协同机制核心优化（彻底解决配额浪费与流程断点）**

1. **Hook强制依赖与状态校验机制**：claude code cli的所有状态都通过hooks通知智能体调度平台，平台收到Hook通知后，必须先完成产物完整性校验，再更新任务状态，杜绝状态与实际产物不一致问题。
2. **Hook失效兜底检测机制**：智能体调度平台设置双重超时检测+状态轮询机制，任务分发后，超过预期执行时间的2倍仍未收到Hook状态打点，主动查询Claude Code任务执行状态，确认完成则手动同步流程、校验产物、触发下游环节；失败则按异常规则处理，彻底解决流程永久停滞问题。
3. **全局规范固化与接口变更全闭环管控机制**：将架构确定的全局接口规范、依赖版本、编码标准、安全要求固化为全局约束文件，智能体调度平台在每个任务分发时强制传入，禁止单模块私自修改；确需调整接口的，必须走规范的变更流程，完成影响范围评估后，立即暂停所有依赖该接口的任务，更新任务上下文与约束条件，100%完成适配校验后，再恢复流程执行，从源头避免接口冲突、依赖混乱、返工导致的重复调用。
4. **Skill优化调用机制**：通过 Claude Code Skill 固化通用模板、复用历史上下文、减少重复查询，大幅降低无效调用次数与Token消耗。
5. **任务状态机刚性管控机制**：每个任务只有「待执行、执行中、已完成、已失败、已阻塞、待更新」6种固定状态，状态变更必须通过 Hook 同步打点，未完成任务禁止下游 Agent 启动；任务失败/阻塞/待更新时，立即暂停下游所有依赖任务，推送告警通知。
6. **并行任务精细化管控与死锁预防机制**：

￮ 并行数与限流联动规则：严格按照配额告警等级动态调整并行上限，同时按任务类型动态管控，核心算法高算力任务并行数不超过3个，避免请求突增触发限流；配额充足时最大并行数不超过5个，配额紧张时自动降低，避免请求突增触发限流；

￮ 优先级调度规则：严格按照「高/中/低」优先级调度，配额紧张时自动暂停低优先级任务，优先保障高优先级核心任务；新增防任务饿死机制，低优先级任务等待超24小时临时提权，避免永久停滞；

￮ 异常隔离机制：单个并行任务出现超时、失败、阻塞异常，仅暂停该任务及其下游依赖任务，不影响其他无依赖的并行任务执行；

￮ 循环依赖与死锁检测：任务清单分发前做静态循环依赖检测，运行中实时监控任务等待状态，动态检测交叉依赖与死锁，发现后立即告警并暂停相关任务。

8. **标准化信息传递机制**：彻底解决上下文丢失、信息传递失真问题

￮ 全局信息固化：将最终确认的「结构化需求文档、系统架构设计、全局技术栈约束、环境要求、验收标准、接口规范」固化为全局配置文件，智能体调度平台 每一次任务推送，必须将对应全局约束随任务详情一并传入Agent；

￮ 上下游产物传递：任务的前置依赖产物，由 智能体调度平台 自动提取、整理后随任务一并传入，确保上下游接口匹配、信息无遗漏；

￮ 状态同步机制：任何Agent完成任务后，同步更新全局任务状态，确保全流程信息一致性；

**四****、全流程执行标准SOP（刚性执行，补全所有闭环断点，减少返工与无效调用）**

**5.****1** **需求澄清与确认阶段**

1. 进入需求澄清阶段，智能体调度平台输出结构化需求文档框架与补充提问清单，引导人类补充完整机器人开发的关键细节、约束条件、性能指标、仿真环境要求、安全红线要求、ROS/依赖版本信息。完成后输出最终版标准化结构化需求文档，提交人类确认；人类确认后进入下一阶段，不通过则继续补充优化。

**5.3 架构设计与批判迭代阶段**

1. 智能体调度平台基于人类确认的需求文档，生成初始版本系统架构设计，明确模块划分、全局接口规范、安全架构、技术选型，输出内容必须符合结构化规范，无歧义。
2. 智能体调度平台基于架构文档，完成全维度架构批判，输出结构化缺陷清单与修复方案。
3. 智能体调度平台基于批判反馈迭代优化架构，架构批判迭代最多3次，超过阈值强制进入人工确认环节，避免无限迭代浪费配额。
4. 人类对迭代后的最终架构做最终确认，不通过则返回修改，通过则进入下一阶段。

【刚性规则】人工驳回架构后，智能体调度平台根据驳回意见修改架构，必须重新走「架构批判迭代」流程，禁止直接跳过批判环节再次提交人工确认；同一版核心需求，架构人工驳回修改最多2次，超过2次仍未通过的，强制触发人工评审，输出历史驳回记录、缺陷清单，引导用户明确核心调整方向，避免无意义反复修改。

【可选触发规则】仅当智能体调度平台反馈架构描述存在歧义、语义模糊时，才做结构化优化，无问题则直接进入人工确认环节，减少无效API调用。

**5.4 任务拆解与****智能体调度平台****分发****阶段**

1. 智能体调度平台基于人工确认的最终架构，生成标准化JSON任务清单，严格遵循固定Schema，明确每个任务的依赖关系、适配模型、验收标准、前置条件、超时时间、风险等级、优先级；必须将全局接口/消息定义任务设为最高优先级，优先执行；必须精准标记高安全风险模块的风险等级，宁严勿漏。
2. 智能体调度平台对任务清单完成复核，重点校验风险等级标记是否准确、依赖关系是否合理、有无循环依赖，复核不通过打回重写，复核通过进入下一环节。
3. 智能体调度平台 完成任务清单的最终前置校验：检查依赖是否存在、是否有循环依赖、模型是否在角色白名单内、验收标准是否明确、超时时间是否符合规范，校验不通过触发重写；校验通过后，正式接管全流程任务，通过 Claude Code SDK 将任务全包给 Claude Code Agent Teams，同时启动心跳检测、配额监控、状态同步机制。

**5.5 全局接口变更全流程闭环SOP（刚性执行，避免适配遗漏与返工）**

针对全局接口/消息定义变更的场景，执行以下刚性闭环流程，确保所有受影响模块同步适配，杜绝集成环节集中爆发问题导致的返工：

1. 全局接口变更必须走规范的变更流程，核心接口变更必须经人工确认，禁止AI自动触发接口变更。
2. 接口变更生效、全局规范更新后，智能体调度平台必须立即执行影响范围全量评估，通过「全局接口-依赖任务/模块」映射清单，梳理所有依赖该接口的任务，无论任务是否已完成、正在执行、待执行，全部标记为「待更新」状态。
3. 立即暂停所有依赖该接口的「正在执行/待执行」任务，避免基于过期的接口规范生成无效代码，浪费配额。
4. 明确责任主体与执行流程：智能体调度平台为唯一调度主体，逐个将变更内容、适配要求、完成时限推送给对应模块的负责claude code cli，明确适配要求。
5. 分级适配刚性规则：

￮ 若为接口字段新增、非核心参数调整，不影响原有逻辑的，要求对应claude code cli完成适配更新，完成后做简单校验即可；

￮ 若为接口结构修改、核心参数变更、输入输出逻辑调整，必须要求对应claude code cli重新修改代码，重新走「本地静态检查→安全校验→单模块测试」全流程，确保适配正确。

7. 全量闭环校验规则：所有受影响的模块全部完成适配并通过全流程校验后，方可恢复下游任务执行、进入后续的集成环节；智能体调度平台必须做100%全量校验，禁止有未完成适配的模块流入集成阶段。

**5.6 Claude Code Agent Teams协同开发阶段**

1. 优先执行最高优先级的全局接口/消息定义任务，完成后由智能体调度平台执行本地编译校验，确保所有模块依赖的接口文件提前生成，为后续单模块预编译提供基础；若接口有变更，立即触发5.5节的全流程闭环SOP。
2. Claude Code Agent Teams 基于任务清单的强依赖关系，通过 Hook 有序触发各Agent执行对应任务，无前置依赖的任务按并行管控规则并行执行。
3. 核心算法/ROS节点等C++代码模块完成编写后，按触发规则自动触发C++性能优化专项Agent完成高性能调优与硬实时性适配。
4. 单模块代码（含优化后代码）完成后，触发算法边界与安全校验Agent完成单模块校验：

￮ 静态检查与安全编码校验：调用Cppcheck/Clang-Tidy/Pylint/roslint完成语法、规范、安全漏洞检查，执行分层校验：工具自动校验+智能体调度平台脚本AST规则校验，覆盖实时循环约束、硬编码检测等工具无法覆盖的规则；

￮ 预编译校验：基于已完成的全局接口依赖，完成单模块代码预编译，验证语法正确性、依赖完整性、跨包引用正确性；

￮ 校验结果分级处理：阻断性错误直接打回对应Agent修改；警告性问题同步要求优化，不阻断流程但需闭环；

￮ 静态检查与预编译校验通过的，方可进入下游环节。

7. 审核/校验通过后，自动触发测试脚本生成Agent输出对应测试脚本、benchmark脚本、故障注入测试脚本。
8. claude code cli实例自动执行测试脚本，完成单元测试、性能测试、仿真场景测试，解析测试日志输出标准化测试结果；测试不通过的，必须把完整的错误日志、失败用例、报错堆栈信息整理后传给对应Agent，明确告知需要修复的问题，避免盲目修改；
9. 单模块测试通过后，智能体调度平台完成产物完整性校验，自动执行Git提交（按用户配置的提交规则执行），更新任务状态为已完成。
10. 循环执行上述流程，直到所有模块全部开发、校验、审核、测试完成；单个任务的代码修改/测试迭代最多3次，超过阈值立即触发人工介入审核。

**5.7 人工修改代码后的重校验SOP（刚性执行，全场景覆盖，避免安全风险与适配遗漏）**

无论是否重启项目，只要智能体调度平台检测到人工修改了已完成模块的代码/配置文件，立即执行以下分级重校验、重测、重审流程，杜绝修改导致的问题流入下游环节：

1. 检测到人工修改后，立即更新数据库中的产物信息、文件哈希值，记录修改内容、修改时间、修改文件路径，写入人工操作日志，全程可追溯。
2. 执行影响范围全量分析：不仅校验修改的文件本身，还要自动分析ROS包依赖关系、头文件引用关系、接口调用关系，梳理所有受影响的模块与任务，比如修改了全局接口头文件，必须识别所有依赖该接口的模块，无论是否已完成。
3. 执行刚性分级处理规则：

￮ 无影响修改：仅修改注释、非核心参数、不影响业务逻辑与安全的内容：仅执行本地静态检查，检查通过后更新产物状态与哈希值，无需重走全流程；

￮ 轻微影响修改：修改了非核心业务逻辑、不涉及接口变更与安全约束：必须重新走「本地静态检查→安全校验→单模块测试」全流程，全部通过后方可更新任务状态为已完成；

￮ 核心影响修改：修改了核心业务逻辑、算法实现、接口定义、安全约束：必须重新走全流程校验测试，若修改涉及接口变更，必须触发5.5节的全局接口变更闭环SOP，通知所有下游依赖任务，触发适配检查与修改；

￮ 高风险模块修改：修改了极高/高安全风险模块的任何内容，无论修改幅度大小，除了重走全流程校验测试，还必须重新触发强制人工安全审核，审核通过后方可更新状态，禁止跳过人工审核环节。

4. 批量修改处理规则：检测到用户批量修改多个模块、全局架构相关文件，立即暂停所有未执行的任务，先完成影响范围全量评估，由智能体调度平台本地脚本输出《人工修改影响范围报告》，经用户确认后，再逐个执行重校验与适配流程，避免遗漏。
5. 告警与同步规则：检测到人工修改核心逻辑、安全相关代码、全局接口后，立即推送通知给用户，明确修改的影响范围，确认后续执行策略；修改后的代码经校验测试通过后，自动同步更新双层记忆库，确保后续复用的是最新正确的内容。

**5.8 全局集成与系统评测阶段**

1. 所有模块开发与单模块测试全部完成后，智能体调度平台完成全量代码编译校验、多模块接口兼容性校验、ROS包规范校验、跨包引用校验、跨模块安全联动深度校验、隐式循环依赖检测。
2. 集成校验通过后，智能体调度平台生成跨模块集成安全测试、全链路安全场景测试脚本，由智能体调度平台本地执行，测试不通过的，定位问题模块并返回对应Agent修改。
3. 集成校验与跨模块安全测试通过后，自动触发系统评测模型，完成全系统全局深度评测、全链路实时性评测、核心算法专项评测、安全专项评测，输出结构化全局评测报告与优化建议，给出明确的终审结论。
4. 人类确认评测结果与优化建议，如需架构级优化则返回智能体调度平台迭代，如仅需局部代码优化则触发对应Agent修改，评测通过则进入人工仿真验证阶段。系统评测迭代最多2次，超过阈值强制进入人工确认环节。

【刚性规则】系统评测结果人工驳回后，根据确认的优化意见，区分为架构级/代码级问题，分别走对应架构迭代/代码修改流程，修改完成后必须重新执行集成校验→系统评测全流程，禁止直接进入人工验证环节。

**5.9 仿真验证与交付归档阶段**

1. 人类执行人工仿真验证、功能测试、算法效果验证、安全功能专项测试，验证代码可用性、功能完整性、性能达标情况、安全机制有效性；测试不通过则反馈问题。

【混合问题处理刚性规则】人工反馈的混合问题，先分级拆解闭环：

• 架构级问题：返回智能体调度平台走架构迭代全流程；

• 单模块代码问题：直接触发对应Agent修改，走对应校验测试流程；

• 多模块联动问题：先由智能体调度平台确认方案，再拆分到对应模块分别修改，完成后做集成联调测试；

• 所有问题必须明确跟踪编号，修改完成后必须重新提交人工验证，确保全部闭环，方可进入最终交付环节。

2. 人工仿真验证通过后，智能体调度平台完成全流程归档、版本变更日志生成。
3. 交付最终产物：按项目类型匹配标准化目录结构，输出完整ROS包/仿真环境文件、配套架构文档、接口说明、测试报告、版本日志、部署运行说明、安全审核记录、Git版本库，任务完成。

**5.10 中途需求变更处理SOP**

1. 人类中途打断任务，提出需求变更指令，智能体调度平台 立即暂停当前所有执行中的任务，记录暂停节点状态。
2. 需求澄清智能体完成需求变更内容梳理，更新结构化需求文档，明确变更范围与影响，提交人类确认。
3. 分级影响评估与处理：

￮ 核心需求变更（满足任意一项即为核心变更，与5.11节定义完全一致：①改变系统核心技术栈/架构模式；②新增/删除核心功能模块；③改变核心算法选型与技术路径；④变更核心性能指标、环境约束、安全要求；⑤局部变更影响3个及以上模块（含直接/间接影响）；⑥同时包含核心+局部变更的混合需求）：由智能体调度平台完成影响范围精细化评估，输出《需求变更影响范围评估报告》，明确「完全不受影响的模块、需局部适配的模块、需完全重构的模块」、预估配额消耗、工期影响、潜在风险，提交用户确认；用户确认后，不受影响的模块直接复用已有产物，需适配/重构的模块重新走对应流程，无需全量作废所有任务，最大化减少重复开发；核心架构变更的，从架构设计阶段开始完整执行，原有作废任务的产物归档保留。

￮ 局部需求变更：不影响核心架构、不改变技术栈、仅调整单模块内部逻辑、非核心参数/接口微调的，不影响其他模块的正常运行，由智能体调度平台完成影响评估与局部架构微调；调整完成后经合规性校验，智能体调度平台 更新对应任务清单，执行以下处理：

▪ 受影响的已完成任务，标记为「待更新」；

▪ 受影响的未执行任务，更新任务要求与依赖关系，按新要求执行；

▪ 未受影响的任务，保持原有状态与产物；

4. 需求变更执行完成后，重新走集成、评测、验证全流程，确保变更内容完整落地且不影响原有功能。
5. 所有需求变更、任务作废、产物更新操作，全部记录到操作日志与版本变更日志，全程可追溯。

**5.11 核心概念与统计口径刚性界定（全文档通用，一处定义全流程引用）**

为避免执行标准不统一、裁量权混乱，全流程所有核心概念执行以下刚性界定标准，所有章节统一引用，禁止自行定义。

**1. 需求变更等级界定**

• **核心需求变更**：满足任意一项即为核心变更，①改变系统核心技术栈/架构模式；②新增/删除核心功能模块；③改变核心算法选型与技术路径；④变更核心性能指标、环境约束、安全要求；⑤局部变更影响3个及以上模块（含直接/间接影响）；⑥混合变更同时包含核心与局部调整。

• **局部需求变更**：不影响核心架构、不改变技术栈、仅调整单模块内部逻辑、非核心参数/接口的微调，不影响其他模块的正常运行。

**2. 缺陷等级界定**

• **架构级缺陷（满足任意一项）**：①模块间接口设计错误、依赖关系矛盾；②系统整体架构无法满足性能/实时性/安全性要求；③多模块联动逻辑存在系统性缺陷；④技术栈选型与场景不匹配。

• **代码级缺陷**：单模块内部的代码实现错误、语法问题、逻辑漏洞、边界条件处理缺失，不影响整体架构与其他模块的正常运行。

**3. 高安全风险模块三级界定标准（宁严勿漏）**

| 风险等级     | 界定标准                                                     | 管控规则                                                     |
| ------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 极高安全风险 | 直接控制机器人急停逻辑、安全防护、故障兜底核心机制的代码模块 | 必须执行强制人工审核，任何核心修改必须重走全流程校验与重审   |
| 高安全风险   | 直接影响机器人运动安全、硬件安全的代码模块，包括：运动控制核心逻辑、避障算法、轨迹生成核心逻辑、机械臂/机器人力矩限制、关节控制、硬件驱动安全相关代码 | 必须执行强制人工审核，核心修改必须重走全流程校验与重审       |
| 一般风险     | 与安全相关但不直接输出控制指令的模块，包括：运动学解算、传感器数据预处理、状态估计中与安全相关的模块 | 完成AI安全校验与自动化测试即可，无需强制人工审核，仅当发现高风险问题时触发人工审核 |
| 低风险       | 工具函数、数据预处理、辅助脚本、非核心业务模块，无安全相关逻辑 | 本地校验+自动化测试即可，无额外安全管控要求                  |

• 兜底规则：架构拆解与任务规划智能体对风险等级标记负全责，边界模糊的模块一律按高安全风险模块标记；批判反思智能体对风险标记执行复核义务，杜绝漏标、错标。

**5. 人工确认节点超时规则**

• 全流程架构确认、高风险模块安全审核、评测结果确认、人工仿真验证4个关键人工节点，默认超时时限为24小时（支持用户在全局配置中心自定义调整）。

• 超时处理：达到超时时限未收到人工指令，智能体调度平台立即触发告警通知用户，同时暂停全流程中所有依赖该人工节点结果的任务，无依赖的已启动任务可执行完成，新任务不再分发；超时超过7天仍未处理的，自动将项目标记为「暂停状态」，关闭所有模型调用，用户需手动确认重启。

• 超时恢复规则：用户超时后给出确认/驳回指令后，智能体调度平台先校验全局架构、接口规范是否发生变化，更新相关任务上下文后，再恢复流程执行，禁止基于过期信息执行任务。

• 刚性约束：禁止无人工确认自动进入下一环节，保障机器人开发的高严谨性与安全性。

**6. 模型调用统计口径**

• 所有模型的每一次API调用（含重试、重写、迭代调用、主备模型切换调用），均计入套餐总调用次数配额；

**7. 任务超时时间默认标准**

架构拆解与任务规划智能体必须严格遵循以下默认超时时间设置，可根据任务复杂度微调，但不得超过上限：

| 任务类型                                    | 默认超时时间 | 超时上限 |
| ------------------------------------------- | ------------ | -------- |
| 全局接口定义、轻量代码实现、轻量bug修复任务 | 30分钟       | 60分钟   |
| 单模块编码、工程化实现、仿真工程化任务      | 60分钟       | 120分钟  |
| 核心算法开发、C++性能优化任务               | 90分钟       | 180分钟  |
| 架构设计、架构批判、系统评测、集成校验任务  | 120分钟      | 240分钟  |

**六、全量调用次数管控体系（基于Coding Plan Pro配额，从根源减少无效请求）**

**6.1 配额基准与统计口径（刚性执行）**

基于 Coding Plan Pro 订阅套餐的官方配额，明确以下统计规则：

• 官方配额基准：

￮ 每 5 小时：6,000 次请求（滚动窗口，自动恢复）

￮ 每周：45,000 次请求（每周一 00:00 重置）

￮ 每月：90,000 次请求（订阅日重置）

• 刚性统计口径：

b. 调用次数统计：以全模型共享的累计总调用次数为统计基准，同时新增单模型独立配额统计，对每个模型的调用次数单独监控，适配阿里云可能的分模型配额规则。

c. Token消耗统计：同步监控全量输入+输出Token累计消耗，匹配套餐Token限制规则，单次与累计Token消耗均纳入管控。

d. 多维度监控：同时监控「每5小时滚动窗口、每周、每月」三个时间维度，任一维度达到告警/熔断阈值，立即触发对应规则。

**6.3 配额精细化管控规则**

**6.3.1 三级阈值告警与熔断机制（优化阶梯式管控，彻底解决预留配额规则矛盾）**

智能体调度平台 实时监控全量+单模型API调用次数与Token消耗，针对三个时间维度分别设置三级刚性告警与熔断规则，配套刚性配额预留机制，确保核心任务优先保障。

| 时间维度  | 配额总量  | 一级告警（50%消耗）                                          | 二级告警（80%消耗，普通配额用尽，启用预留配额）             | 三级告警（100%消耗，全量配额用尽）                           |
| --------- | --------- | ------------------------------------------------------------ | ----------------------------------------------------------- | ------------------------------------------------------------ |
| 每 5 小时 | 6,000 次  | 轻度告警：推送配额消耗提示；并行上限降至3个；                | 中度告警：并行上限降至2个。                                 | 重度熔断：立即暂停所有大模型调用，推送紧急告警，等待滚动窗口配额自动恢复。 |
| 每周      | 45,000 次 | 轻度告警：推送周配额消耗提示；并行上限降至4个；              | 中度告警：并行上限降至2个。评估剩余任务配额缺口，提前预警。 | 重度熔断：立即暂停所有大模型调用，推送紧急告警，等待周一配额重置。 |
| 每月      | 90,000 次 | 轻度告警：推送月配额消耗提示；评估剩余任务配额缺口，提前预警。 | 中度告警：评估剩余任务配额缺口，提前预警。                  | 重度熔断：立即暂停所有大模型调用，推送紧急告警，等待订阅日配额重置。 |

**6.3.2 多维度告警优先级与联动管控规则**

1. 告警优先级刚性规则：当多个时间维度的告警同时触发时，以最严格的告警等级为准，执行对应最严格的管控规则。例如：周配额达到二级告警、5小时配额仅一级告警，统一执行二级告警对应的管控规则。
2. 全局联动管控规则：仅当某一维度达到二级及以上告警时，才执行全局统一的管控策略（降低并行任务上限、收紧调用频率限流）；一级告警仅做提示，不强制收紧全局管控规则，避免过度管控拖慢进度。
3. 规则自动降级机制：当某个维度的配额恢复（如滚动窗口刷新、周/月配额重置），智能体调度平台立即重新评估所有维度的告警等级，自动降级到对应等级的管控规则，同时推送配额恢复与规则调整的通知给用户。

**6.4 并行任务管控与请求平滑调度机制（避免限流与重试浪费）**

为避免短时间请求突增触发阿里云限流，导致重试浪费配额，执行以下刚性管控规则：

1. 并行数与调用限流联动机制：严格按照配额告警等级，动态调整并行任务上限与单分钟调用频率，同时按任务类型动态管控，联动规则如下（支持用户自定义调整）：

| 配额告警等级          | 最大并行任务数 | 单分钟最大调用次数 |
| --------------------- | -------------- | ------------------ |
| 无告警（配额充足）    | 5个            | ≤20次              |
| 一级告警              | 3个            | ≤15次              |
| 二级告警              | 2个            | ≤10次              |
| 三级告警/预留配额使用 | 1个            | ≤5次               |

**6.6 API调用异常处理与备用模型兜底规则（优化，减少无效重试与低质量输出）**

智能体调度平台 搭建统一API网关层，所有模型调用全部走该网关，统一处理异常，执行分级处理与兜底规则，平衡成功率与配额消耗：

**1. 错误类型分级处理策略（最小化无效重试）**

| 错误类型                                 | 处理策略                                                     |
| ---------------------------------------- | ------------------------------------------------------------ |
| 偶发网络错误、超时错误                   | 执行指数退避重试，初始间隔1s，倍数2，最多重试1次；连续2次超时直接切换备用模型，不再重试主模型 |
| 模型限流错误                             | 直接跳过重试环节，立即切换至该角色的备用模型兜底，同时推送限流告警通知，避免重试浪费配额 |
| 平台服务错误（5xx）                      | 仅重试1次，失败则切换备用模型                                |
| 格式校验不通过错误                       | 最多触发1次同模型重写，明确告知格式要求，失败触发人工介入；纯格式重写不计入代码迭代次数 |
| 内容合理性校验不通过错误                 | 计入任务迭代次数，打回对应Agent修改，遵循迭代次数管控规则    |
| 账号权限错误、配额用尽、模型下线致命错误 | 立即触发全局熔断，暂停所有任务，推送紧急告警给人工，禁止任何重试 |

**6.8 配额熔断后的恢复与重启规则**

• 每5小时维度熔断：配额自动恢复后，不直接自动重启所有任务，先推送恢复通知给用户，等待用户确认重启范围；若用户1小时内未确认，仅自动重启未完成的高优先级核心任务，非核心任务保持暂停。

• 每周/每月维度熔断：配额重置后，不会自动重启任务，必须由用户人工确认后，方可继续执行，避免大范围的无效执行。

• 重启后的断点校验：重启任务前，必须重新校验任务的前置依赖、全局规范是否发生变化，完成依赖链一致性校验，确认无误后再执行，避免基于过期的信息执行任务，引发无效配额消耗。

**七、安全管控规则（刚性执行，无死角全链路覆盖）**

**7.1 全链路安全机制专项刚性管控**

机器人系统的安全性是核心刚性要求，在全流程各环节强化安全管控，设置强制审核节点，全链路覆盖安全测试与评审，杜绝高风险代码绕过管控：

• **需求与架构层面**：需求澄清阶段必须明确安全约束指标、安全红线要求；智能体调度平台设计架构时，必须单独设计安全防护模块、急停逻辑链路、故障降级与兜底机制，明确安全边界；评审时，将安全性作为最高优先级评审维度，重点排查架构层面的安全漏洞、单点故障风险。

• **开发与编码层面**：所有编码Agent必须严格遵循安全编码规范，禁止使用不安全函数、存在内存风险的操作；核心控制代码必须添加双层极限值约束、异常兜底逻辑；工程化Agent必须实现节点异常捕获、故障安全降级机制，异常状态下自动触发安全操作。

• **单模块校验层面**：智能体调度平台，针对高/极高安全风险模块，必须做专项全场景覆盖校验，包括极限值测试、异常工况测试、边界条件全覆盖、急停逻辑分支全验证，输出专项安全校验报告，作为人工审核的必备材料，校验不通过禁止流入下游环节。

• **强制审核层面**：

￮ 极高/高安全风险模块完成单模块校验后，必须先完成单元测试，审核通过后方可进入后续环节；

￮ 审核必查项刚性要求：审核必须逐项校验：急停逻辑全分支覆盖、运动控制极限值约束、异常工况兜底机制、边界条件处理、安全编码规范符合性、测试覆盖情况；

￮ 审核迭代限制：单个高风险模块的审核驳回修改，最多允许3次迭代，超过3次仍未通过审核的，触发架构级评审；

￮ 存档与追溯规则：审核必须输出明确的审核记录，包含审核模块、对应task_id、代码版本、审核时间、审核人、审核结论、详细修改意见；所有审核记录必须持久化存入本地数据库，与对应任务的代码产物、版本哈希值强绑定；最终交付时，必须将所有高风险模块的审核记录，作为交付物的一部分存入docs目录。

• **测试验证层面**：智能体调度平台必须针对安全机制，生成故障注入测试脚本、急停触发测试脚本、极限工况测试脚本、异常数据注入脚本，全面验证安全机制的有效性；测试不通过的直接打回修改，禁止流入集成环节。

• **集成与联动校验层面**：智能体调度平台必须做跨模块安全联动深度校验（基础层接口校验+逻辑层全链路安全机制完整性校验+边界层多模块联动边界条件校验）；完成校验后，必须生成跨模块集成安全测试、全链路安全场景测试脚本，通过自动化测试验证安全联动逻辑的有效性，测试不通过禁止进入系统评测环节。

• **全局评测层面**：智能体调度平台必须做安全专项评测，对全系统的安全架构、安全机制覆盖度、风险点、故障兜底能力做全面评审，输出专项安全评测报告；存在致命安全隐患的，必须返回架构层迭代优化，禁止通过评审。

**7.2 安全编码规范与分层校验机制（刚性执行，确保落地）**

**（1）统一安全编码规范**

所有编码类Agent必须严格遵循以下安全编码规范，本地校验环节将强制校验，不符合规范的直接打回修改：

1. **C++安全编码规范**：强制遵循《Google C++ Style Guide》，同时符合ROS C++编码规范；禁止使用不安全的函数（如strcpy、sprintf、gets等）、存在内存溢出、数组越界、空指针解引用风险的操作；必须处理所有异常情况、边界条件，禁止硬编码的魔法数字；运动控制相关代码必须添加双层极限值约束、异常兜底逻辑；实时性代码必须遵循硬实时开发规范，禁止在实时循环内执行风险操作。
2. **Python安全编码规范**：强制遵循PEP8规范，同时符合ROS Python编码规范；禁止使用不安全的代码写法、存在代码注入风险的操作；完善异常处理机制，禁止裸except捕获所有异常，必须明确异常类型与兜底处理逻辑。
3. **ROS/ROS2代码安全规范**：强制遵循ROS/ROS2官方安全开发规范，话题、服务、参数的命名与权限配置符合安全要求；节点必须实现异常状态的安全降级，关键节点配置自动重启机制；ROS2节点必须规范实现生命周期管理，异常状态下有明确的安全处理逻辑。
4. **算法安全规范**：核心算法必须处理所有边界条件、异常输入、传感器数据失效场景，有明确的故障降级与安全兜底机制；禁止在异常数据下输出危险的控制指令，必须做输入合法性校验、输出限幅约束。

**（2）安全编码分层校验机制（精准校验，可落地执行，替代正则方案）**

针对通用工具无法覆盖的安全规则，补充三层校验机制，确保规范100%落地，杜绝漏判误判：

• **第一层：本地工具自动校验**：通过Cppcheck、Clang-Tidy、Pylint、Roslint完成通用语法、安全漏洞、规范符合性校验；Cppcheck/Clang-Tidy开启全量安全检查规则，包括内存泄漏、空指针、缓冲区溢出、不安全函数调用、实时循环违规操作检测等，使用标准化配置文件，确保校验规则统一。

• **第二层：****智能体调度平台****脚本AST规则校验**：基于代码AST抽象语法树解析，针对工具无法覆盖的规则，做自动校验，可落地实现的校验规则包括：

a. 实时循环内的动态内存分配检测、阻塞调用检测、高频日志打印检测；

b. 硬编码的极限值/魔法数字检测、安全参数硬编码检测；

c. ROS跨包引用规范符合性检测、头文件依赖完整性检测；

d. ROS2 QoS配置合规性检测、生命周期管理规范符合性检测；

e. 不安全函数、风险操作的补充检测。

技术实现方案：基于Python的libclang库实现C++代码AST解析，基于Python原生ast模块实现Python代码AST解析，确保规则可落地执行。

• **第三层：算法边界与安全校验Agent的逻辑校验**：针对安全兜底机制、边界条件处理、异常分支覆盖、急停逻辑完整性，做业务逻辑层面的校验，确保安全规范落地，校验不通过直接打回修改。

**八、ROS工程化与实时性适配优化（补全所有落地细节，减少后续修改请求）**

**8.1 ROS包结构与依赖管理完整规范**

**（1）ROS包分包与结构规范**

• **分包分级适配原则（明确量化界定标准）**：

￮ 大型项目：包含≥5个核心功能模块、跨3个及以上技术域（如感知+规划+控制+仿真）、多机器人协同系统，严格按功能模块划分独立的ROS包，遵循「高内聚、低耦合」原则，比如：robot_bringup（启动包）、motion_control（运动控制）、path_planning（路径规划）、obstacle_avoidance（避障算法）、robot_simulation（仿真相关），禁止多个不相关的功能混在一个包里。

￮ 中小型项目：核心功能模块≤4个，技术域集中，可按功能域合并分包，允许单个ROS包内包含多个关联节点，避免过度分包导致的工程复杂度提升。

￮ 单节点、工具类功能：无需单独分包，可纳入通用工具包，避免冗余的包结构。

• **ROS包标准结构刚性要求**：每个ROS包必须严格遵循ROS/ROS2官方标准目录结构：

￮ C++节点：必须包含include/包名/（头文件）、src/（源码）、launch/、config/、msg//srv//action/（自定义接口）、CMakeLists.txt、package.xml；

￮ Python节点：必须包含规范的包结构、scripts//py_package_name/、launch/、config/、package.xml、[setup.py](setup.py)（ROS2）。

**（2）ROS包依赖管理与跨包引用刚性规范**

为确保生成的ROS包可直接编译通过，无依赖缺失、路径错误问题，执行以下规范，本地校验环节将强制检查：

1. **package.xml编写规范**：必须严格遵循ROS/ROS2官方标准，准确区分build_depend、buildtool_depend、exec_depend、test_depend，禁止冗余依赖、缺失依赖；必须明确标注依赖的最低版本要求，避免版本兼容问题；必须填写完整的包信息（名称、版本、描述、维护者、许可证）。
2. **CMakeLists.txt编写规范**：严格遵循CMake与ROS的最佳实践，明确编译标准（C++17及以上）、头文件目录、链接库、安装规则，确保编译后的节点、库文件、launch文件、配置文件能正确安装到install目录；禁止硬编码路径，必须使用ROS提供的标准宏与变量；跨包引用的头文件、库必须正确配置依赖，确保编译时能找到。
3. **跨包引用刚性规范**：

￮ 跨ROS包的C++头文件引用，必须使用<包名/头文件名.h>的ROS标准格式，禁止使用相对路径引用其他包的头文件；

￮ 跨包依赖必须在package.xml中明确添加对应依赖包的build_depend和exec_depend，禁止隐式依赖；

￮ CMakeLists.txt中必须正确配置find_package查找依赖包，添加对应的头文件目录与链接库配置；

￮ 本地预编译校验、集成编译校验环节，必须专项检查跨包引用与依赖配置的正确性，编译失败直接打回修改。

**8.2 ROS2专属完整规范（符合官方最佳实践）**

在工程化代码实现Agent职责、统一编码规范中，强制执行ROS2开发完整规范：

1. **QoS策略刚性要求**：

￮ 实时控制指令（运动控制、急停指令）：必须使用RELIABLE可靠性、VOLATILE持久性、深度≤10、短deadline，确保指令实时、可靠传输；

￮ 高频传感器数据（激光雷达、IMU、里程计）：根据场景选择BEST_EFFORT（高频低延迟）或RELIABLE，合理设置队列深度，避免内存溢出；

￮ 配置/管理类服务：使用RELIABLE可靠性，确保请求响应不丢失。

2. **节点生命周期管理要求**：ROS2实时控制、安全相关的节点，必须规范实现lifecycle_node生命周期状态机，完整处理配置、启动、停止、错误、关闭全状态转换，异常状态必须有明确的安全降级/急停处理逻辑。
3. **组件化开发分级规范（明确适用场景）**：

￮ 强制使用场景：多节点间需要低延迟通信、同进程部署、实时控制相关的节点，必须使用组件化开发；

￮ 推荐使用场景：功能关联紧密的多个节点，可通过组件化减少进程间通信开销；

￮ 不强制场景：独立的工具节点、非实时的辅助节点、单节点功能，允许使用传统节点写法，不强制组件化，避免不必要的工程复杂度。

￮ 代码编写规范：组件类必须继承rclcpp::Node，使用RCLCPP_COMPONENTS_REGISTER_NODE宏完成组件注册；禁止硬编码节点名称，支持通过launch文件配置命名空间、参数、QoS策略。

￮ 编译配置规范：CMakeLists.txt中必须正确添加rclcpp_components依赖，使用rclcpp_components_register_node宏注册节点，生成对应的插件描述文件，明确库文件、插件描述文件的安装路径，确保编译后文件能正确安装到install目录。

￮ package.xml规范：必须添加rclcpp_components、composition的依赖声明，明确组件运行的依赖包。

￮ launch文件规范：组件化节点的launch文件，必须使用ROS2标准的ComposableNodeContainer和LoadComposableNodes写法，支持同进程部署、参数与命名空间配置。

4. **命名空间规范**：必须规范使用节点命名空间、话题命名空间，避免硬编码的绝对命名，支持通过launch文件配置命名空间，适配分布式部署场景。
5. **参数管理规范**：使用ROS2的参数声明机制，明确参数类型、默认值、取值范围、描述信息，支持运行时动态重配置；必须处理参数变更的回调，支持参数动态更新后的逻辑适配，禁止硬编码参数。
6. **launch文件规范**：使用ROS2标准的Python launch文件，遵循模块化、可复用的设计原则，分模块编写launch文件，支持参数、命名空间、仿真环境的灵活配置；禁止在一个launch文件中堆砌所有节点，不利于维护与扩展。

**8.3 硬实时系统适配完整规范（补全核心落地细节）**

在C++性能优化专项Agent职责、实时性测试要求中，补充硬实时系统适配的完整刚性规范：

1. **线程优先级分级刚性规则**：明确“安全优先级最高、控制次之、感知再次之”的优先级排序原则，采用适配Linux PREEMPT_RT实时内核的相对优先级区间，避免与内核中断优先级冲突，支持用户自定义调整：

￮ 急停安全处理线程：优先级90-98（最高）；

￮ 实时控制循环线程：优先级80-89；

￮ 传感器数据处理线程：优先级60-79；

￮ 非实时的管理、日志、UI线程：优先级低于50。

2. **实时循环刚性约束**：实时控制循环必须做到执行时间可预测，严格禁止以下操作：

￮ 禁止在实时循环内执行任何动态内存分配/释放（new/delete、malloc/free）；

￮ 禁止在实时循环内执行任何阻塞调用、系统调用、文件IO操作；

￮ 禁止在实时循环内打印DEBUG/INFO级别的常规日志；

￮ 循环内的逻辑必须精简，非实时的计算、处理逻辑必须放到非实时线程中执行。

3. **内存锁定规范**：必须在程序初始化阶段执行mlockall(MCL_CURRENT | MCL_FUTURE)锁定所有内存，禁止运行时出现内存换页；实时线程使用的栈内存、数据缓冲区，必须提前预分配，禁止运行时动态申请；明确说明权限依赖（需要root权限或相应的memlock权限配置）。
4. **优先级翻转规避规范**：必须使用优先级继承的互斥锁（PTHREAD_PRIO_INHERIT），禁止使用普通的互斥锁，避免优先级翻转导致的实时性问题；实时循环内尽量避免锁竞争，采用无锁数据结构实现线程间通信。
5. **编译优化规范**：开启适配实时场景的编译优化选项，确保代码执行效率，减少不必要的指令开销；明确编译标准、优化级别、平台适配选项。
6. **分场景刚性实时性验收标准**：

￮ 极高实时性要求（人形/四足机器人关节控制、机械臂力控）：控制周期抖动≤50us，最大延迟不超过控制周期的5%；

￮ 高实时性要求（运动控制、轨迹跟踪、急停逻辑）：控制周期抖动≤100us，最大延迟不超过控制周期的10%；

￮ 一般实时性要求（路径规划、传感器数据处理、导航）：处理延迟≤1ms，无强制抖动要求；

￮ 非实时场景（日志、诊断、参数管理）：无硬性实时性要求。

7. **测试与校验要求**：测试脚本生成Agent必须生成对应的实时性专项测试脚本，统计控制周期的最大/最小/平均延迟、抖动分布，输出标准化测试报告，明确是否达到验收标准，不达标则返回优化环节重新调整。
8. **实时内核适配说明**：补充基于Linux PREEMPT_RT实时内核的配置要求、权限配置步骤、环境验证方法，确保用户可落地执行。

**8.4 多传感器数据融合场景适配规范**

在核心算法代码编写Agent职责、校验与测试规则中，补充刚性开发规范：

1. **时间同步规范**：多传感器数据融合必须使用ROS官方的message_filters库实现时间同步，根据场景选择合适的同步策略（精确时间同步、近似时间同步），禁止自行实现简易的时间对齐逻辑，确保同步精度与稳定性。
2. **数据有效性校验规范**：必须对每帧传感器数据做有效性校验，包括时间戳有效性（是否超时、是否乱序）、数据完整性、异常值过滤（离群点、超出量程的数据），无效数据必须做兜底处理，禁止直接传入融合算法。
3. **丢包与延迟处理规范**：必须处理传感器数据丢包、延迟过大的场景，设置合理的超时阈值，超时后必须触发对应的降级处理逻辑，禁止使用过期数据做融合计算，避免输出错误的结果。
4. **校验与测试要求**：算法边界与安全校验Agent必须针对融合模块，重点校验时间同步逻辑、异常数据处理、边界条件覆盖的完整性；测试脚本生成Agent必须生成对应的测试脚本，包括时间同步精度测试、异常数据注入测试、丢包场景鲁棒性测试，验证算法的稳定性与精度。

**8.5 ROS节点日志与可观测性规范**

在工程化代码实现Agent职责、统一编码规范中，补充刚性规范：

1. **日志级别使用规范**：严格遵循ROS日志级别定义，禁止滥用日志级别：

￮ DEBUG：仅用于开发调试的详细信息；

￮ INFO：用于关键状态变化、正常流程节点、重要事件通知；

￮ WARN：用于非致命异常、可恢复的警告、非预期但不影响功能的情况；

￮ ERROR：用于功能异常、执行失败、错误事件，需要关注与修复的问题；

￮ FATAL：用于系统致命错误、安全相关的紧急事件，会导致系统停止运行的情况。

2. **高频循环日志禁令**：严格禁止在实时控制循环、高频数据回调函数中，打印DEBUG/INFO级别的常规日志；仅允许在出现异常、警告时，打印有限次数的错误日志，避免高频日志导致的CPU占用飙升、实时性下降。
3. **关键信息日志要求**：节点启动/关闭、参数加载/更新、模式切换、安全状态变化、异常触发等关键事件，必须输出明确的日志，包含关键参数、状态信息，确保可追溯。
4. **诊断信息规范**：核心控制、安全相关的节点，必须发布标准化的诊断信息（ROS1的diagnostic_msgs、ROS2的diagnostic_updater），上报节点运行状态、健康度、关键指标，方便系统监控与故障排查。

**8.6 算法参数标准化管理规范**

在核心算法与工程化Agent职责、输出规范中，补充刚性要求：

1. **参数管理刚性要求**：

￮ 核心算法、控制逻辑的所有可调整参数，必须统一通过ROS参数服务器、独立的yaml配置文件管理，绝对禁止在代码中硬编码参数；

￮ 配置文件必须按模块分类，结构清晰，方便用户查找与修改；

￮ 每个参数必须添加详细的注释，包括：参数物理含义、计量单位、推荐取值范围、调优说明、安全限制、默认值；

￮ 必须设置参数的安全极限值，代码中必须对用户输入的参数做范围校验，超出安全范围的自动限制到合理区间，避免参数设置错误导致的安全风险。

2. **默认值要求**：必须设置合理的默认参数值，确保默认配置可直接在仿真环境中正常运行，无需用户额外修改即可验证基础功能。
3. **交付文档要求**：最终交付的文档中，必须包含核心参数说明、调优指南，针对不同场景给出参数调整建议，方便用户根据实际机器人平台、应用场景适配调整。

**九、异常处理与迭代终止规则（补全所有盲区，全场景覆盖）**

**9.1 常规异常处理流程**

基于缺陷等级界定标准，执行分级异常处理，确保问题精准闭环，不扩大影响范围，减少无效迭代：

• **代码级缺陷异常**：单模块代码实现错误、静态检查不通过、校验不通过、测试不通过，直接回流给对应编码Agent修改，同步更新迭代次数，触发迭代次数限制规则。

• **架构级缺陷异常**：测试/集成/评测环节发现架构设计层面的系统性缺陷，由智能体调度平台完成架构迭代优化，再重新拆解任务执行。

• **集成兼容性异常**：多模块集成校验不通过，定位问题根因，若为单模块代码/接口问题，回流给对应Agent修改；若为接口设计架构级问题，上报智能体调度平台调整。

• **系统评测优化异常**：系统评测提出的优化需求，经人类确认后，架构级问题由智能体调度平台迭代架构，代码级问题直接触发对应Agent修改，重新执行对应流程。

• **局部架构适配异常**：局部架构调整后，经合规性校验评审不通过的，返回局部架构适配Agent修改，超过2次迭代仍不通过的，升级为智能体调度平台处理。

• **任务失败异常**：单任务执行失败、校验不通过，立即暂停该任务下游所有依赖任务，推送告警通知，明确失败原因与处理方案，问题闭环后自动恢复下游任务执行。

• **接口变更适配异常**：全局接口变更后，下游模块适配失败，立即标记任务为失败，推送告警，明确适配要求，重新触发对应Agent修改。

**9.2 任务超时与流程停滞处理机制**

1. **超时判定标准**：智能体调度平台 基于任务清单中每个任务设定的超时时间（严格遵循5.11节默认标准），实时监控任务执行状态；任务进入「执行中」状态后，超过设定的超时时间未返回完成结果，即判定为任务超时。
2. **分级超时处理规则**

￮ 首次超时：触发指数退避重试，重新推送任务给对应主模型执行，最多重试1次，同时记录超时日志；

￮ 重试后仍超时：自动切换到备用模型执行1次，同时推送告警信息给用户；

￮ 备用模型执行仍超时：立即将任务状态置为「已阻塞」，触发任务阻塞紧急告警，暂停该任务下游的所有依赖任务，等待人工介入处理，禁止无限重试。

3. **Hook失效兜底检测机制**：设置双重超时检测+状态轮询机制，任务分发后，超过预期执行时间的2倍，仍未收到Hook的状态更新打点，无论Agent是否在执行，均触发超时告警；智能体调度平台主动查询Claude Code中该任务的执行状态，若确认任务已完成，手动拉取产物、完成校验、触发下游流程；若任务卡住、执行失败，按超时规则处理。
4. **全流程心跳兜底机制**：智能体调度平台的全局心跳检测，不仅检测Claude Code的运行状态，还会遍历所有进行中的任务，检查状态更新是否正常，识别长时间无状态变化的停滞任务，主动触发告警与处理，避免静默停滞。

**9.3 任务失败与依赖管控规则**

1. **影响范围刚性管控**：任务被标记为「已失败/已阻塞/待更新」后，智能体调度平台立即暂停所有直接/间接依赖该任务的下游任务，同时推送任务失败告警给用户，明确失败原因、影响范围、建议处理方案。
2. **失败闭环处理规则**：

￮ 可修复的代码/内容问题：直接返回对应Agent修改，计入迭代次数，修改完成后重新执行全流程校验；

￮ 不可重试的系统异常：触发人工介入，由人工确认处理方案后，再继续执行；

￮ 依赖缺失/接口不匹配问题：定位根因后，若为上游任务问题，返回上游任务修改；若为架构设计问题，上报智能体调度平台处理。

3. **失败恢复机制**：失败任务修改完成、重新标记为已完成后，智能体调度平台自动唤醒下游依赖的任务，从断点继续执行，无需人工干预流程调度。

**9.4 循环依赖与死锁检测处理机制**

1. **静态循环依赖检测**：架构拆解输出任务清单后，智能体调度平台必须先做依赖关系校验，通过拓扑排序检测是否存在循环依赖，存在循环依赖的，直接打回架构拆解智能体，重新修正任务依赖关系，禁止流入调度环节。
2. **隐式循环依赖检测**：在集成校验环节，智能体调度平台必须基于ROS包依赖、头文件引用、服务调用关系，扫描模块间的隐式循环依赖，提前规避编译与运行时风险，发现后立即输出问题清单，触发对应模块修改。
3. **动态死锁检测**：任务调度过程中，智能体调度平台实时监控所有任务的等待状态，若任务等待依赖任务的时长超过自身超时阈值的50%，立即主动检查依赖任务的执行状态；若出现多个任务相互等待、循环依赖导致的死锁，立即触发告警，暂停所有相关任务，推送死锁详情与影响范围给用户，等待人工介入处理。
4. **依赖有效性校验**：除了循环依赖，还要校验依赖的task_id是否真实存在，避免依赖不存在的任务，导致任务永久停滞。

**9.5 人工干预流程与规则**

明确任务执行过程中，人工干预的完整处理规则：

1. 支持的人工干预指令：暂停全流程任务、终止单个任务、终止全流程项目、修改任务参数、跳过已完成的任务、调整配额管控规则、强制修改全局规范、自定义配置参数调整。
2. 暂停处理规则：收到人工暂停指令后，智能体调度平台立即停止分发新的任务，等待当前正在执行的任务完成后，暂停全流程，保存当前所有状态，推送暂停成功的通知；禁止强制中断正在执行的模型调用，避免状态不一致。
3. 终止任务规则：收到单个任务终止指令后，若任务未执行，直接标记为已取消；若任务正在执行，等待本次调用完成后，终止后续流程，标记为已取消，同时暂停其下游依赖任务；收到全流程终止指令后，停止所有任务调度，完成当前执行中的任务后，终止全流程，归档所有数据。
4. 干预后的恢复规则：暂停后的任务，收到人工恢复指令后，从暂停的断点继续执行，无需重复执行已完成的环节。

**9.6 断点续跑执行规则（补全全局变更校验，避免过期信息复用）**

1. **重启一致性与变更校验规则**：智能体调度平台 重启后，首先读取本地SQLite数据库中的持久化数据，执行以下全量校验流程：

￮ 第一步：校验每个任务节点的完成状态、产物完整性，做双重校验——不仅校验数据库中的状态标记，还要校验对应产物文件是否存在、文件哈希值是否与数据库记录一致、是否通过了下游环节校验；

￮ 第二步：校验全局信息是否发生变更，检查「全局需求文档、架构设计、接口规范」在中断期间是否被人工修改，若有变更，先完成影响范围全量评估，更新所有相关任务的要求与上下文；

￮ 第三步：依赖链一致性校验，校验已完成任务的产物变更，是否影响下游已完成/待执行的任务，若上游核心内容变更，自动标记下游相关任务为「待重校验/待更新」；

￮ 第四步：输出《断点评估报告》，明确已完成的任务、待执行的任务、检测到的变更与影响范围、建议执行策略，由用户确认后，再继续执行。

2. **人工修改检测与同步**：

￮ 若任务中断期间，人工修改了已完成节点的产物，以人工修改的本地文件为准，更新数据库中的产物信息、文件哈希值、修改记录，同时写入人工操作日志；

￮ 若修改仅影响模块内部逻辑，自动标记该模块需要重新执行校验、测试流程；若修改涉及接口定义、输出格式变更，立即触发5.5节的全局接口变更闭环SOP，通知所有相关任务，触发适配检查与修改。

3. **断点定位与执行规则**：以「最近一个100%完成、且通过下游环节校验的节点」为断点起点，重启后仅执行该节点之后的未完成任务，已完成且校验通过的节点，直接复用历史产物，不再重复调用模型，杜绝配额浪费。
4. **特殊节点处理规则**：若中断前处于人工确认/审核环节，重启后保持等待状态，重新推送确认提醒给用户，禁止自动跳过人工环节；若中断前处于熔断暂停状态，重启后保持暂停，等待配额恢复或人工指令。
5. **长时任务会话失效兜底**：若Claude Code会话失效，智能体调度平台自动创建新会话，从数据库中读取最新的任务状态、全局约束、已完成产物，重新推送任务，从断点继续执行，无需人工干预；同时明确告知Agent已完成的任务与待执行内容，禁止重复生成已完成的代码。

**9.7 本地持久化与数据安全机制**

1. **数据库并发优化**：SQLite数据库默认开启WAL（预写日志）模式，提升并发写入性能，解决多任务并行写入的锁冲突问题；新增写入队列机制，所有数据库写入操作统一进入串行队列，避免多线程并发写入导致的锁竞争与数据损坏。
2. **异常捕获与处理**：智能体调度平台的每一次数据库写入、文件保存操作，都做完整的异常捕获，写入失败时立即触发告警，暂停非核心任务，保留内存中的关键状态，避免程序直接崩溃。
3. **降级备份机制**：单次数据库写入失败，立即重试1次；连续3次写入失败，立即触发告警，暂停新的模型调用，同时自动将内存中的关键状态数据写入本地临时JSON文件，做兜底备份，避免数据丢失。
4. **自动备份机制**：

￮ 增量备份：每完成一个关键节点（架构确认、单模块完成、集成校验通过、最终交付），自动备份完整的数据库文件与项目产物，保留最近5个版本的备份；

￮ 定时备份：项目运行期间，每2小时自动备份一次数据库文件；

￮ 最终交付备份：项目最终交付时，自动打包全量项目数据、代码、文档、日志、数据库，生成归档备份文件，永久留存。

5. **数据库维护规则**：项目交付完成后，自动对数据库执行VACUUM优化，整理碎片、压缩体积；运行期间，每月自动执行一次数据库优化；每次启动时，执行数据库完整性检查，若出现损坏，自动从最近的备份文件恢复，同时扫描本地临时兜底文件，补充恢复缺失的进度数据。
6. **磁盘空间监控**：智能体调度平台启动时与运行过程中，实时监控磁盘剩余空间、文件系统权限，空间不足时提前触发告警，禁止写入操作，避免数据库文件损坏、写入不完整。

**9.8 日志管理与磁盘空间保护规则**

1. **日志存储规范**：明确统一的存储路径，数据库文件固定存放在项目根目录的.data/db/目录，本地文本日志存放在项目根目录的data/logs/目录，禁止散落在项目各处。
2. **日志分级与关键字段规范**：

￮ 严格遵循DEBUG/INFO/WARN/ERROR/FATAL五级日志级别，禁止滥用；

￮ 所有日志必须包含固定关键字段：时间戳、模块名、任务ID、角色名、模型名、操作内容、错误详情、请求ID，确保可精准追溯；

￮ API调用日志必须完整记录：请求时长、调用结果、错误码，便于问题排查。

3. **日志轮转与归档规则**：

￮ 本地文本日志按天轮转拆分，单日日志过大时按大小拆分；

￮ 最近30天的日志文件保留原文，超过30天的自动压缩归档；

￮ 超过90天的归档日志自动清理，释放磁盘空间。

4. **磁盘空间监控与清理**：智能体调度平台实时监控日志与数据库所在磁盘分区的剩余空间，当剩余空间低于10%时，立即触发告警，同时自动清理过期的归档日志，避免磁盘占满导致程序异常。
5. **问题排查支撑**：在项目README、运行手册中，明确日志的存储位置、分类、查询方法，配套常见问题排查指南，方便用户快速定位问题。

**9.9 Git版本管理完整规则（补全人工修改适配、冲突处理、兼容性优化）**

**（1）权责分工明确**

• 智能体调度平台脚本：Git操作的唯一执行主体，负责仓库初始化、代码自动提交、分支管理、版本标记、冲突检测与告警的自动化执行，严格遵循用户配置的分支策略与提交规则。如果claude code cli实例未能输出提交规范、版本号规则、CHANGELOG内容，智能体调度平台则阅读代码自行总结版本号规则、CHANGELOG内容等信息并推送git

• claude code cli实例：负责输出提交规范、版本号规则、CHANGELOG内容，为智能体调度平台的Git操作提供标准化输入，不直接执行Git操作。

**（2）刚性执行规则（可配置化，兼顾灵活性与兼容性）**

1. **仓库初始化规则**：项目启动时，若目录无Git仓库，自动初始化；若已有仓库，沿用现有仓库，不强制覆盖，尊重用户已有的Git管理体系，不擅自修改已有分支、提交记录。
2. **分支管理可配置规则**：

￮ 将分支策略纳入全局配置中心，支持用户自定义开发分支、保护分支名称，而非固定main+dev；

￮ 提供3种可选择的分支策略适配模式：

i. 默认双分支模式：main（稳定交付分支）+ dev（开发分支），开发过程中所有自动提交均提交到dev分支，最终交付验收通过后，合并到main分支并创建版本Tag；

ii. Gitflow兼容模式：适配master/develop/feature/release分支体系，自动提交到对应开发分支；

iii. 用户自定义模式：完全按用户配置的分支名称执行提交，禁止擅自切换分支。

￮ 保护分支刚性约束：用户标记的保护分支（如main/master），禁止自动提交、自动合并，仅在最终交付经用户确认后，方可执行合并操作，避免破坏用户的稳定分支。

3. **自动提交可配置规则**：

￮ 提供两种提交模式，支持用户在全局配置中心选择，默认采用里程碑节点合并提交模式，减少碎片化提交：

ⅰ. 单模块完成自动提交模式：单模块完成「开发-校验-测试」全流程，标记为已完成后，自动执行Git提交，提交信息固定规范为：[auto-commit] 完成任务：[task_id] - [task_name]，确保提交记录可追溯、与任务一一对应；

ⅱ. 里程碑节点合并提交模式：在架构确认、全模块开发完成、集成校验通过、最终交付这4个核心里程碑节点完成后，执行统一自动提交，减少碎片化提交，保持Git历史整洁。

￮ 全局开关：支持用户一键关闭自动提交功能，完全由人工控制Git提交操作。

￮ 提交内容范围刚性规范：自动提交仅包含代码源文件、配置文件、launch文件、文档、测试脚本等项目核心产物；临时文件、日志文件、编译产物、模型文件等，自动添加到.gitignore，禁止提交。

4. **人工修改的检测与处理规则**：

￮ 智能体调度平台在执行自动提交前，必须先检查Git工作区的修改内容，若检测到用户未提交的人工修改，先暂停自动提交，推送通知询问用户是否将人工修改一并提交，禁止自动覆盖、丢弃用户的人工修改。

￮ 检测到人工修改了已提交的代码，在更新数据库产物信息的同时，提示用户提交Git修改，确保Git版本与本地产物一致。

5. **合并与冲突处理规则**：

￮ 最终交付验收通过后，开发分支合并到保护分支前，必须先做合并冲突预检查；

￮ 若存在合并冲突，立即暂停合并操作，推送冲突告警给用户，必须由人工解决冲突后，再执行合并操作，禁止强制合并、自动覆盖冲突内容。

6. **标签管理规范**：最终交付的版本Tag，严格遵循语义化版本号规则，Tag信息必须包含版本号、交付日期、核心变更内容，便于版本追溯；Tag创建后禁止自动修改、删除，仅允许人工操作。
7. **历史版本保护规则**：保护分支的历史提交、已发布的Tag，禁止自动修改、覆盖、重写，仅允许人工操作，避免自动提交导致的历史版本丢失、追溯失效。
8. **版本回滚与流程适配SOP**：

￮ 架构版本回滚流程：当架构迭代出现问题，需要回滚到上一版已确认的架构时，由总架构师输出回滚后的架构文档，经用户确认后，智能体调度平台基于Git回滚对应架构产物，重新执行任务拆解、影响范围评估，标记需要重新开发/适配的任务，复用不受影响的模块代码，避免全量返工。

￮ 单模块代码回滚流程：若单模块修改后出现问题，需要回滚到上一版本，智能体调度平台执行Git回滚后，重新触发本地校验、测试流程，无需重新编码，减少API调用。

**十、智能体统一输出规范（刚性执行，减少返工与无效调用，平衡规范与成本优化）**

**10.1 通用基础规范（所有Agent必须严格遵守）**

1. 所有输出必须结构清晰、章节固定，禁止无结构的自由文本输出，禁止输出与任务无关的内容。
2. 所有结构化内容必须严格遵循固定字段与格式，禁止新增非规定字段、修改字段名称、变更数据类型。
3. 代码输出必须完整可执行，禁止截断、伪代码、省略核心逻辑，必须包含完整的依赖导入、注释、异常处理、边界条件处理。
4. 所有输出必须严格遵循全局约束、接口规范、编码标准，禁止偏离架构设计与需求要求。
5. 迭代修改的输出，必须明确标注修改原因、对应问题单号、修改前后的核心变化，确保可追溯，同时必须先列出所有待修复问题，再逐一说明修复情况，确保一次性闭环。
6. 输出Token刚性约束：禁止输出任何与任务无关的开场白、寒暄语、解释性内容、结束语，直接输出规定的核心内容；代码类任务禁止输出大段原理说明，仅保留代码与规定的说明字段。
7. 术语统一规范：全文档统一使用中文术语，括号标注英文，禁止中英文混用导致的歧义，核心术语严格遵循5.11节的定义。
8. **分级适配规则**：输出内容的详略程度，必须匹配项目规模与需求类型，避免过度输出导致Token浪费与API调用增加：

￮ 大型全栈项目：严格执行完整的输出规范与交付物要求；

￮ 中小型项目：精简非核心文档与说明内容，保留核心架构、接口说明、运行手册等关键内容；

￮ 单功能代码片段需求：精简非必要章节，仅保留核心的代码、使用说明、依赖说明，最小化输出内容。

**10.2 需求澄清智能体输出规范**

必须输出3个固定章节，禁止遗漏：

1. 项目核心需求概述：明确机器人开发的核心目标、应用场景、核心功能边界、仿真环境要求、安全红线、版本约束（ROS/Ubuntu/依赖库）；
2. 结构化需求详情：分模块明确功能需求、性能指标、环境约束、技术栈要求、安全要求、验收标准；
3. 待确认补充事项：针对模糊、缺失的关键信息，输出明确的补充提问清单，引导用户完善需求。

**10.3 总架构师智能体输出规范**

必须输出5个固定章节，禁止遗漏，输出内容必须结构化、无歧义，可直接用于下游评审与任务拆解：

1. 系统整体架构设计：明确系统分层架构、模块划分、整体技术方案选型、模块间调用关系、安全架构设计；
2. 模块职责与接口规范：明确每个核心模块的职责、输入输出接口、数据类型定义、调用关系、时序要求；
3. 核心技术方案与算法选型：明确核心算法选型、技术实现路径、关键技术难点解决方案、实时性与安全保障方案；
4. 技术栈与环境约束：明确开发语言、框架、ROS版本、依赖库、仿真环境、运行环境的刚性要求；
5. 系统验收标准：明确全系统的功能验收、性能验收、安全验收、仿真验证验收、可交付性验收标准。

**10.4 提示词工程智能体输出规范**

必须输出3个固定章节，禁止遗漏：

1. 优化后的结构化架构提示词：无歧义、结构化、可直接被下游批判反思智能体执行的标准化提示词，完整保留架构核心信息，固化约束条件；
2. 核心约束清单：提炼架构中的刚性约束、核心指标、边界条件、安全要求，确保下游执行不偏离；
3. 下游执行要求：明确批判反思智能体的评审维度、输出要求、评审标准。

**10.5 批判反思智能体输出规范**

必须输出3个固定章节，禁止遗漏：

1. 架构整体评审结论：明确架构整体合理性、可行性、风险等级的综合评价，给出明确的评审结论；
2. 结构化缺陷清单：每条缺陷必须严格遵循以下固定字段，按优先级排序：

| 字段                | 说明                        |
| ------------------- | --------------------------- |
| defect_id           | 缺陷唯一编号                |
| defect_level        | 枚举值：致命/严重/一般/建议 |
| influence_scope     | 缺陷影响的模块与范围        |
| problem_description | 清晰明确的问题描述          |
| root_cause_analysis | 问题根因分析                |
| repair_suggestion   | 可落地的具体修复方案        |
| priority            | 枚举值：高/中/低            |

3. 优化优先级与落地建议：明确缺陷修复的优先级、迭代建议，以及架构整体的优化方向。

**10.6 架构拆解与任务规划智能体输出规范**

必须输出固定Schema的标准化JSON格式，禁止其他格式，JSON结构严格遵循以下规范：

JSON {  "project_info": {   "project_name": "项目名称",   "architecture_version": "架构版本",   "global_constraint": "全局约束与规范摘要"  },  "global_interface_spec": "全局接口定义、自定义消息/服务规范、依赖版本统一要求",  "task_list": [   {    "task_id": "字符串，唯一任务编号，用于依赖关联",    "task_name": "任务名称，明确核心内容",    "task_type": "枚举值：核心算法开发、C++性能优化、ROS工程化开发、仿真环境开发、轻量代码开发、局部架构适配、全局接口定义、bug修复",    "depend_task_id": ["依赖的前置任务task_id数组，无依赖为空数组"],    "adapt_model": "适配的主模型名称，严格匹配架构规定",    "acceptance_criteria": "明确、可量化、无歧义的任务验收标准",    "preconditions": "任务执行的前置条件",    "timeout": "任务超时时间，单位：分钟，严格遵循全局统一标准",    "risk_level": "枚举值：极高安全风险/高安全风险/一般风险/低风险",    "priority": "枚举值：高/中/低"   }  ],  "parallel_execution_rule": "并行任务执行规则与约束",  "delivery_requirement": "任务输出与交付要求" }

**10.7 局部架构适配Agent输出规范**

必须输出3个固定章节，禁止遗漏：

1. 调整背景与影响范围：明确本次局部调整的原因、需求依据、影响的模块与范围，明确说明不涉及核心架构变更、不影响2个及以上模块；
2. 调整后的架构与接口规范：明确调整后的模块设计、接口定义、参数变化、适配方案，与原有全局规范的差异说明；
3. 验证与落地建议：明确调整后的验证要求、对下游任务的影响、执行建议、需要同步更新的内容。

**10.8 所有编码类Agent统一输出规范（刚性执行）**

核心算法、工程化、C++优化、仿真工程化、轻量代码、bug修复等所有编码类Agent，必须输出以下固定格式，禁止纯代码无说明的输出，路径规范按项目类型适配，单功能需求可精简非必要章节：

1. 代码文件路径（刚性规范）：

￮ ROS项目：明确该代码在ROS工作空间src目录为基准的完整相对路径，比如motion_control/src/motion_control_node.cpp，C++头文件必须放在对应ROS包的include/包名/目录下，路径必须符合ROS官方标准；

￮ 纯算法/无ROS项目：以项目根目录为基准，按通用C++/Python项目结构规范输出路径，比如include/path_planning/a_star.h、src/a_star.cpp；

￮ 单脚本/单文件任务：明确文件名与推荐存放路径。

2. 功能说明：简要说明该代码模块的核心功能、输入输出、适用场景、遵循的接口规范；
3. 编译/运行依赖：明确该代码的编译依赖、运行依赖、版本要求、环境配置，跨包依赖必须明确标注对应的ROS包名；
4. 复用说明：明确标注是否复用了记忆库内容、复用的来源、做了哪些适配修改、版本适配说明，无复用则标注“无复用”；
5. 完整代码内容：带完整注释、符合统一编码规范、可直接编译执行的完整代码，禁止截断、伪代码；跨包头文件引用必须使用ROS标准格式，禁止相对路径引用；
6. 自测说明：代码的自测方法、关键参数说明、边界情况说明、使用注意事项；
7. 修改说明：若为迭代修改的代码，必须明确标注修改原因、对应问题编号、修改前后的核心变化。

**10.9 算法边界与安全校验Agent输出规范**

必须输出以下固定格式，禁止模糊表述：

1. 校验整体结论：明确是否通过校验，给出明确的通过/不通过/有条件通过结论；
2. 模块安全校验结果：针对高/极高安全风险模块，输出专项安全校验结果，明确安全机制覆盖情况、边界条件校验结果、风险点；
3. 结构化问题清单：每条问题必须包含「问题编号、问题等级、问题位置、问题描述、影响范围、修复建议」固定字段；
4. 后续执行建议：明确是否需要返回修改、是否可进入下一环节、注意事项。

**10.10 测试脚本生成Agent输出规范**

必须输出以下固定格式，确保本地脚本可自动化执行：

1. 测试脚本文件路径：明确测试脚本的完整相对路径与文件名；
2. 测试覆盖范围：明确该测试脚本覆盖的测试场景、测试用例、验证的核心指标、测试类型（单元测试/性能测试/仿真测试/故障注入测试/安全测试/实时性测试）；
3. 测试执行命令：明确的本地可执行的测试命令，包含环境准备、执行指令、报告生成指令；
4. 通过判定标准：明确测试通过的刚性判定条件，无歧义、可量化，尤其是实时性测试的验收指标；
5. 完整测试脚本代码：可直接执行的完整测试脚本代码，包含注释、断言、日志输出、测试报告生成功能。

**10.11 代码集成与兼容性校验Agent输出规范**

必须输出以下固定格式：

1. 集成校验整体结论：明确是否通过集成与兼容性校验，给出明确的通过/不通过结论；
2. ROS包工程规范与编译校验结果：ROS包结构、编译依赖、跨包引用规范符合性、全量代码可编译性的校验结果；
3. 接口兼容性校验结果：多模块接口、ROS话题/服务的一致性、匹配度、调用时序的校验结果；
4. 跨模块安全联动校验结果：全链路安全机制、急停逻辑、故障兜底机制的联动有效性校验结果；
5. ROS2规范符合性校验结果：针对ROS2项目，输出QoS配置、生命周期管理、组件化配置、launch文件规范的校验结果；
6. 循环依赖检测结果：隐式循环依赖的扫描结果，发现的问题与修复建议；
7. 结构化问题清单与修复建议：按优先级排序的问题列表，明确问题位置、根因、影响范围、可落地的修复建议；
8. 依赖文件与环境要求：整理后的全项目依赖清单、环境配置要求、编译运行说明。

**10.12 系统评测模型输出规范**

必须输出以下固定章节，禁止遗漏：

1. 系统整体终审结论：明确全系统是否通过评测，给出明确的通过/有条件通过/不通过结论；
2. 架构合理性评测：针对系统架构设计的合理性、可实现性、可扩展性、场景适配性的评测结果；
3. 代码质量全局评测：全量代码的编码规范、可读性、可维护性、安全性、性能表现的评测结果；
4. 核心算法与实时性评测：核心算法的选型合理性、性能指标、场景适配性、鲁棒性，以及全链路实时性、控制延迟、时序稳定性的评测结果；
5. 安全性专项评测：全系统安全架构、急停逻辑、故障兜底能力、边界条件处理、安全编码规范的专项评测结果，明确安全风险点与修复建议；
6. 工程化与可维护性评测：ROS工程规范、包结构、参数管理、文档注释、可维护性的评测结果；
7. 结构化问题清单与优化建议：按优先级排序的问题清单，每条问题明确「问题等级、影响范围、根因分析、可落地的优化建议、优先级」；
8. 后续落地与优化建议：针对仿真验证、后续迭代、场景扩展的专业建议。

**10.13 迭代闭环与版本管理智能体输出规范**

最终交付时，必须输出以下固定内容，同时严格按照标准化目录结构整理全量交付物，禁止零散文件交付：

1. 版本基本信息：明确本次交付的版本号、版本迭代记录、需求变更记录、项目周期；
2. 全流程闭环记录：需求变更、架构迭代、代码修改、测试问题、缺陷修复的全链路闭环情况说明；
3. 最终交付物清单：明确本次交付的所有文件、代码包、文档的清单、路径、说明；
4. 版本变更日志（CHANGELOG）：严格遵循语义化版本（Semantic Versioning）标准，规范如下：

￮ 版本号规则：采用MAJOR.MINOR.PATCH的语义化版本号，其中：

▪ MAJOR：核心架构变更、不兼容的API变更、核心需求变更；

▪ MINOR：功能新增、局部架构优化、向下兼容的功能迭代；

▪ PATCH：bug修复、代码优化、文档更新，不影响功能与接口。

￮ 变更内容分类规范：每个版本的变更内容，必须按「新增（Added）、优化（Changed）、修复（Fixed）、废弃（Deprecated）、移除（Removed）、安全（Security）」分类记录，每一条变更必须清晰明确，可追溯。

￮ 格式规范：遵循行业通用的CHANGELOG格式，每个版本标注版本号、发布日期，变更内容按分类用列表呈现。

5. 部署与运行说明：代码的环境配置、依赖安装、编译步骤、仿真环境启动方法、使用说明、常见问题排查方案；
6. 核心参数调优指南：核心算法、控制模块的参数说明、调优方法、不同场景的推荐配置；
7. 后续维护与迭代建议：针对项目后续维护、算法优化、场景扩展的专业建议。

**10.14 全流程统一编码规范（刚性执行，全环节校验）**

所有编码类Agent必须严格遵循以下统一编码规范，本地静态检查环节将强制校验，不符合规范的直接打回修改：

1. **C++代码规范**：强制遵循《Google C++ Style Guide》，同时符合ROS C++编码规范；命名规则、代码格式、注释规范、内存管理、异常处理严格遵循规范要求；禁止使用不安全的函数、存在内存溢出风险的操作；实时性代码必须遵循硬实时开发规范，禁止在实时循环内执行违规操作。
2. **Python代码规范**：强制遵循PEP8规范，同时符合ROS Python编码规范；代码缩进、命名规则、注释规范、导入规则严格遵循要求；禁止使用不安全的代码写法，完善异常处理机制。
3. **ROS/ROS2代码规范**：强制遵循ROS/ROS2官方命名规范、节点设计规范、接口设计规范；话题、服务、参数、帧ID的命名必须符合行业通用标准，节点设计遵循模块化、低耦合原则；严格遵循ROS2的QoS、生命周期管理、组件化开发、命名空间、参数管理、launch文件规范；严格遵循跨包引用与依赖配置规范，确保编译零报错。
4. **安全编码规范**：禁止使用不安全的函数、存在内存溢出、数组越界、空指针解引用风险的操作；必须处理所有异常情况、边界条件，禁止硬编码的魔法数字；运动控制相关代码必须添加双层极限值约束、异常兜底逻辑；所有输入数据必须做合法性校验，输出控制指令必须做限幅约束。
5. **注释规范**：强制要求Doxygen风格的函数注释、模块说明注释，包含功能描述、参数说明、返回值、异常处理、注意事项；关键逻辑、复杂算法、边界处理必须添加行内注释，明确设计思路与约束条件；参数定义必须添加详细的注释说明。
6. **可移植性规范**：分离算法核心逻辑与硬件/仿真接口，抽象通用接口，提升代码可移植性；避免平台相关的硬编码，使用跨平台的标准实现；依赖版本明确标注，避免版本兼容问题。
7. **算法可复现性规范**：

￮ 传统算法：必须明确算法的原理说明、参数调优指南、边界条件说明，代码必须模块化、可复用，核心逻辑与接口封装分离，提供完整的测试用例。

￮ 深度学习/强化学习算法：必须明确模型的依赖框架与版本要求、模型文件说明、推理环境配置要求；必须封装标准化的推理接口，与ROS节点解耦；必须提供模型的加载、推理、异常处理的完整逻辑，禁止硬编码的模型路径、参数；必须说明模型的训练环境、输入输出格式、精度指标，确保用户可复现推理效果。

**10.15 最终交付物标准化目录结构（刚性执行，分场景适配，支持精简模式）**

迭代闭环与版本管理智能体必须按项目类型，匹配对应的标准化目录结构，确保结构清晰、规范统一，用户可快速上手使用，减少后续额外提问与修改请求，禁止零散文件交付。中小型项目可合并精简非核心文档，单功能需求无需完整项目结构。

**（1）ROS全栈项目标准目录结构**

Plain Text robot_project_vx.x.x/ ├── src/           # 核心代码与ROS包源码，所有功能节点、算法实现、接口定义均在此目录，严格遵循ROS包规范 ├── docs/          # 全量项目文档，所有文档必须内容完整、可落地，禁止空泛内容 │  ├── 01-需求文档.md │  ├── 02-系统架构设计.md │  ├── 03-接口说明文档.md    # 必须包含所有话题、服务、动作的详细说明、数据结构定义 │  ├── 04-编译运行手册.md    # 必须包含环境配置、依赖安装、编译步骤、启动方法、节点说明 │  ├── 05-仿真环境说明.md │  ├── 06-核心参数调优指南.md  # 必须包含核心参数的含义、范围、调优方法、场景化推荐值 │  ├── 07-测试报告.md │  ├── 08-系统评测报告.md │  ├── 09-安全审核记录.md   # 高风险项目必须包含，所有高风险模块的人工审核记录 │  ├── 10-故障排查与常见问题.md # 必须包含高频问题的根因分析与解决方案 │  └── 11-真机适配指南.md   # 必须包含仿真到真机的适配步骤、硬件驱动适配、安全验证流程 ├── simulation/       # 仿真环境相关文件，包括URDF/Xacro模型、世界文件、仿真launch文件、传感器配置 ├── scripts/         # 辅助脚本、一键依赖安装脚本、全量编译脚本、启动脚本、测试脚本 ├── config/         # 全局参数配置文件、算法参数yaml、launch文件依赖的配置文件，按模块分类 ├── test/          # 单元测试、集成测试、仿真测试脚本与测试报告 ├── CHANGELOG.md       # 标准化版本变更日志，严格遵循语义化版本标准 ├── README.md        # 项目总览、核心功能说明、环境依赖、3步以内的快速启动命令、常见问题排查、许可证信息 ├── requirements.txt     # Python依赖清单，明确包名与版本范围 ├── rosdep.yaml       # ROS系统依赖清单 └── LICENSE         # 开源协议说明（若有）

**（2）纯算法项目标准目录结构**

Plain Text algorithm_project_vx.x.x/ ├── include/         # C++头文件 ├── src/           # 核心算法源码 ├── docs/          # 算法说明文档、接口说明、编译运行手册、调优指南、测试报告 ├── scripts/         # Python原型脚本、辅助工具、测试脚本 ├── config/         # 算法参数配置文件 ├── test/          # 单元测试、性能测试脚本与测试报告 ├── examples/        # 使用示例、demo代码 ├── CHANGELOG.md       # 版本变更日志 ├── README.md        # 项目总览、快速上手、环境依赖、使用说明 ├── requirements.txt     # Python依赖清单 ├── CMakeLists.txt      # C++工程编译配置 └── LICENSE         # 开源协议说明

**（3）单功能代码片段交付规范**

• 必须包含：代码文件、使用说明、依赖说明、测试方法、注意事项，无需完整项目目录结构，确保简洁可用。

**十一、机器人高频场景专属适配优化（细化刚性规范，减少后续修改，明确执行主体）**

无需新增角色，仅在现有Agent职责与全流程规则中，补充机器人开发高频场景的专属适配刚性规范，明确各环节的执行主体，确保生成的代码符合对应框架的官方最佳实践，减少后续修改与额外请求。

**11.1 机械臂与MoveIt!适配刚性规范**

• **版本适配规范**：明确区分ROS1 MoveIt!与ROS2 MoveIt2的适配标准，禁止生成版本不兼容的代码，必须严格匹配项目指定的ROS版本，所有API调用、配置文件格式必须符合对应版本的官方规范。

• **执行主体与职责划分**：

￮ 总架构师：负责MoveIt!相关的架构设计、模块划分、接口规范定义；

￮ 工程化代码实现Agent：负责MoveIt!配置包开发、接口封装、ROS节点实现、launch文件编写；

￮ 仿真工程化Agent：负责仿真环境与MoveIt!的适配、仿真场景配置、传感器与碰撞场景配置；

￮ 测试脚本生成Agent：负责MoveIt!相关的运动规划、轨迹执行、急停功能测试脚本生成。

• **配置包开发规范**：生成的MoveIt!配置包必须符合官方标准结构，包含完整的SRDF模型、关节限位与安全约束、运动学插件配置、规划 pipeline 配置、碰撞检测配置、执行器控制适配，确保配置包可直接与MoveIt!框架兼容，支持运动规划、碰撞检测、轨迹执行。

• **接口封装规范**：工程化代码实现Agent必须规范封装MoveIt!的C++/Python接口，包括规划组初始化、目标位姿设置、运动规划、轨迹执行、碰撞场景更新、紧急停止的标准化接口，确保与MoveIt!框架无缝对接，禁止硬编码参数，支持通过配置文件调整。

• **安全规范**：必须包含机械臂运动的极限值约束、碰撞检测与安全停止机制、轨迹执行异常兜底逻辑、急停接口适配，确保运动安全；必须处理规划失败、执行超时、奇点规避等异常场景，有明确的兜底处理逻辑。

• **校验要求**：在本地预编译校验、集成校验环节，补充MoveIt!配置包的可编译性、规范符合性校验；测试环节必须包含运动规划、轨迹执行、奇点处理、急停功能的测试用例。

**11.2 SLAM与导航适配刚性规范**

• **版本适配规范**：明确区分ROS1 Navigation Stack与ROS2 Nav2的开发规范，插件开发必须符合官方接口标准，禁止生成版本不兼容的代码，严格匹配项目指定的ROS版本。

• **执行主体与职责划分**：

￮ 总架构师：负责SLAM与导航系统的架构设计、模块划分、接口规范、技术选型；

￮ 核心算法代码编写Agent：负责SLAM算法、路径规划算法、避障算法、控制器算法的核心实现；

￮ 工程化代码实现Agent：负责ROS节点封装、导航插件开发、launch文件与配置文件编写、接口适配；

￮ 仿真工程化Agent：负责仿真环境与导航模块的适配、地图场景配置、传感器仿真配置；

￮ 测试脚本生成Agent：负责建图、导航、避障、异常场景的测试脚本生成。

• **导航适配规范**：导航相关节点的开发必须符合官方框架的标准，包括costmap插件、行为树插件、控制器插件、规划器插件的开发规范，确保与ROS导航栈完全兼容；必须规范配置参数的注释与调优说明，确保用户可根据场景调整；必须处理导航失败、障碍物规避、目标不可达等异常场景，有明确的降级与兜底逻辑。

• **SLAM适配规范**：SLAM算法的传感器数据适配、位姿输出接口，必须完全兼容ROS导航栈的标准接口，包括里程计、TF变换、地图发布的标准格式，确保与导航模块无缝对接；必须处理传感器数据异常、位姿跳变的兜底逻辑；必须保证建图与定位的输出符合ROS标准，无需额外适配即可被导航模块使用。

• **测试规范**：测试脚本生成Agent必须针对SLAM与导航模块，生成建图测试、导航路径规划、避障测试、异常场景测试、回环检测测试的脚本，确保功能完整性与鲁棒性。

**11.3 视觉感知适配刚性规范**

• **执行主体与职责划分**：

￮ 总架构师：负责视觉感知模块的架构设计、接口规范、技术选型；

￮ 核心算法代码编写Agent：负责视觉检测、识别、跟踪、分割算法的核心实现；

￮ 工程化代码实现Agent：负责ROS节点封装、话题接口适配、消息格式转换、参数配置管理；

￮ 测试脚本生成Agent：负责视觉模块的精度、帧率、鲁棒性测试脚本生成。

• **ROS图像消息处理规范**：ROS图像消息与OpenCV格式的转换，必须使用cv_bridge官方库，禁止自行实现格式转换，避免内存泄漏、格式错误问题；必须处理图像帧的时间戳、帧ID同步，确保与其他模块的时间同步，尤其是多传感器融合场景。

• **消息格式规范**：视觉检测结果的输出，必须使用ROS标准的Detection2DArray/BoundingBoxArray/Image等通用消息类型，禁止自定义非标准消息，确保与其他模块的兼容性；必须包含检测结果的时间戳、帧ID、置信度等核心字段，符合行业通用标准。

• **节点开发规范**：视觉处理节点必须符合ROS节点开发规范，支持参数动态重配置、QoS策略适配、异常数据兜底处理，避免因图像数据异常、模型推理失败导致节点崩溃；必须处理推理超时、输入数据无效的场景，有明确的异常处理逻辑。

• **测试规范**：测试脚本生成Agent必须针对视觉感知模块，生成图像输入测试、检测精度测试、异常数据注入测试、帧率与延迟测试的脚本，验证算法的稳定性与实时性。

**11.4 四足/人形机器人运动控制适配刚性规范**

• **执行主体与职责划分**：

￮ 总架构师：负责运动控制系统的分层架构设计、模块划分、接口规范、安全架构设计；

￮ 核心算法代码编写Agent：负责运动学解算、步态规划、动力学控制、平衡控制、ZMP计算等核心算法实现；

￮ 工程化代码实现Agent：负责ROS节点封装、控制接口实现、参数管理、生命周期管理；

￮ C++性能优化专项Agent：负责实时控制代码的硬实时适配、性能优化、内存锁定、线程优先级配置；

￮ 算法边界与安全校验Agent：负责运动控制模块的安全机制、极限约束、异常兜底逻辑的专项校验；

￮ 测试脚本生成Agent：负责关节控制、步态稳定性、急停功能、异常工况的测试脚本生成。

• **核心架构规范**：总架构师设计架构时，必须明确分层控制架构，包括步态规划层、运动学解算层、关节控制层、安全防护层，明确各层的接口规范与时序要求，确保控制链路的实时性与安全性。

• **运动学与动力学规范**：核心算法代码编写Agent必须实现完整的正逆运动学解算、刚体动力学计算、零力矩点(ZMP)稳定性判据，代码必须模块化、可复用，参数可配置，禁止硬编码机器人结构参数；必须处理奇异位形、关节限位等边界场景，有明确的兜底约束。

• **步态控制规范**：步态生成算法必须包含完整的步态时序规划、足端轨迹规划、落地冲击缓冲处理、地形自适应逻辑；必须支持步态参数的可配置化，明确参数的物理意义与调优范围；必须处理步态切换、摔倒防护、异常姿态兜底的安全逻辑。

• **安全与实时性规范**：必须遵循硬实时系统适配规范，关节控制线程、步态控制循环必须满足实时性要求，禁止在实时循环内执行违规操作；必须实现双层关节极限约束、力矩限幅、急停逻辑、姿态异常安全兜底、摔倒检测与防护机制，确保机器人运动安全。

• **ROS工程化规范**：必须严格分离核心控制算法与ROS接口，确保算法可移植性；ROS节点必须实现状态监控、参数动态更新、异常降级处理；控制指令的话题必须符合实时性QoS策略要求，确保控制指令的低延迟与可靠性。

• **测试与校验规范**：安全校验环节必须重点校验运动控制的极限约束、急停逻辑、异常兜底机制的完整性；测试脚本必须生成关节限位测试、步态稳定性测试、急停功能测试、异常工况注入测试的脚本，确保控制逻辑的安全性与稳定性。

**11.5 深度强化学习机器人控制适配刚性规范**

• **执行主体与职责划分**：

￮ 总架构师：负责强化学习控制系统的架构设计、训练与推理逻辑解耦、接口规范、安全防护架构设计；

￮ 核心算法代码编写Agent：负责强化学习推理核心逻辑、模型封装、推理接口实现；

￮ 工程化代码实现Agent：负责ROS节点封装、传感器数据预处理、控制指令后处理、接口适配；

￮ 算法边界与安全校验Agent：负责推理输出的安全约束、极限值校验、异常兜底逻辑的专项校验；

￮ 测试脚本生成Agent：负责模型推理接口、安全约束、端到端延迟的测试脚本生成。

• **算法与工程分离规范**：必须严格分离强化学习的训练环境、模型推理、ROS控制接口三层架构，禁止将训练逻辑与部署推理逻辑耦合；核心算法Agent负责模型推理核心逻辑的实现，工程化Agent负责ROS节点封装与接口适配，确保代码的可维护性与可移植性。

• **模型推理规范**：必须明确深度学习框架与版本要求、模型文件格式、依赖库版本，避免版本兼容问题；必须封装标准化的推理接口，支持模型文件路径、推理设备、推理参数的可配置化，禁止硬编码；必须处理模型加载失败、推理超时、输出异常的场景，有明确的安全兜底逻辑，禁止在异常情况下输出危险的控制指令。

• **安全约束规范**：推理输出的控制指令必须经过双层限幅与安全校验，包括关节极限约束、速度/加速度约束、力矩限制，禁止直接将模型输出发送给硬件执行器；必须实现异常状态的安全降级机制，当模型输出异常、传感器数据失效时，自动触发安全停止或姿态保持逻辑。

• **ROS工程化规范**：ROS节点必须实现传感器数据的预处理、时间同步、推理结果的后处理、控制指令发布全流程；必须支持参数动态重配置、话题QoS策略适配；必须完善日志输出，便于调试与问题排查；必须分离算法核心与ROS接口，确保sim2real适配的便捷性。

• **可复现性规范**：必须明确说明模型的训练环境、算法框架、输入输出格式、状态空间与动作空间定义、精度指标；必须提供模型的部署步骤、环境配置要求、仿真验证方法，确保用户可复现推理效果；必须提供sim2real适配的指导说明，明确仿真与真机的适配要点。

• **测试规范**：测试脚本必须包含模型推理接口测试、输入输出边界测试、异常数据注入测试、安全约束有效性测试、端到端控制延迟测试，确保算法的稳定性与安全性。

**11.6 端到端自动驾驶与移动机器人导航适配刚性规范**

• **执行主体与职责划分**：

￮ 总架构师：负责自动驾驶/导航系统的分层架构设计、模块划分、接口规范、安全架构设计；

￮ 核心算法代码编写Agent：负责感知、定位、决策、规划、控制各模块的核心算法实现；

￮ 工程化代码实现Agent：负责各模块的ROS节点封装、接口适配、launch文件与配置文件编写；

￮ 仿真工程化Agent：负责自动驾驶仿真场景搭建、传感器仿真、环境模型配置；

￮ 测试脚本生成Agent：负责各模块单元测试、全链路集成测试、场景化仿真测试、安全功能测试脚本生成。

• **架构分层规范**：总架构师设计架构时，必须遵循自动驾驶通用的分层架构，包括感知层、定位层、决策规划层、控制层、安全防护层，明确各模块的职责边界、输入输出接口、数据格式与时序要求，确保全链路的实时性与安全性。

• **感知与定位规范**：多传感器融合必须遵循8.4节的时间同步与数据有效性规范；定位模块的输出必须符合ROS标准的TF变换、Odometry格式，确保与规划控制模块无缝兼容；必须处理传感器失效、定位跳变、数据超时的异常场景，有明确的降级与安全兜底逻辑。

• **决策规划规范**：全局路径规划、局部轨迹规划、行为决策模块必须解耦，接口标准化；规划算法必须考虑车辆/机器人的运动学约束、动力学约束、障碍物避障、交通规则（适用于自动驾驶场景）；必须处理目标不可达、路径规划失败、动态障碍物规避的场景，有明确的降级处理逻辑；规划输出的轨迹必须满足控制模块的接口要求，确保可执行性。

• **运动控制规范**：纵向/横向控制、底层运动控制算法必须满足实时性要求，严格遵循硬实时开发规范；必须实现控制指令的极限值约束、异常工况兜底、故障安全停止机制；必须处理轨迹跟踪偏差过大、执行器异常的场景，触发安全降级逻辑。

• **安全冗余规范**：必须设计独立的安全监控模块，实时监控全系统的健康状态、传感器有效性、定位精度、规划轨迹合理性、控制指令安全性，当出现异常时，立即触发急停或安全停车逻辑，确保最高优先级的安全防护；急停逻辑必须独立于主控制链路，确保单点故障不影响安全功能。

• **ROS工程化规范**：各模块的ROS节点必须遵循实时性QoS策略，尤其是控制指令、急停指令的话题，必须保障可靠性与低延迟；节点必须实现生命周期管理、异常捕获、故障降级机制；参数必须通过配置文件统一管理，明确注释与调优说明；必须完善诊断信息发布，便于系统监控与故障排查。

• **测试与校验规范**：安全校验环节必须重点校验急停逻辑、极限约束、异常兜底机制的完整性；测试脚本必须生成模块级单元测试、全链路集成测试、场景化仿真测试、故障注入测试、极限工况测试的脚本，确保系统的功能完整性、实时性与安全性。

**十二、全文档规则一致性说明与核心规则索引**

为确保全文档执行标准统一，所有核心规则采用「一处定义、多处引用」的方式，避免重复描述导致的不一致问题，所有章节严格遵循以下统一规则，禁止自行定义。

**12.1 全文档统一规则说明**

1. 核心概念与统计口径：统一在5.11节完成最终定义，所有章节直接引用，不重复描述，禁止自行定义。
2. 迭代次数限制规则：统一在5.11节、9.10节明确定义，所有SOP章节、角色职责章节均引用统一规则，无例外。
3. 人工驳回与审核规则：统一在5.3节、5.11节、7.1节定义，所有相关章节保持一致。
4. 需求变更分级规则：统一在5.10节、5.11节定义，所有相关章节引用统一标准。
5. 并行任务与配额管控规则：统一在3.3节、第六部分定义，所有相关章节保持一致。
6. 预留配额使用规则：统一在6.3.3节定义，所有相关章节引用统一规则。
7. 高风险模块界定与管控规则：统一在5.11节、第七部分定义，全流程所有环节遵循统一标准。
8. 安全编码规范：统一在7.2节、10.14节定义，全流程所有编码环节严格执行。
9. ROS/ROS2工程化规范：统一在第八部分、10.14节定义，所有相关章节保持一致。
10. Git版本管理规则：统一在9.9节定义，所有相关章节引用统一规则。
11. 模型梯度调用规则：统一在6.2节定义，所有角色与调用环节严格执行，禁止突破。
12. 异常处理与超时规则：统一在9.1节、9.2节定义，全流程所有环节遵循统一标准。

**12.2 全文档核心刚性规则索引表**

| 规则类别   | 核心规则名称               | 定义章节         | 刚性等级   |
| ---------- | -------------------------- | ---------------- | ---------- |
| 核心约束   | 成本优先最高约束           | 一、核心刚性约束 | 最高优先级 |
| 核心约束   | 平台唯一模型约束           | 一、平台唯一约束 | 刚性红线   |
| 核心约束   | 安全刚性约束               | 一、核心刚性约束 | 刚性红线   |
| 成本管控   | 模型梯度调用铁则           | 6.2节            | 刚性执行   |
| 成本管控   | 三级告警与熔断机制         | 6.3.1节          | 刚性执行   |
| 成本管控   | 配额预留与使用规则         | 6.3.3节          | 刚性执行   |
| 成本管控   | 本地优先拦截体系规则       | 第四部分         | 刚性执行   |
| 流程管控   | 全流程SOP执行标准          | 第五部分         | 刚性执行   |
| 流程管控   | 全局接口变更闭环SOP        | 5.5节            | 刚性执行   |
| 流程管控   | 人工修改重校验SOP          | 5.7节            | 刚性执行   |
| 流程管控   | 需求变更处理SOP            | 5.10节           | 刚性执行   |
| 安全管控   | 高风险模块三级界定标准     | 5.11节           | 刚性红线   |
| 安全管控   | 高风险模块强制人工审核规则 | 7.1节            | 刚性红线   |
| 安全管控   | 安全编码规范与分层校验机制 | 7.2节            | 刚性执行   |
| 角色协同   | 各角色权责边界与触发规则   | 3.2节            | 刚性执行   |
| 迭代管控   | 全场景无限迭代终止规则     | 9.10节           | 刚性执行   |
| 迭代管控   | 迭代次数统计口径           | 5.11节           | 刚性执行   |
| 工程化规范 | ROS包结构与依赖管理规范    | 8.1节            | 刚性执行   |
| 工程化规范 | ROS2专属开发规范           | 8.2节            | 刚性执行   |
| 工程化规范 | 硬实时系统适配规范         | 8.3节            | 刚性执行   |
| 异常兜底   | 断点续跑执行规则           | 9.6节            | 刚性执行   |
| 异常兜底   | Git版本管理规则            | 9.9节            | 刚性执行   |
| 输出规范   | 各角色统一输出规范         | 第十部分         | 刚性执行   |
| 交付规范   | 最终交付物标准化目录结构   | 10.15节          | 刚性执行   |

**环境依赖与部署指南**

本架构所有本地功能的实现，依赖以下系统工具与Python库，部署前需完成环境配置，支持在线部署与离线部署两种模式。

**一、基础环境要求**

| 环境项      | 版本要求           | 说明                                                         |
| ----------- | ------------------ | ------------------------------------------------------------ |
| Ubuntu系统  | 20.04/22.04 LTS    | 适配ROS1 Noetic、ROS2 Humble/Iron官方推荐系统版本            |
| Python      | 3.8+/3.10+/3.11+   | 需与ROS版本匹配：ROS1 Noetic对应Python3.8，ROS2 Humble对应Python3.10 |
| ROS/ROS2    | Noetic/Humble/Iron | 需提前安装对应ROS版本，包含roslint、cv_bridge、message_filters等基础包 |
| C++编译环境 | GCC/G++ 7.5+       | 支持C++17及以上标准                                          |

**二、系统依赖安装**

Bash # Ubuntu系统依赖一键安装命令 sudo apt update && sudo apt install -y \ cppcheck \ clang-tidy \ pylint \ roslint \ git \ mlock \ libclang-dev

**三、Python依赖安装**

Bash # Python依赖包安装，推荐使用虚拟环境 pip install -r requirements.txt

requirements.txt内容如下（明确版本兼容要求）：

Plain Text faiss-cpu>=1.7.4 sentence-transformers>=2.2.2 sqlite3 pyyaml>=6.0 gitpython>=3.1.0 transformers>=4.30.0 libclang>=16.0.0 wget>=3.2

**四、Embedding模型配置**

**1. 在线部署（默认）**

• 代码中已集成模型自动下载逻辑，首次运行时会自动从Hugging Face Hub下载bge-small-zh-v1.5和bge-small-code-v1.5模型，自动缓存到本地，无需手动配置。

• 若国内网络访问受限，可配置镜像源：

Bash export HF_ENDPOINT=https://hf-mirror.com

**2. 离线部署方案**

1. 提前下载模型文件：

￮ bge-small-zh-v1.5：https://huggingface.co/BAAI/bge-small-zh-v1.5

￮ bge-small-code-v1.5：https://huggingface.co/BAAI/bge-small-code-v1.5

2. 将下载的模型文件夹，放置到项目固定目录（如/home/qizheng/auto_code_ws/models/）；
3. 在全局配置中心，配置本地模型路径，关闭自动下载功能，程序启动时会直接加载本地模型文件。

**五、全局配置中心**

所有可配置参数均纳入全局配置中心，配置文件采用yaml格式，存放于项目根目录的config/auto_code_config.yaml，支持用户自定义调整以下核心配置：

1. 本地拦截层配置：拦截阈值、缓存配置、知识库路径、模型路径；
2. 配额管控配置：各时间维度预留比例、告警阈值、并行数上限、限流规则；
3. Git管理配置：分支策略、自动提交模式、保护分支配置；
4. 人工节点配置：各人工确认节点的超时时限；
5. 迭代次数配置：各环节迭代次数上限（不可突破刚性红线）；
6. 告警通知配置：webhook通知渠道、告警等级配置；
7. 多项目/多租户配置：隔离模式、项目路径配置。

**六、快速启动与部署步骤**

1. 完成上述环境依赖安装；
2. 克隆/下载项目代码到本地；
3. 修改全局配置文件，匹配自身环境与需求；
4. 启动智能体调度平台主脚本，完成初始化、模型加载、知识库初始化；
5. 输入开发需求，启动自动化开发流程。

**七、常见问题排查**

1. Embedding模型加载失败：检查网络连接，或使用离线部署方案手动下载模型；
2. 本地静态校验工具执行失败：确认系统依赖已完整安装，检查工具版本与配置文件；
3. ROS包编译失败：检查ROS环境是否正确激活，跨包依赖是否配置完整，ROS版本是否匹配；
4. 实时性配置不生效：确认系统已安装PREEMPT_RT实时内核，程序运行具备相应的权限（mlock、线程优先级配置权限）；
5. Git操作异常：检查Git仓库权限、分支配置，确认无未解决的合并冲突。

 
