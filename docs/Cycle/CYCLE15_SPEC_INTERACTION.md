# CYCLE 15 - UI/UX 优化 Spec #2: 交互规范

> **文档版本**: v1.0.0
> **创建日期**: 2026-07-29
> **适用范围**: Hermes 智能体调度平台全部 3 个 frontend 项目
> **依赖**: [CYCLE15_SPEC_VISUAL.md](./CYCLE15_SPEC_VISUAL.md)
> **状态**: ✅ 待 Phase 3 实施

---

## 1. 交互设计原则

### 1.1 核心原则

1. **即时反馈 (Immediate Feedback)**: 所有用户操作 100ms 内必须有视觉反馈
2. **可逆优先 (Reversible First)**: 破坏性操作必须可撤销或二次确认
3. **状态可见 (Status Visibility)**: 系统状态任何时刻都清晰可见
4. **错误友好 (Error Friendly)**: 错误信息具体、可操作、有引导
5. **键盘友好 (Keyboard Friendly)**: 核心操作 100% 支持键盘
6. **效率优先 (Efficiency First)**: 高频操作 1 次点击 / 1 个快捷键完成

### 1.2 交互禁忌（红线）

- ❌ **无反馈操作**: 点击后无 loading / 无 toast / 无状态变化
- ❌ **死路流程**: 操作后无退出路径
- ❌ **歧义文案**: "确定"、"是/否" 等模糊词汇
- ❌ **未保存离开**: 表单编辑后意外离开无提示
- ❌ **无限等待**: 无超时机制
- ❌ **破坏性操作无确认**: 删除、回退、覆盖无二次确认
- ❌ **错误无引导**: 只显示错误码，无下一步建议

---

## 2. 操作路径精简（核心用户路径）

### 2.1 路径 1: 创建新项目（5 步 → 3 步）

#### 旧路径（5 步）
1. 主页 → 点击"新项目"
2. 选择模式（4 选 1）
3. 输入项目名
4. 选择目录
5. 确认创建

#### 新路径（3 步）
1. 主页 → **Cmd+N**（或点击"新项目"）
2. **单页表单**: 模式 + 名称 + 目录（上下排列）
3. **回车确认** 或 **Esc 取消**

**优化要点**:
- 默认值：模式 = 第一个常用项，名称 = 当前时间戳，目录 = 上次目录
- 实时校验：名称冲突立即提示
- 自动聚焦：第一个输入框

### 2.2 路径 2: 与 AI 对话（4 步 → 1 步）

#### 旧路径
1. 打开对话
2. 输入需求
3. 选择模型（点击下拉）
4. 点击发送

#### 新路径
1. **Cmd+I** 全局唤起输入框
2. **直接输入并回车**（模型走用户偏好默认）
3. 实时显示思考过程

**优化要点**:
- 任意页面 Cmd+I 唤起
- 输入框支持 @ 文件引用（fuzzy search < 100ms）
- 草稿自动保存到 localStorage（防意外丢失）
- 回车发送，Shift+回车换行

### 2.3 路径 3: 查看代码变更（3 步 → 1 步）

#### 旧路径
1. AI 完成 → 点击"查看变更"
2. 弹出 DiffView
3. 选择文件查看 diff

#### 新路径
1. AI 完成 → **自动弹出 Diff 浮层**（3s 后自动收起）
2. 点击文件 → 直接打开
3. **实时跟随模式** 自动跳转至代码编辑面板

**优化要点**:
- DiffView 可固定显示（不自动收起）
- 支持词级/字符级切换
- 影响函数列表直接展示
- 一键接受/回退/创建分支

### 2.4 路径 4: 回退代码（3 步 → 2 步）

#### 旧路径
1. 打开时间线
2. 选择版本
3. 点击回退

#### 新路径
1. **Cmd+Z** 撤销上一步（或点击 Toast 撤销按钮）
2. 确认弹窗 → 5s 倒计时 → 默认执行

**优化要点**:
- 100 步 Undo Stack（indexedDB 持久化）
- 5s 倒计时（可取消）
- 误操作可立即 Ctrl+Shift+Z 重做

---

## 3. 操作反馈规范

### 3.1 Loading 状态（4 种标准）

#### 类型 1: Spinner（旋转图标）
- **使用场景**: 局部操作（按钮提交、表单提交）
- **时长**: < 3s
- **样式**: 16×16 旋转图标 + 半透明遮罩
- **位置**: 按钮内 / 元素中心

#### 类型 2: Skeleton（骨架屏）
- **使用场景**: 页面/区块加载
- **时长**: 0.5s - 3s
- **样式**: 灰色矩形 + 脉冲动画
- **位置**: 内容区

#### 类型 3: Progress Bar（进度条）
- **使用场景**: 已知进度（文件上传、AI 生成）
- **样式**: 顶部 4px 进度条 + 百分比
- **位置**: 顶部 / 元素内

#### 类型 4: Pulse（脉冲）
- **使用场景**: 后台轮询（待处理任务）
- **样式**: 16×16 圆点 + 缩放呼吸动画
- **位置**: 角落

### 3.2 成功反馈

#### 简短操作（< 1s）
- **无 Toast**，仅视觉反馈（按钮变绿 500ms 后恢复）

#### 中等操作（1-5s）
- **Toast**: 顶部右侧，3s 自动消失
- **格式**: ✓ 图标 + 简短描述 + 可选操作
- **示例**: `✓ 项目创建成功 [查看]`

#### 长时间操作（> 5s）
- **完成 Toast**: `✓ 任务完成 · 用时 23.4s`
- **可点击查看详情**

### 3.3 失败反馈

#### 用户错误（输入有误）
- **Inline 错误**: 输入框下方红字
- **即时**: 输入停止 500ms 后校验
- **示例**: `项目名称已存在，请使用其他名称`

#### 网络错误
- **Toast**: 顶部右侧，6s 显示
- **格式**: ✕ 图标 + 描述 + 重试按钮
- **示例**: `✕ 网络连接失败 [重试]`

#### 系统错误
- **Modal**: 居中模态 + 错误码 + 建议
- **格式**: ⚠ 图标 + 标题 + 描述 + [查看日志] [上报] 按钮

### 3.4 操作确认机制

#### 5 级确认强度

| 级别 | 触发条件 | 确认方式 | 示例 |
|------|---------|---------|------|
| L0 无需确认 | 读取、查询 | 无 | 查看日志 |
| L1 视觉反馈 | 写入、创建 | 按钮变化 | 创建草稿 |
| L2 Toast 提示 | 重要操作 | 1s Toast | 保存配置 |
| L3 弹窗确认 | **破坏性操作** | 模态 + 5s 倒计时 | 删除项目 |
| L4 文本确认 | **不可逆操作** | 输入"CONFIRM" | 清空数据库 |

#### L3 弹窗确认规范

```
┌──────────────────────────────────────────┐
│  ⚠  确认回退到上一版本？                  │
├──────────────────────────────────────────┤
│  此操作将覆盖当前未保存的更改。           │
│                                          │
│  变更摘要：                               │
│  - 3 个文件                              │
│  - +12 -5 行                             │
│  - 影响函数: handleSubmit, validateInput │
│                                          │
│  ⏱ 5s 后自动执行 (按 Esc 取消)            │
├──────────────────────────────────────────┤
│              [取消]  [确认回退]            │
└──────────────────────────────────────────┘
```

#### L4 文本确认规范

```
┌──────────────────────────────────────────┐
│  ⚠  危险操作：清空所有项目数据             │
├──────────────────────────────────────────┤
│  此操作将永久删除所有项目数据，且无法恢复。│
│                                          │
│  请输入 "CONFIRM" 以确认操作：            │
│  ┌──────────────────────────────────┐    │
│  │                                  │    │
│  └──────────────────────────────────┘    │
├──────────────────────────────────────────┤
│              [取消]  [确认（禁用）]       │
└──────────────────────────────────────────┘
```

---

## 4. 错误处理 UX

### 4.1 表单校验

#### 实时校验规则
- **失焦校验**: 离开输入框后立即校验
- **输入校验**: 边输入边校验（仅在错误已显示时）
- **提交校验**: 点击提交时所有字段校验

#### 错误展示
- **字段级**: 输入框下方红字 + 红色边线
- **表单级**: 顶部红色 banner
- **示例文案**:
  - ✅ `项目名称不能为空`
  - ✅ `项目名称不能超过 50 字符（当前 67）`
  - ✅ `项目名称 "test" 已存在`
  - ❌ `无效输入`（过于模糊）

### 4.2 网络异常兜底

#### 检测机制
- **fetch 超时**: 30s（可配置）
- **重试策略**: 指数退避 1s/2s/4s（最多 3 次）
- **离线检测**: `navigator.onLine` 事件

#### 兜底 UI
- **网络断开**: 顶部黄色 banner `⚠ 网络连接已断开，部分功能不可用`
- **请求失败**: 红色 Toast + [重试] 按钮
- **超时**: Toast `请求超时 [重试]`

### 4.3 边界情况引导

| 边界情况 | 当前行为 | 优化后行为 |
|---------|---------|----------|
| 列表为空 | 空白页面 | 引导插画 + 操作建议 |
| 搜索无结果 | "无结果" | `未找到 "{query}" 相关内容 [清除搜索] [换个关键词]` |
| 文件过大 | 报错 | `文件 {name} 大小 {size} 超过限制 {limit}，请压缩或分割` |
| 权限不足 | 报错 | `您没有访问此资源的权限 [申请权限] [返回]` |
| 加载失败 | 空白 | 占位图 + `加载失败 [重试]` |
| 操作超时 | 报错 | `操作超时（> 30s） [重试] [取消] [查看进度]` |

### 4.4 错误恢复路径

#### 撤销机制
- **操作类型**: 可撤销（创建、改名、删除、回退）
- **不可撤销**: 危险操作（清空数据库、推送到远程）
- **撤销方式**:
  - Ctrl+Z 全局快捷键
  - Toast 上的 [撤销] 按钮（4s 内可点击）
  - 撤销栈深度：100 步

#### 自动保存
- **草稿**: 输入时自动保存到 localStorage（每 1s 防抖）
- **恢复**: 重新打开页面时提示 `发现未保存的草稿 [恢复] [丢弃]`
- **冲突**: 与服务端冲突时模态提示

---

## 5. 快捷键体系

### 5.1 全局快捷键

| 快捷键 | 操作 | 适用范围 |
|--------|------|---------|
| `Cmd+I` | 唤起 AI 输入框 | 全局 |
| `Cmd+K` | 命令面板 | 全局 |
| `Cmd+N` | 新建项目 | 全局 |
| `Cmd+S` | 保存当前 | 全局 |
| `Cmd+Z` | 撤销 | 全局 |
| `Cmd+Shift+Z` | 重做 | 全局 |
| `Cmd+/` | 切换注释 | 编辑器 |
| `Cmd+F` | 查找 | 编辑器 |
| `Cmd+,` | 打开设置 | 全局 |
| `Esc` | 关闭弹窗/取消 | 全局 |
| `?` | 显示快捷键帮助 | 全局 |

### 5.2 命令面板 (Cmd+K)

#### 设计
- 顶部居中浮层
- 模糊搜索（fuzzy）
- 实时结果（< 50ms）
- 支持键盘上下选择 + 回车执行
- 支持最近使用 + 收藏

#### 命令分类
- **操作类**: 新建、打开、保存、关闭
- **导航类**: 跳转各页面
- **视图类**: 切换主题、切换布局
- **AI 类**: 切换模型、调整推理强度

### 5.3 快捷键提示

- 所有按钮/菜单的 Tooltip 显示快捷键
- `?` 弹出快捷键总览
- 可在设置中自定义

---

## 6. 批量操作支持

### 6.1 多选模式

#### 触发
- 列表项点击空白区域选中
- `Cmd+A` 全选
- `Shift+点击` 范围选择
- `Cmd+点击` 多选切换

#### 视觉反馈
- 选中项：蓝色边线 + 浅蓝背景
- 状态栏：底部显示 `已选 N 项`
- 工具栏：顶部出现批量操作按钮

### 6.2 批量操作菜单

```
已选 3 项 | [批量删除] [批量导出] [批量移动] [取消选择]
```

#### 二次确认
- 批量删除：模态确认 + 显示列表
- 批量移动：选择目标位置

### 6.3 应用场景

- **会话列表**: 批量删除、批量导出、批量归档
- **文件树**: 批量提交、批量回退
- **消息列表**: 批量复制、批量导出

---

## 7. 关键交互流程

### 7.1 Vibe Coding 流程

```
用户输入需求 (Cmd+I)
   ↓
@ 文件引用 (fuzzy search)
   ↓
回车发送 → 状态: idle → running
   ↓
实时显示思考过程（双层折叠 + 阶段标签 + 计时器）
   ↓
工具调用 → 状态: tool-calling（紫色扳手图标）
   ↓
代码生成 → Monaco 实时更新
   ↓
完成 → 状态: completed（绿色勾）
   ↓
DiffView 浮层（3s 后自动收起，可固定）
   ↓
用户选择: [接受] [回退] [修改]
   ↓
接受 → 状态: completed（持久化）
回退 → 状态: failed（红色 + Undo Toast）
```

### 7.2 循环工作流控制

```
┌────────────────────────────────────────────┐
│  ● 轮转中 · 第 3/10 轮 · 用时 02:34        │
│                                            │
│  [⏸ 暂停]  [⏹ 停止]  [⏭ 跳过]             │  ← 三按钮 hover-reveal
└────────────────────────────────────────────┘
```

#### 按钮交互
- **⏸ 暂停**: 状态 → paused（橙），再次点击恢复
- **⏹ 停止**: 模态确认 → 状态 → cancelled（灰）
- **⏭ 跳过**: 立即跳过当前步骤，状态保持 running

#### 键盘控制
- `Space`: 暂停/恢复
- `Esc` 单击: 暂停
- `Esc` 双击: 停止
- `→`: 跳过

### 7.3 代码回退流程

```
时间线面板（左侧滑出）
   ↓
显示 N 步历史（每步一个节点 + 摘要）
   ↓
hover 节点 → 显示该步 diff preview
点击节点 → 打开 Diff Preview 模态
   ↓
模态显示: [仅看 Diff] [创建分支回退] [直接回退]
   ↓
选择"直接回退" → 5s 倒计时确认
   ↓
执行回退 → Toast [撤销] (4s 可点击)
   ↓
点击 Toast [撤销] → 恢复到回退前状态
```

### 7.4 思考过程可视化

#### 折叠/展开
- **默认**: 折叠（仅显示标题栏）
- **hover 标题栏**: 展开预览（前 200 字符）
- **点击标题栏**: 完整展开
- **再次点击**: 折叠

#### 阶段切换动画
- **分析阶段** (蓝): 用户输入解析
- **设计阶段** (紫): 架构设计、方案选择
- **编码阶段** (绿): 代码生成
- **验证阶段** (橙): 测试、评审

每个阶段切换时：
1. 旧阶段内容淡出（200ms）
2. 新阶段标题高亮（脉冲 1 次）
3. 新阶段内容 typewriter 出现（200ms/字符）

#### 流式渲染优化
- **rAF 节流**: 60fps
- **200ms 防抖**: 避免过度重渲染
- **react-window 虚拟列表**: 超过 100 步时启用

---

## 8. 移动端交互适配

### 8.1 触控目标

#### 最小尺寸
- **按钮**: 44×44pt（iOS HIG）/ 48×48dp（Material）
- **列表项**: 最小高度 48px
- **图标按钮**: 32×32pt（含 padding 后 44×44pt）

#### 间距
- 元素间最小间距: 8px
- 避免误触: 主要操作之间间距 ≥ 16px

### 8.2 手势支持

| 手势 | 操作 | 适用范围 |
|------|------|---------|
| 左滑 | 删除/归档 | 列表项 |
| 右滑 | 标记已读 | 消息 |
| 下拉 | 刷新 | 列表顶部 |
| 上滑 | 加载更多 | 列表底部 |
| 双指捏合 | 缩放 | 编辑器、图表 |
| 长按 | 多选模式 | 列表项 |
| 左右拖动 | 切换 Tab | Tab Bar |

### 8.3 移动端特有交互

#### 底部 Tab Bar
```
┌──────────────────────────────────────────┐
│  🏠  💬  ⚡  📊  ⚙️                       │
│ 主页  对话 工作  统计 设置                │
└──────────────────────────────────────────┘
```

#### 浮动操作按钮 (FAB)
- 位置: 右下角
- 主操作: Cmd+I 唤起 AI
- 样式: 圆形 56×56px，主色背景，白色图标

#### 抽屉式导航
- 左侧抽屉: 320px
- 手势: 边缘右滑唤起
- 遮罩: 半透明黑色

---

## 9. Toast 通知规范

### 9.1 类型

| 类型 | 图标 | 背景 | 持续 | 用途 |
|------|------|------|------|------|
| success | ✓ | 绿色 | 3s | 操作成功 |
| info | ℹ | 蓝色 | 4s | 信息提示 |
| warning | ⚠ | 橙色 | 5s | 警告 |
| error | ✕ | 红色 | 6s | 错误 |
| loading | ⏳ | 蓝色 | ∞ | 加载中 |

### 9.2 结构

```
┌──────────────────────────────────────┐
│ [图标]  标题                          │
│         描述（可选）                  │
│                       [撤销] [操作]  │
└──────────────────────────────────────┘
```

### 9.3 行为

- **位置**: 顶部居中（避免遮挡内容）
- **堆叠**: 最多 3 个同时显示，更早的自动消失
- **可关闭**: 鼠标 hover 时显示 ✕ 按钮
- **可撤销**: 重要操作显示 [撤销] 按钮（4s 内可点击）
- **进度条**: 底部细线显示剩余时间

---

## 10. 模态/弹窗规范

### 10.1 类型

| 类型 | 用途 | 行为 |
|------|------|------|
| Alert | 重要通知 | 单按钮 [确定] |
| Confirm | 操作确认 | 双按钮 [取消] [确定] |
| Prompt | 输入确认 | 输入框 + [取消] [确定] |
| Dialog | 详情展示 | [关闭] |
| Drawer | 侧边详情 | 右侧滑入 |

### 10.2 通用规范

- **背景遮罩**: `rgba(0,0,0,0.5)` + `backdrop-blur(4px)`
- **打开动效**: fade + scale(0.95→1) 200ms
- **关闭动效**: fade + scale(1→0.95) 150ms
- **Esc 关闭**: 默认支持
- **点击外部关闭**: 危险操作禁用
- **焦点陷阱**: Tab 键循环在模态内

### 10.3 确认模态模板

参见 [3.4 L3 弹窗确认规范]

---

## 11. 拖拽交互

### 11.1 支持场景

- **文件拖入**: 上传文件、附加到对话
- **列表项拖拽**: 排序、跨列表移动
- **面板拖拽**: 调整宽度、位置
- **Tab 拖拽**: 重新排序

### 11.2 视觉反馈

- **拖动中**: 元素半透明 + 阴影
- **可放置区**: 蓝色虚线边线
- **不可放置**: 红色边线 + ✕ 图标
- **放置完成**: 蓝色脉冲动画

---

## 12. 实时数据交互

### 12.1 WebSocket 连接

- **状态指示**: 右上角小圆点（绿/黄/红）
- **断线重连**: 指数退避 1s/2s/4s/8s
- **断线提示**: 顶部黄色 banner

### 12.2 流式响应

- **token-by-token 显示**: 200ms 防抖
- **光标动画**: 闪烁竖线
- **完成提示**: ✓ 图标 + 耗时

### 12.3 多端同步

- **乐观更新**: 立即反映用户操作
- **服务端确认**: 1-2s 后到达
- **冲突解决**: 模态提示用户选择

---

## 13. 无障碍交互

### 13.1 键盘导航

- **Tab 顺序**: 符合视觉顺序（左→右、上→下）
- **焦点环**: 2px 蓝色实线 + 4px 半透明光晕
- **跳过导航**: `Skip to main content` 链接
- **快捷键**: 不与浏览器冲突

### 13.2 屏幕阅读器

- **语义化 HTML**: button / nav / main / aside
- **ARIA 标签**: 所有图标按钮有 aria-label
- **状态变化**: aria-live="polite" 通知
- **跳过装饰**: aria-hidden="true"

### 13.3 减弱动效

- 自动检测 `prefers-reduced-motion`
- 所有动效降至 0.01ms
- 保留状态变化，仅移除过渡

---

## 14. 交互验收标准

### 14.1 必须达成（P0）

- ✅ 所有用户操作 100ms 内有视觉反馈
- ✅ 所有破坏性操作有 L3 或 L4 二次确认
- ✅ 所有可逆操作有 [撤销] 入口
- ✅ 所有错误状态有明确引导（不只显示错误码）
- ✅ 核心操作 100% 支持键盘
- ✅ 任何路径无死路（用户可随时退出）

### 14.2 应达成（P1）

- ✅ 80% 常用操作有快捷键
- ✅ Cmd+K 命令面板可用
- ✅ 批量操作支持 5 个以上场景
- ✅ 减弱动效模式自动适配
- ✅ 移动端最小触控目标 ≥ 44pt

### 14.3 可达成（P2）

- ✅ 快捷键可自定义
- ✅ 操作历史可追溯（30 天）
- ✅ 多端同步（桌面↔移动）

---

**文档完成时间**: 2026-07-29
**文档字数**: 5,200 字
**下一步**: 创建 Spec #3: 技术实现
