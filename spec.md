# 架构设计文档 (spec.md)

## 1. 模块视图

系统分为三个主要层次：前端可视化层、后端调度服务层、仿真环境层。各层内部模块划分如下：

### 1.1 前端可视化层 (React 18 + Vite + TypeScript)
- **认证模块**：负责用户登录、JWT Token管理、路由守卫。
- **地图渲染模块**：基于Canvas/SVG渲染2D栅格地图，显示AGV位置、航向、轨迹。
- **AGV状态监控模块**：列表展示AGV ID、任务、速度、电池、模式、健康状态。
- **任务管理模块**：任务队列视图（待执行/执行中/已完成/失败），支持按AGV过滤。
- **KPI看板模块**：系统级与AGV级KPI展示。
- **任务下发模块**：地图点击或坐标输入，选择AGV，支持批量下发。
- **紧急停止模块**：全局红色按钮，确认弹窗，广播停止指令。
- **告警面板模块**：实时推送告警，按严重程度筛选。
- **WebSocket客户端模块**：接收后端增量推送（位置、状态、告警）。

### 1.2 后端调度服务层 (ROS2 Humble + Python 3.10)
- **认证授权中间件**：JWT验证、角色权限（admin/operator/observer）。
- **AGV管理模块**：注册/注销AGV，维护数据库（ID、位置、速度、任务、能力标签）。
- **任务管理模块**：接收任务请求，维护队列，按优先级调度。
- **路径规划模块**：基于Navigation2全局规划器，支持多目标点排序。
- **避障模块**：基于Navigation2局部规划器，多AGV协同避让。
- **负载均衡调度模块**：采用最少负载算法+能力标签匹配。
- **三级执行策略路由模块**：将复杂任务拆分为规划、执行、校验三级子任务，协调AGV或内部模块。
- **拓扑排序循环依赖检测模块**：检测任务依赖图是否有环，拒绝产生循环的任务组。
- **紧急停止模块**：接收信号后向所有AGV发布停止指令，记录时间戳，0.5秒内确认动力切断。
- **人工干预管理器**：提供API暂停/取消/回滚任务，记录操作日志，终止子进程，回滚数据库事务。
- **状态变更推送模块**：通过WebSocket增量推送（位置、任务状态、告警），延迟≤1秒。
- **数据库访问层**：使用SQLAlchemy + aiosqlite，WAL模式，异步队列，写入重试3次。
- **日志模块**：基于ROS2日志框架，记录关键事件，级别可配置。

### 1.3 仿真环境层 (Gazebo + ROS2 Navigation2)
- **场景建模模块**：工厂/仓库环境，障碍物、货架、通道，≥20m×20m。
- **AGV模型模块**：差速轮底盘，带激光雷达、IMU、里程计，参数与真实硬件一致。
- **多AGV仿真模块**：支持10台AGV同时运行，AGV间通信，模拟冲突与避让。
- **环境噪声模块**：模拟传感器噪声（高斯噪声、里程计漂移）。

## 2. 接口契约

### 2.1 RESTful API (JSON)
| 接口 | 方法 | 认证 | 说明 |
|