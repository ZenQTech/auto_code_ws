# 架构设计文档 (spec.md)

## 1. 模块视图
### 系统模块划分
- **感知模块 (Perception)**: 负责处理2D LiDAR和IMU数据，生成环境感知信息（障碍物点云、里程计数据）。
- **定位模块 (Localization)**: 基于IMU和编码器数据，结合LiDAR点云进行实时定位，输出AGV位姿（x, y, theta）。
- **规划模块 (Planning)**: 接收目标点B和定位信息，生成全局路径，并根据实时障碍物信息进行局部避障规划。
- **控制模块 (Control)**: 将规划模块输出的速度指令（线速度、角速度）转换为电机控制信号，驱动AGV移动。
- **安全模块 (Safety)**: 监控障碍物距离和速度，触发急停或边界防护机制。

### 模块职责
- **感知模块**: 发布 `/scan` (LiDAR数据) 和 `/imu` (IMU数据) 话题。
- **定位模块**: 订阅 `/scan` 和 `/imu`，发布 `/odom` (里程计位姿) 和 `/tf` (坐标变换)。
- **规划模块**: 订阅 `/odom` 和 `/map` (地图，可选)，发布 `/cmd_vel` (速度指令) 和 `/plan` (路径)。
- **控制模块**: 订阅 `/cmd_vel`，驱动电机。
- **安全模块**: 订阅 `/scan` 和 `/cmd_vel`，若障碍物距离<0.3m或速度>1.0m/s，发布 `/emergency_stop` (急停信号) 到控制模块。

## 2. 接口契约
### 通信协议
- 采用 **ROS2** 的 Topic 和 Service 机制。
- 所有模块间通信均使用标准 ROS2 消息类型（如 `sensor_msgs/LaserScan`, `nav_msgs/Odometry`, `geometry_msgs/Twist`）。

### 模块间接口定义
| 源模块 | 目标模块 | 接口类型 | 数据格式 | 频率要求 |
|