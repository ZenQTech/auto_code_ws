# 架构设计文档 (spec.md)

## 1. 模块视图

系统划分为以下核心模块，职责清晰、接口明确：

### 1.1 调度中心 (Scheduler)
- **职责**：负责全局任务分配、路径规划（A*全局、DWA局部）、交通管制（区域互斥、电子围栏）、死锁检测（拓扑排序）、三级执行策略路由决策、AGV状态监控与异常处理。
- **接口**：
  - 与AGV客户端：通过ROS1话题（Topic）和服务（Service）通信，接收AGV状态、传感器数据，下发控制指令和任务。
  - 与Web管理界面：通过FastAPI REST API和WebSocket发布任务状态、AGV状态；接收任务创建/取消、紧急停止指令。
  - 与数据库：通过aiosqlite（异步SQLite）进行数据持久化。

### 1.2 AGV客户端 (AGV Client)
- **职责**：运行在每台AGV上，负责执行调度中心下发的路径规划任务（局部轨迹跟踪）、传感器数据采集（激光雷达、IMU）、物理急停响应、状态上报（位置、速度、电量、任务状态）。
- **接口**：
  - 与调度中心：通过ROS1话题/服务与调度中心交互。
  - 与硬件：通过ROS驱动控制差速底盘、读取激光雷达和IMU、响应物理急停按钮。

### 1.3 Web管理界面 (Web UI)
- **职责**：提供实时AGV监控（地图、位置、速度、电量、任务状态）、任务管理（创建、取消、查看）、紧急停止、用户认证（JWT）、健康检查。支持浏览器访问。
- **接口**：
  - 与调度中心：通过FastAPI REST API（如`/tasks`、`/agvs`、`/emergency_stop`）和WebSocket（`/ws/status`）通信。
  - 与用户：通过HTTP/HTTPS提供Web页面和API。

### 1.4 数据库模块 (Database)
- **职责**：存储任务记录、AGV轨迹、故障日志、系统配置。支持WAL模式、异步写入、重试与备用数据库切换。
- **接口**：
  - 与调度中心：通过aiosqlite库提供异步数据库访问接口。

## 2. 接口契约

### 2.1 调度中心 ↔ AGV客户端 (ROS1)

| 接口名称 | 类型 | 数据格式 | 频率/触发条件 | 方向 |
|